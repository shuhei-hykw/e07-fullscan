# Dependencies are managed with pyenv + requirements.txt (see README).
# This file only holds tool configuration.

[tool.pytest.ini_options]
markers = [
  "slow: mark test as slow (run with pytest -m slow)",
]

[tool.ruff]
line-length = 100
target-version = "py310"
