# E07 フルスキャン — 解析ノート

開発日誌 + リファレンス。結果だけでなく議論・仮説・行き詰まり
の記録も含む。エントリは **最新が上**（逆時系列）。見出しは
`## YYYY-MM-DD HH:MM JST — タイトル`。
旧 ANALYSIS.md（英語）/ ANALYSIS_ja.md を 2026-07-11 に統合。
英語原文は git 履歴に残る。技術 API リファレンス: README.md。

**開始:** 2026-05-08

---

## リファレンス

### スキャン形状

| 項目 | 値 |
|---|---|
| プレート | MOD108 / PL12 / tohoku-v1 / AREA00 |
| ビュー数 | 2025（45 × 45 グリッド）|
| FOV 間隔 | ~0.5 mm |
| FOV サイズ | ~594 × 594 μm |
| **ピクセルスケール** | **0.29 μm/px**（スキャナ JSON は 3.0 → 誤り）|
| 乳剤深さ | ~150 μm |
| スライス数 | ~100、z 間隔 ~1.5 μm/スライス |

0.29 μm/px の確認：2048 px × 0.29 = 594 μm ≈ 0.5 mm FOV 間隔。

### パイプライン

```
SPNG 画像
    │  [スライス・ビューごと]
    ▼  Z 投影 → フォグ除去 → Otsu 二値化 → ノイズ除去
    │  HoughLinesP
    ▼  トラックセグメント（chunk_NNNN.parquet、計 ~24M トラック）
    │  品質指標: length_px, mean_intens, angle_deg, n_grains, grain_density
    ▼  全ペア交点計算 + エンドポイント確認
    │  スライスごとの vertex 候補（vertices.parquet）
    ▼  z スライス間の XY 近傍マージ
    マージ済み vertex 候補（vertices_merged.parquet）
```

### 物理目標

**現フェーズ**: 任意の反応 vertex の efficiency 優先選択。
対象: ビーム反応、単一 Λ 超核、α 崩壊チェーン、核星。

**最終目標**: ダブル超核（ΛΛ）探索。
シグネチャ：同一ビュー内で ~100–500 μm（30–167 px）離れた 2 つの connected vertices：
1. **プライマリ vertex** — ビーム + 標的 → ΛΛ超核 + 他粒子
2. **セカンダリ vertex** — 超核の弱崩壊（kink または小さな星）

**重要方針：efficiency 最優先（purity は後回し）**
specials_x20 はすべてダブル超核イベントであり、ほかにも多数の vertex が存在する。
ダブル超核 vertex を loss しないことが最重要課題。

### ビーム方向

ビームは乳剤面の X 方向（画像の水平方向）に進む。
全トラックの 22% は `angle_deg < 15°` または `> 165°`（ビーム方向）。
`beam_angle_cut = 15°` でこれらを除去。

---

---

## 未解決課題 / 次のステップ

- [x] **グラウンドトゥルース記録**: `tests/specials_gt.json` 完成（2026-05-12）
- [x] **vertex miss の根本原因**: グリッドハッシュクラスタリングバグ → KDTree union-find で修正（2026-05-13）。全 9 イベントが position test をパス。
- [x] **v5 vertex ラン（KEKCC）**: 完了（2026-05-13）。n_tracks≥10 が +83.5%。
- [x] **2 頂点探索**: `find_vertex_pairs()` 実装（2026-05-13）。p_ntracks≥10 で 5,059 候補。
- [ ] **ΛΛ 候補の目視確認**: vertex_pairs_v5 上位候補の 2 頂点 crop を生成してスキャン。
- [ ] **2D 解析の試験実装**: ±2〜4 スライス重ね合わせ + コントラスト改善（CLAHE）。
- [ ] **前処理修正**: `noise_amax_upper` を `preprocess()` に追加；KEKCC 再実行が必要。
- [ ] **ティーチャーデータ拡充**: 更新された vertex 結果から 100–200 クロップを目視確認。
- [ ] **トラック再解析**: `px_scale=0.29` で `e07analyze` を再実行（`grain_density` 修正）。
- [ ] **grain density による PID**: 修正後は α / 遅いプロトン / MIP の識別に使用可能。

---

## 開発ログ（最新が上）

## 2026-09-08 22:00 JST — 密度の壁は越えられた: 分類器フィルタ＋最適化1件で cell=6 が 2.77 時間/view に

同日前半の結論は「壁は格子ではなく密度」「cell=6 は 1 region 25分超で
非現実的」だった。**両方とも覆った。** 打ち手は2つ。

### 打ち手1: エクスポート段階のノイズフィルタを分類器モードに

`export_hits_grid` の `denoise_method` を実タイルで比較した
（cell=6、既存512ラベルで学習した `track_classifier`。**人手追加なし**）:

| method | hits | hits/region | 所要 |
|---|---|---|---|
| `legacy`（既定）| 1,667,324 | 6,513 | 98 s |
| `threshold` | 1,717,924 | 6,711 | 957 s |
| **`classifier`** | **784,522** | **3,065** | 853 s |

`threshold`（手決めの cov_frac/max_gap カットオフ）は legacy より
**甘く、10倍遅い**——使う理由が無い。`classifier` は **2.1倍**落とす。

### 打ち手2: `_merge_overlapping` を全セグメント同時に

`refine_endpoints` を直した後、密な実 region では
**`_merge_overlapping` が段1の85%**になっていた。中身は
「この hit を自分以外のセグメントが取っているか」をセグメントごとに
(N, M-1) の列スライスを作り直して数える O(N·M²)。
これは**行ごとの claim 数から自分の bit を引く**だけで全セグメント分が
一度に出る。

| N | 前 | 後 |
|---|---|---|
| 2,300 | 110.9 s | **18.4 s** |
| 3,013 | 331.8 s | **39.0 s** |

参照は 1,239本 全一致・最大 2.5e-13 px のまま。

### 結果: cell=6 が使える

| | cell=30 (legacy) | **cell=6 (classifier)** |
|---|---|---|
| hits/view | 240,824 | 784,522 |
| hits/region（中央値）| 972 | 3,024 |
| エクスポート | 80 s | 853 s |
| 段1 | 2,449 s | **約 2.77 h** |
| シミュレーションでの purity | 17.5% | **75.9%** |

**合計 約3時間/view。** LSF queue `h`（walltime 8日、4 GB）に
1 view/job で投げれば、2025 view は同時 200 本で約1.5日。
段1 のメモリは実測 0.3 GB なので 4 GB 制限に余裕で収まる。

### この日の段1 高速化まとめ（すべて厳密、参照は不変）

| 修正 | 効果 |
|---|---|
| `refine_endpoints` は動いた列だけ | N=800 で 176.9 → 4.23 s |
| `_merge_overlapping` を同時計算 | N=3,013 で 331.8 → 39.0 s |

当初の実 region（861点）186 秒からの積み上げで、
**実 E07 の段1 は当初推定 21時間/view から 2.77時間/view（cell=6）**、
cell=30 なら 41分/view。移植直後の MATLAB 換算（約9.2時間/view、
しかもシミュレーション密度）と比べても実データで速い。

### 残る宿題

- **`z_scale` は未切替**（`E07_Z_SCALE = 1.5/0.29`）。実データでしか
  検証できない。cell=6 の出力が出たら効果を測れる。
- **cell=6 の end-to-end はまだ通していない**（段1 に 2.77 時間かかる
  ので、ワークサーバではなく LSF に投げるべき）。
- `_GRAPH_CELL_PX = 6` は既定にしてあるが、`denoise_method` の既定は
  まだ `legacy`。分類器モードは学習済みモデルを渡す必要があるので、
  既定を変えるならエクスポート CLI 側の手当てが要る。

## 2026-09-08 — グラフ検出器が実 E07 タイルで通るようになった（厳密な最適化3件で 27時間 → 45分）。ただし壁は格子ではなく**密度**

Phase 2 の宿題「6 px の実 E07 データでのコストは未測定」を測った。
測ったら**まず走らなかった**ので、走るようにしてから測り直した。
**最適化はすべて厳密**で、参照照合（段1 1,239本 全一致、段2 149本、
段3 eff 92.9%/purity 92.3%）は一切変わっていない。

### エクスポートは安い。壁は段1だった

実タイル `V00000000`（58スライス、2048²、前景 9.7%）:

| | cell=30 | cell=6 |
|---|---|---|
| エクスポート | 240,824 hits / 80 s | 1,667,324 hits / 97 s |
| hits / sub-region（中央値）| 972 | 6,732 |

シミュレーションは 163 hits/region なので、**実 E07 は cell=30 でも
6倍、cell=6 なら 41倍密**である。そして段1 は当時 **N^2.6** だった:
861点の region 1個に **186 秒**。256 region で 21時間/view、
2025 view で約5年。

### 最適化1: `refine_endpoints` は動いた1本だけ計算し直せばよい（42倍）

プロファイルすると **97% が `membership_from_segments`**（400点で
約20,000回呼ばれる）。端点を1つずらしても**変わるのはその
セグメントの列だけ**なのに、MATLAB は候補シフトごとに全セグメント分を
計算し直していた。他の列は1パスにつき1回にし、動く列だけ更新した。

| N | 前 | 後 |
|---|---|---|
| 400 | 36.1 s | 1.63 s |
| 800 | 176.9 s | **4.23 s** |

指数 2.6 → 1.6、参照 view は 14.0 → 8.4 秒。
**MATLAB の癖を1つ意図的に引き継いだ**: 元コードは membership 行列に
「選んだ候補」ではなく「**最後に試した候補**」の結果を残し、次のパスが
それに当てはめて再フィットする。書き直し側も同じ行列を再構成している。

### 最適化2・3: 段2 の全走査を2箇所やめた

- **端点探索を k-d tree に**。箱の半幅で割ると Chebyshev 半径1の球に
  なる。セグメントは移動せず消費されるだけなので木は1回作れば足りる。
- **`_resample` と `attachment_codes` の hit 走査を近傍クエリに**。
  どちらも「この折れ線はどの hit を持つか」を全 hit 走査で答えていた。
  曲線から reach 以内の hit は、いずれかのセグメント中点から
  「半長 + reach」以内にあるので、セグメントごとの球クエリが上位集合に
  なる。シミュレーションで段2 8.1→0.9 秒、段3 0.8→0.3 秒。

### 定数スケーリングの分類が間違っていた（測って直した）

`for_spacing()` は全ての長さを間隔に比例させていた。これだと
cell=30 で端点探索箱が 50→**500 px** になり、view の1/4を覆う。
実タイルの段2 が **60分超でも終わらなかった**原因はこれ。

シミュレーションを再間引きして3案を比較した:

| block | 全部スケール | **間隔のみ** | スケールなし |
|---|---|---|---|
| 6 | 90.2% / 66.0% | **87.8% / 75.5%** | 85.4% / 75.9% |
| 10 | 78.0% / 54.0% | **75.6% / 67.3%** | 75.6% / 58.9% |
| 30 | 51.2% / 20.0% | 29.3% / 16.9% | 53.7% / 17.5% |

**弱いのは purity なので、間隔のみスケールが正しい。** 定数は3群に
分かれる:
- **間隔スケール**（次の hit までどれだけ探すか）= `grow_margin`、
  `refine_dl*`、`single_point_max_dist`。**これだけスケールする。**
- **透過方向の許容値**（軌跡の幅＋測定誤差）= スケールしない。
- **軌跡幾何のギャップ**（軌跡の断片同士がどれだけ離れうるか、
  vertex にどれだけ届かず終わりうるか）= `neighbour_xy`、`end_reach`、
  `vertex_merge` など。**物理と小領域格子が決めるのでスケールしない。**

実タイルの段2: **60分超で未完 → 154 秒**。

### 段3 は表現を変えないと動かなかった

`attachment_codes` は密な (N, M) を返していた。実タイルは
241,000 hits × 29,000 polylines = **7 GB**（kekcc の全 queue が 4 GB）。
`branch_points` は M² = **8.4億ペア**を回していた。
→ codes を疎行列に、adjacency を疎積に、連結成分を
`scipy.sparse.csgraph` に（先頭メンバ順に振り直して走査順非依存に）、
`branch_points` はペアではなく **junction hit を歩く**（1 hit に付く
折れ線は数本なので O(H·k²)）。

### 実タイル end-to-end

| 段 | 結果 | 時間 |
|---|---|---|
| エクスポート | 240,824 hits | 80 s |
| 段1 | 44,360 segments | 2,449 s |
| 段2 | 28,988 polylines（総長 1,282,196 px）| 163 s |
| 段3 | 20,511 groups、**8,118 branch points**（多重度 2〜4）| 10 s |
| | | **合計 約45分/view** |

**走るようになった。が、出力は使い物にならない。** シミュレーションが
149本/view の折れ線を出すところで実 E07 は **28,988本**——約200倍
断片化している。branch point も 13個に対し **8,118個**。多重度も
最大4止まり（シミュレーションは8）。

### 結論: 壁は格子サイズではなく**軌跡密度**

Phase 2 前半の結論は「30 px は粗すぎる、6 px にせよ」だった。それは
シミュレーション上では正しいが、**実 E07 では 6 px が現実的でない**
——1 region（6,732点）が段1 で 25分以上かかる（256 region で
4日/view 超）。そして cell=30 の時点で既に密度が200倍で、
グラフ検出器が想定した「159本/view」から桁が違う。

つまり `_GRAPH_CELL_PX` を下げるだけでは解けない。次に効くのは
**エクスポート段階での密度削減**（ノイズフィルタの強化、
`remove_unaligned_noise_v2` の分類器モード）か、**小領域を細かくして
1 region あたりの点数をシミュレーション並みに保つ**か、いずれか。

### 副産物: Method A は LSF に投げられる（7月の結論を撤回）

`config/kekcc.yaml` に**3つの致命的バグ**があった:
1. input パスに `E07/` の階層が欠落（全ジョブが即死する）。
2. `scripts/kekcc_job.sh` が存在しない `module.analyze` を呼んでいた
   （正しくは `module.pipeline`）。
3. `n_jobs: 135` ＝ 15 view/job。**実測**: 1 view 1.55 GB、
   2 view 4.35 GB、4 view 7.10 GB（結果をチャンク終了までメモリに
   貯めるため）。**7月の「Method A は 6.75 GB なので LSF に投げられない」
   はチャンク4 view 相当の測定**であって、**1 view なら 1.55 GB で
   楽に収まる**（実測 2分/view、CPU 4.5分）。
→ `n_jobs: 2025`（1 view/job）に。出力先も `results/fullscan_v7` に
分けた（`results/` の 2026-05-14 産物と chunk 名が衝突するため）。
ジョブスクリプトは conda が無いノードでも `myenv` の python を
使うようにし、既存ファイルがあれば上書きせず落ちるようにした。

### 副産物: mg=40 の④への影響（全域再解析の前提条件だった測定）

実 view 1枚、本番パラメータ vs mg=5:

| | mg=5 | mg=40 |
|---|---|---|
| tracks/view | 47,490 | 634,577（**13.4倍**）|
| 品質カット後 | 2,340 (4.9%) | 6,653 (1.0%) |
| vertex 候補 (n≥3) | 193 | 2,533（**13.1倍**）|
| 最大多重度 | 7 | **19** |
| n≥8 の候補 | **0** | **66** |

候補は13倍に増えるが、**mg=5 では高多重度の星が原理的に見えない**
（この view で n≥8 がゼロ）。ΛΛ の一次反応点は高多重度なので、
これは単なるノイズ増ではなく**見えていなかったものが見える**変化である。
全域で 193→2,533/view ＝ 約5.1M 候補になるので、④のフィルタ強化が
同時に要る。

## 2026-09-07 — Phase 2: **エクスポートの 30 px 格子が粗すぎることを実測**。purity 78.8% → 17.5%

移植3段が揃ったので、E07 実データ向けの較正に入った。最初に測ったのは
**「シミュレーションの点間隔 3 px と、実データ export の 30 px の差は
何をするか」**である。答えは「壊す」だった。

### 方法: 真値を保ったまま点間隔だけ変える

実 E07 データには真値が無いので、較正の目的関数が作れない。代わりに
**同じシミュレーションを別のブロックサイズで間引き直す**。真の分岐点は
`Summary` から来るので間引きに影響されない。そのために `mabiki.m` を
`module/graphdet/downsample.py` へ移植した（`dspl` と**完全一致**を確認）。

`python scripts/scan_sampling.py --events 1-3`（真の分岐点 41 個、
一致半径 25 px、MATLAB 定数のまま）:

| block | hits | 線分 | 軌跡 | 頂点 | efficiency | purity |
|---|---|---|---|---|---|---|
| **3**（シミュレーション） | 139,647 | 4,180 | 489 | 52 | **92.7%** | **78.8%** |
| 6 | 79,391 | 4,127 | 491 | 54 | 85.4% | 75.9% |
| 10 | 52,565 | 4,085 | 513 | 73 | 75.6% | 58.9% |
| 15 | 38,105 | 4,031 | 546 | 84 | 70.7% | 34.5% |
| 20 | 30,675 | 4,005 | 591 | 102 | 48.8% | 20.6% |
| **30**（現 export） | 22,893 | 3,865 | 801 | 120 | **53.7%** | **17.5%** |

**現在の `_GRID_CELL_PX = 30` は purity を 78.8% → 17.5% に落とす。**

### どの段が壊れているか

段ごとに切り分けたところ、**段1・段2 は 30 px でもほぼ無傷**だった
（事象1、長さ定数を間隔に合わせてスケールした場合）:

| | block 3 | block 30 |
|---|---|---|
| 軌跡数 | 149 | 159 |
| 軌跡総長 | 118,284 px | 116,176 px（**98%**）|
| 真の頂点の 25 px 以内に折れ線の頂点がある | 14/14 | **14/14** |

**壊れているのは段3だけ**である。理由は構造的で、定数では直らない:
`detectbunki` は「**1.5 px 以内で共有された hit**」で分岐を判定する。
hit こそが 10 倍疎になったものなので、共有 hit が消える。
実際 `attach_max_dist` を 1.5→12 px に緩めても efficiency は 57→64% で
頭打ちになり、頂点数だけ 20→119 に増えて purity が 30%→4% に落ちた。

### 代案（共有 hit をやめる）も試作したが解にならなかった

「折れ線の端点が別の折れ線に近い」という**幾何的な判定**を試作した
（3事象、長さ定数はスケール済み）:

| 判定 | block 3 | block 30 |
|---|---|---|
| 共有 hit（移植版） | 92.7% / 78.8% | 51.2% / 20.0% |
| 端点近接 reach=20 | 90.2% / 52.9% | **82.9%** / 26.1% |
| 端点近接 reach=30 | 92.7% / 39.7% | 85.4% / 24.1% |

**efficiency は 51%→83% に戻るが purity は 26% までしか戻らない。**
粗い格子では軌跡が断片化し、断片の端点が別の軌跡の近くで終わるだけで
偽の分岐になるため。**採用せず、コードには入れていない**（数字はここに
残したので必要なら再現できる）。

### 結論: 定数を直すのではなく、細かくサンプルする

`_GRID_CELL_PX = 30` は 2026-07-11 に **MATLAB の
`detectlseg_smallregion` を約2.5時間/tile に収めるため**に選ばれた。
**その制約はもう無い**——移植版は同density換算で約250倍速い。上の表の
膝は **6 px**（purity 75.9%、efficiency 85.4%）にある。

→ `matlab_export._GRAPH_CELL_PX = 6` を新設し、`export_hits_grid` と
CLI の既定にした。ノイズフィルタと viewer overlay は別に 30 px で
チューニングされているので `_GRID_CELL_PX = 30` のまま分離した
（`noise_cell` 引数）。

**未測定のリスク**: 6 px の実 E07 データでのコストは測っていない。
シミュレーションは 159 本/view だが実 E07 はもっと桁違いに密である。
エクスポートを回す前に 1 tile で実測すること。

### 較正の足場を作った

- `module/graphdet/config.py` — `DetectorConfig`。3段の全ての距離定数を
  1箇所に集め、`cfg` として渡せるようにした。既定は MATLAB 値そのまま
  なので参照照合（段1 全一致、段2 149本）は不変。
- **透過方向の許容値と長さスケールを分けた**のがこのファイルの要点。
  前者（`th_split`、`attach_max_dist` など）は軌跡の幅と測定誤差なので
  **間隔と一緒にスケールしてはいけない**。後者（`grow_margin`、
  `end_reach` など）はスケールしなければならない。
  `DetectorConfig.for_spacing()` が後者だけを直す。
- `module/graphdet/simeval.py` — 真値の復元と efficiency/purity 採点。
  `eval_branches.py` と `scan_sampling.py` が共用する。

### 次

1. 実 E07 タイル 1 枚を `--cell-px 6` でエクスポートし、**コストを実測**。
2. `geom.MATLAB_Z_SCALE` を E07 の `E07_Z_SCALE = 1.5/0.29` に切り替える
   （定義は入れたが、まだ既定にしていない）。シミュレーションでは
   検証できない——正しい値が 3.0/0.29 だから。
3. specials_x20 の既知 vertex で「分岐グループの順位」を見る。

## 2026-09-07 — Phase 1-3: `detectbunki` を移植。**このパイプラインの efficiency/purity を初めて測定**（96.2% / 74.0%）

MATLAB グラフ検出器の第3段（分岐している軌跡のグループ化）を
`module/graphdet/branch.py` へ移植し、移植3段が揃った。そして
**「MATLAB と同じか」ではなく「使えるか」を初めて測った**。

### 真値がある

`simdata8.mat` の `Summary` は各シミュレーション飛跡の始点・終点を
持つ。**2本以上の飛跡が端点を共有していればそれが真の分岐点**である
（`detect_tracks.m` L61-64 のコメントアウト部分）。10事象で
**真の分岐点 130 個**（多重度 2〜13）を復元できた。

### 結果（10事象、真 130 個 vs 再構成 215 個）

距離は等方化して測る（3µm スライス / 0.29µm px なので 1 スライス ≒
10 px。生のユークリッド距離だと 1 スライスのずれを 1 px と誤認する）。

| 一致半径 | efficiency | purity |
|---|---|---|
| 10 px | 93.1% (121/130) | 55.3% (119/215) |
| **25 px** | **96.2% (125/130)** | **74.0% (159/215)** |
| 50 px | 96.2% (125/130) | 79.5% (171/215) |
| 100 px | 96.2% (125/130) | 85.1% (183/215) |

**efficiency は高い（96.2%）。弱点は purity。** 真値 130 個に対して
215 個を出しており、25 px では 56 個が偽である。この非対称は Method A
（Hough）の④と同じ症状だが、**程度は桁違いに軽い**——Method A は
1タイル 196〜380 候補に対し正解 1 個（purity 0.3〜0.5%）だった。

補足2点:
- 一致した 125 個のうち **4 個は、別の真の分岐点と同じ再構成頂点を
  共有している**。事象1の `[1470,1456,46]` `[1484,1456,46]`
  `[1492,1462,46]`（互いに 14〜22 px）は 1 個に潰れた。**分解能は
  efficiency の数字が示すほど良くない。**
- 偽 56 個の一部は「3本の軌跡の胴体が交差した点」である可能性がある。
  判定は hit の符号和 > 2 なので、胴体+胴体（1+1）は除外されるが
  **胴体3本（1+1+1）は分岐として通る**。

### 移植とその外

`detectbunki` 本体は素直だった。MATLAB は 0/1 距離行列に
`linkage`+`cluster`(cutoff 0.5) をかけているが、**single linkage の
既定でこれは連結成分そのもの**なので、密な M×M 距離行列を作らず
BFS で置き換えた（O(M³)→O(M²)）。

`branch_points()` は**移植ではなく追加**である。`detectbunki` は
グループしか返さず頂点座標を返さないが、E07 が要るのは座標なので、
グループ化が既に見つけている「共有 hit」から導出した：軌跡の各ペアの
共有 hit の重心を候補とし、近いもの（等方 10 px 以内）を統合する。

### 副産物: 事象2以降で退化ケースが露見

事象1には長さ 0 の線分が無かったが、事象2以降には有る。MATLAB は
`v./norm(v)` で NaN を作り、`min` が NaN を飛ばすことで結果的に
処理していた——**ただし候補が全部 NaN のとき `min` は NaN と添字1を
返し、`~isinf(NaN)` が真なので誤って接続する**。移植側は退化線分を
inf（接続不可）として明示的に弾いた。事象1の結果は不変
（polylines 149本、参照との照合も不変）。

### 速度の注意

事象1〜5 は stage1+2+3 で 26〜59 秒/view、事象6〜10 は 130〜397 秒。
**これはデータではなくワークサーバの 4 コア制限**（cgroup
`restricted_user`）。3段すべてが同じ比率で遅くなっていることから
外部要因と判断できる。素の値は事象1の **24.5 秒/view**。

### 次

Phase 2（定数を E07 に較正）。目的関数がここで決まった：**真の分岐点
に対する purity**。まず効くのは `geom.MATLAB_Z_SCALE`（E07 は
1.5µm/スライスなので z が2倍過大）と `_GRID_CELL_PX = 30`（実データの
点間隔がシミュレーションの10倍粗い）。

## 2026-09-07 — Phase 1-2: `integrate_smallregions` を移植。149本一致、ただし**参照はビット再現不能**と判明

MATLAB グラフ検出器の第2段（小領域ごとの線分 → 軌跡の折れ線）を
`module/graphdet/{integrate,polyfit}.py` へ移植した。参照の `lseg`
1,239線分を入力に与えると **polylines を 149 本ちょうど**返し、
全長は 118,270 px（参照 118,033 px、差 0.2%）、**全ての折れ線が参照の
どれかから 16 px 以内**に収まる。生 hit から通した所要は
stage1 14.0 秒 + stage2 10.5 秒 = **24.5 秒/view**。

### 一番の発見: このアルゴリズムは端点で数値的に不安定

Phase 1-1 は 1,239 セグメント全てが 2.5e-13 px 一致だったので、同じ
基準を当てたところ **149本中 97本しか一致しない**。原因を追うと、
アルゴリズムそのものの ill-conditioning に行き着いた。

`resamplingpoly` は折れ線ごとに `ell>=0 & ell<=lpoly & err<TH` で
「自分の hit」を選び直す。ところが**折れ線の端の頂点は、定義上その
折れ線の一番外の hit を直線に射影した点そのもの**である。したがって
その hit の `ell` は厳密には 0 で、実際には丸め誤差で ±1e-13 に
落ちる。実測すると **175本の chain すべてが、始点側に1個・終点側に1個、
`|ell| < 1e-8` の hit を持っていた**（合計 351 個）。この 2 個が
入るか外れるかで、端点が約 1 px 動く。

MATLAB の BLAS と NumPy の BLAS はここで最後の 1 bit が食い違う。
`np.linalg.norm` を BLAS の `dnrm2` に差し替えると一致本数は 97→99 に
しか動かず、単一の式の違いではなく ulp レベルの揺らぎだと確認できた。
**つまり参照 `polylines` はこの計算機では再現できない。**

### 採った方針: 境界 hit は常に含める（`_ELL_SLACK_PX = 1e-9`）

3案を実測して比較した:

| 方針 | 本数 | 完全一致 | Hausdorff ≤5px | 最悪 |
|---|---|---|---|---|
| MATLAB 逐語（`>= 0`） | 150 | 97 | 93.3% | 76.4 px |
| 常に含める（採用） | **149** | 74 | 94.0% | **16.1 px** |
| 常に除く | — | — | — | **落ちる** |

「常に除く」は、hit を 1 個ずつ削り続けた折れ線が hit ゼロになって
例外を出す。「常に含める」は厳密演算での `>= 0` の意味と一致し、
**本数が参照とちょうど揃い**、最悪誤差も逐語版の 1/5 になる。
完全一致本数は減るが、これは**参照自体が再現不能なコイントスの
1 標本**であって最適化すべき目的関数ではない。決定性（BLAS 実装に
依存しない）を採る。

### 検証の測り方も変えた

頂点リストの一致で測ると「同じ軌跡だが曲がり角の頂点が hit 数個分
ずれている」だけで不一致になる。折れ線を 1 px 間隔で再サンプリング
して**曲線同士の Hausdorff 距離**で測るよう
`scripts/check_polylines_reference.py` を書いた。軌跡に頭も尻尾も
無いので進行方向は問わない。

### MATLAB 側で見つけた不具合

- `kousinflag` を宣言して `koushinflag` に代入している（綴り違い）。
  後者は未定義変数への代入なので logical 配列として新規作成され、
  結果的に動く。宣言側は死にコード。
- 最終行 `polylines(koushinflag) = resamplingpoly(polylines(koushinflag),x)`
  は、`resamplingpoly` が重複折れ線を削除すると左右の要素数が合わず
  MATLAB が例外を出す。参照ランでは削除が起きなかったので通っている。
  移植側は「削除されたものは取り除く」（本来の意図）にした。
- `pixellist2poly` の flag=2/3 分岐（最小全域木で hit を並べ替える）は
  全点間距離行列を作るので view 規模では動かない。本番経路は flag=1
  しか通らないため移植していない。

### 次

Phase 1-3（`detectbunki`）。`simdata8.mat` の `Summary` から**真の
分岐点14個**（多重度 最大6）を復元できるので、**このパイプラインの
efficiency / purity をここで初めて測れる**。

## 2026-08-22 — Phase 1-1: `detectlseg_smallregion` を Python へ移植。参照出力と完全一致、1 view 9時間 → 14秒

MATLAB グラフ検出器（`e07/matlab`）の第1段を `module/graphdet/` へ
移植した。**`work1.mat` の参照出力と 1,239 セグメント全てが一致**
（最大端点誤差 2.5e-13 px＝倍精度の丸め）。

### 参照データが手元にあった

移植の検証に MATLAB は不要だった。`e07/matlab/` の `.mat` が入出力の
対を保持していた:

| ファイル | 中身 |
|---|---|
| `simdata8.mat` | シミュレーション10事象。事象1は hit 148,634点 → `mabiki(·,3)` で 41,609点。`Summary` に159本の飛跡（始終点・edep・pid） |
| `work1.mat` | その41,609点に対する出力: `lseg` 1,239線分、`polylines` 149本 |

`work1.mat` の `x` は `simdata8` の `dspl{1}[:,:3]` と完全一致を確認。
さらに `Summary` から**真の分岐点14個**（多重度 最大6）を復元できる
（`detect_tracks.m` L61-64 の計算、本体ではコメントアウト）。
**このパイプラインの efficiency/purity は未測定**なので、Phase 1-3 で
初めて測れる。

### 結果

| | MATLAB | Python 移植 |
|---|---|---|
| セグメント数 | 1,239 | **1,239** |
| 端点の最大差 | — | **2.5e-13 px** |
| 1 view (256領域) | 約9.2時間※ | **14.0 秒** |

※ MATLAB の 129.6 s/領域 は 2026-07-12 に**実 KISO データ**（約424点/
領域）で測った値。今回の参照は**シミュレーション**（163点/領域平均、
中央値151、最大508）なので、この 2,370倍 という比は密度が揃っていない。
移植側の実測は N 400-600 の領域で 0.60 s なので、**同density換算では
約250倍**が妥当な見積り。

領域あたりの実測（移植側、シミュレーション）:

| N/領域 | 領域数 | 平均 | 最大 |
|---|---|---|---|
| 0-100 | 57 | 0.015 s | 0.033 s |
| 100-200 | 123 | 0.040 s | 0.135 s |
| 200-300 | 63 | 0.080 s | 0.201 s |
| 300-400 | 11 | 0.158 s | 0.379 s |
| 400-600 | 2 | 0.603 s | 0.634 s |

速い理由は2つ。(1) `pdist(x,@distfun1)` の `distfun1` は**非線形では
なく z を 3/0.29 倍しただけのユークリッド距離**なので、スケーリングを
一度だけ適用すれば距離計算を完全にベクトル化できる（MATLAB は
カスタム距離関数のため全ペアで関数呼び出しが発生する）。(2) `lseg2L`
は端点候補ごとに呼ばれるホットスポットだが、線分方向にベクトル化
できる。アルゴリズム自体は一切変えていない。

### 忠実さのために意図的に再現した MATLAB の癖

移植の一致を取るため、以下は「直さずに」再現した。Phase 2 で触る。

- `subfunc2` の `D` は while ループを跨いで**持ち越される**。各反復で
  書き換わるのは線分の到達範囲内の行だけで、範囲外の行は前回の値が
  残る。
- `max(U,[],'all')` は**列優先の最初の最大値**を返す。
- `subfunc1` の復活ループは `L` を**ループ内で書き換えながら**進むので
  `rmedges` の順序（＝切断順）に依存する。よって切断は MATLAB と同じ
  「1パスにつき各成分1本」の順序で行う必要があり、成分ごとの再帰分割
  に置き換えると復活の結果が変わりうる。
- `conncomp` のラベルは**ノード初出順**。直後の安定ソートに効くため
  一致させた。

### 移植して分かったこと

- **`distfun.m`（隣接スライスを距離0扱いにする非線形版）は死にコード**。
  `pixellist2poly` の flag=2/3 からしか呼ばれず、本番経路
  （`resamplingpoly` → `pixellist2poly(x1,1,Dim,TH)`）は flag=1。
- **`z_scale = 3/0.29` は 3µm スライス前提**。E07 は 1.5µm/スライスなので
  **z 方向の距離を実際の2倍に見積もっている**。深さ方向に飛跡が千切れ
  やすくなるため、実データの tangle の一因の可能性がある。
  `geom.MATLAB_Z_SCALE` として括り出し、引数で差し替え可能にした。
- 定数はすべて**シミュレーション（159本/view、飛跡に沿った点間隔 約3px）**
  で決まっている。実データのエクスポートは `_GRID_CELL_PX = 30` なので
  **点間隔が10倍粗い**。`subfunc2` の `dl = 20`（伸長窓）や `detectbunki`
  の `err<1.5` は、この粗さでは意味をなさない。移植で detectlseg が
  桁で速くなったので、**`_GRID_CELL_PX` を下げて MATLAB 側の閾値が
  効く density に戻す**選択肢が現実的になった。

### 成果物

- `module/graphdet/geom.py` — `norma`/`isaline5`/`isaline6`/
  `mindistance_tolineseg`/`L2lseg`/`lseg2L` の移植
- `module/graphdet/detectlseg.py` — `subfunc1`/`subfunc2` と領域走査
- `scripts/check_lseg_reference.py` — 参照照合（幾何でマッチング。
  SVD 軸の符号は LAPACK と MATLAB で一致する保証がないので、端点の
  入れ替わりは同一とみなす）
- `tests/test_graphdet.py` — 高速7件（うち参照の先頭16領域）+ slow 1件
  （全256領域）。既存スイートに影響なし（59 passed）

**次**: Phase 1-2 = `integrate_smallregions` + `pixellist2poly` の移植、
`work1.mat` の `polylines`（149本）と照合。


## 2026-07-29 12:40 JST — 不一致優先レビュー `/label_disagree` を追加。ついでにラベル破壊バグを発見・修正

前回の結論「足りないのは junk と track を分ける情報」を受けて、人手
レビューを**最も情報量の高い順**に出す仕組みをブラウザ UI に足した。

### 不一致は実際に豊富だった（作る前に測定）

本番パラメータ（thr=35/ml=30/mg=40）で 4 タイル計 **47,498 候補**に対し、
古典分類器と CNN の確率を突き合わせた:

| | 件数 | 割合 |
|---|---|---|
| 判定が**真逆**（0.5 閾値） | 14,341 | 30.2% |
| \|確率差\| > 0.3 | 15,511 | 32.7% |
| \|確率差\| > 0.5 | 4,398 | 9.3% |
| \|確率差\| > 0.7 | 396 | 0.8% |

十分な量がある。最上位は「古典 0.074 / CNN 0.934」のように真っ向から
対立するもので、ここを 1 クリック解決する価値は、両者が一致している
候補を 1 クリックするより明確に高い。

### 実装

- `/label_disagree`（＋ `/label_disagree_segments`）を追加。既存の
  `/label_uncertain` と**同じテンプレートを共用**し、endpoint と
  ラベルだけテンプレート変数で差し替えた。操作感は従来どおり
  「切り出し画像を見て true/false を押すと自動で次へ」。
- CNN 確率は**ファイル経由**で渡す。torch と OpenCV を同一プロセスに
  載せると backward がデッドロックするため（2026-07-28 の知見）、
  Flask 側に torch を持ち込まない。生成は ML リポジトリ側の
  `dump_segments.py --all-candidates --hough 35,30,40` →
  `score_candidates.py --checkpoint <pt>`。

### 応答が 84 秒かかっていたのを 0.12 秒に

初回リクエストが 84 秒。原因は 2 つとも「ラベルに依存しない計算の
再実行」だった:

1. キュー側で毎回 4 タイル分の Hough ＋ 12,000 セグメントの特徴量抽出
2. `track_classifier.build_training_set()` が**クリックのたびに**全
   ラベルファイルのタイルで Hough を再実行

両方をメモ化（`labeling._cached_features`、
`track_classifier.features_for_record`）。初回のみ 84 秒（キャッシュ
構築、ローディング表示あり）、以降 **0.12 秒**。`/label_uncertain` も
同じ恩恵を受け 0.067 秒になった。

### 【重要】ラベル破壊バグを発見・修正

`label_decide` は保存時にレコードの Hough パラメータを**現在のサーバ
既定値で上書き**する。ところがレビュー既定値は 2026-07-23 に
8/10/20 → 35/30/40 へ変更されており、既存 512 判定はすべて 8/10/20 で
記録されている（最終更新 2026-07-18）。

決定は候補リストへの**添字**なので、パラメータが変われば指す先が変わる。
つまり**新規に 1 クリック保存した瞬間、既存 512 判定が別の候補
（24,038 本 vs 12,003 本、順序も独立）を指すよう静かに書き換わる**。
7/23 以降クリックが無かったため実害は出ていなかったが、今回 UI を
使い始めれば即座に踏むところだった。

対策: **パラメータ別にラベルファイルを分ける**。既存ファイルと
パラメータが一致すればそのまま追記、違えば
`..._z29__t35l30g40.json` のように別ファイルへ書く。
`real_label_dataset.py` は各ファイルのパラメータを読んで Hough を
再実行する設計なので、両方を正しく扱える。
実機で検証: 新規判定は別ファイルに入り、既存ファイルの md5 は不変、
512 判定は無傷。

### 使い方

```
python -m module.server.app --port 8123
# ブラウザで http://127.0.0.1:8123/label_disagree
```

初回だけ 80 秒ほど「loading...」が出る。以降は即応。判定は
`results/manual_labels/..._t35l30g40.json` に貯まる。

### 注意点

- CNN スコアは特定チェックポイント（現在は
  `results/local_nosp/orient_V00001012_s1.pt`）に紐づく。モデルを
  更新したら `score_candidates.py` を回し直す。キュー応答の
  `checkpoint` フィールドでどれを使ったか分かる。
- 新規ラベルは 35/30/40 の候補集合上に貯まるので、既存 512
  （8/10/20）とは**別の母集団**。学習時は両方使えるが、混ぜて
  「合計 N 件」と数えるときは母集団が違うことを意識すること。

## 2026-07-28 19:50 JST — specials をラベルに足しても AUC は動かなかった（0.592 → 0.594）

E07 specials の反応点から放射する飛跡を正例として学習に加え、加えない
場合と**同条件で対照実験**した。結果は**帰無**——差は無かった。

### 結果（4 fold × seed 1、20 epoch、n=512、基準率 43.6%）

| | precision | recall | 予測陽性率 | **AUC** |
|---|---|---|---|---|
| specials なし | 48.0% [42.7, 53.4] | 70.4% | 63.9% | **0.592** |
| specials あり | 50.0% [43.7, 56.3] | 54.3% | 47.3% | **0.594** |

層別 AUC: 上位100位帯 0.555 → 0.560、裾帯 0.538 → 0.543。

precision/recall は動いているが（specials 側は保守的で recall 70→54%、
precision 48→50%）、これは動作点が移っただけで、閾値非依存の AUC は
**0.002 しか変わらない**。

**ただし検出力に注意**: n=512（track 223 / junk 289）での AUC の
標準誤差は Hanley-McNeil で 0.025、差の検出限界は **0.070 AUC**。
つまりこの実験が言えるのは「**0.07 を超える大きな改善は無い**」で
あって、「効果がゼロ」ではない。

なお specials なしの本実験（20 epoch・1 seed、AUC 0.592）は、朝の
50 epoch・3 seed の結果（0.597）を再現しており、短縮版の設定でも
ベースラインは安定している。

### なぜ効かなかったか（解釈）

最も素直な説明は、**specials が正例しか供給しない**こと。判別すべきは
「線状の junk」と「線状の本物」の区別だが、specials が教えるのは
「これは本物だ」だけで、junk の例を一つも足していない。しかもモデルは
既に track を過剰予測する傾向がある（背景修正後でも予測陽性率 64%）。
つまり足りていなかったのは正例ではなく、**junk と track を分ける情報**
だった可能性が高い。

タイル多様性（3→9）が効かなかったことも、この解釈と整合する。多様性が
足りないのではなく、判別に必要な種類の教師が足りない。

### 方針への影響

「幾何からラベルを安く大量に作る」路線では、少なくとも正例側は頭打ち。
残る選択肢は:

1. **junk 側の教師を増やす**——結局は人手レビュー。ただし古典と ML が
   食い違う箇所を優先すれば労力あたりの情報量は最大化できる。
2. **別種の信号を入れる**——スライス間の 3D 整合性、grain density など。
   1 スライスの見た目だけでは junk と track が原理的に分離しにくい
   可能性がある（実際 AUC 0.59 はその示唆かもしれない）。
3. 純度の閾値を上げる（現在 contrast≥3）、`perp_max` を絞るなど
   specials 側の質を上げる余地はあるが、上記のとおり効果は限定的と予想。

### 補足: 実行環境の制約

queue s は CPU 上限 150 分（9 タイルでは 12 epoch 相当）で 20 epoch が
入らない。さらに本ユーザーの fairshare 優先度が全 queue で 0.000 に
落ちており（queue l に 400 件超を投入した影響）、s/l/h いずれも
数時間 pending のままだった。そのためワークサーバの 4 コア枠で
逐次実行した（8 ラン、約 4.5 時間）。LSF 側の 3 seed 版は投入したまま
残してあるので、順番が回れば統計が増える。

## 2026-07-28 07:30 JST — 背景負例を入れた LOTO 再実行: AUC 0.54→0.59。効いたが足りない

05:00 に特定した原因（背景を負例として一度も見せていない）への対策を実装し、
同条件で LOTO を回し直した（LSF 配列 55017237、queue h、24 run）。

### 対策

`real_label_dataset.py` に `_sample_background()` を追加。**全**Hough 候補
（レビュー済みに限らない 24,038 本）を 12px 幅で描き、その外側の画素を
junk としてサンプリングする（明示ラベル 1 画素あたり背景 2 画素、seed 固定）。
1 タイルのラベル被覆率は 0.95% → 2.9% になり、pos_weight は 1.00 → 4.16。

サンプリングして全部使わないのは、背景が候補外領域の大半を占めるため、
そのまま入れると人手ラベル約 4 万画素が埋没するから。

### 結果（4 fold × 3 seed 統合、n=1536、基準率 43.6%）

| arm | precision | recall | 予測陽性率 | AUC | 修正前 AUC |
|---|---|---|---|---|---|
| binary のみ | 49.5% [46.0, 53.1] | 55.8% | 49.0% | **0.582** | 0.549 |
| binary+方向 | 48.6% [45.3, 52.0] | 61.0% | 54.6% | **0.597** | 0.539 |

**改善した点**:
- **precision が基準率を有意に上回った。** 43.6% に対し 49.5% / 48.6% で、
  95% CI 下限（46.0 / 45.3）も基準率より上。修正前は precision = 基準率
  ちょうどで、判別力が完全にゼロだった。
- 予測陽性率が 97.7% → 49.0% に。「全部 track と答える」状態を脱した。
- スライス上で飛跡と判定する面積が 78.6% → 14.3%、確率中央値 0.563 →
  0.072、最小値 0.340 → 0.000。背景を自信を持って棄却するようになった。
- AUC が 0.54 → 0.59。

**足りない点**: AUC 0.59 は偶然（0.5）よりましというだけで、実用には遠い。
層別で見ると上位100位帯の precision 73.9% は基準率 70.4% とほぼ同じ
（CI 下限 69.5 が基準率を下回る）。裾帯も 17.4% vs 14.6% で有意でない。
**全体の AUC が上がったのは主に、層の混合比を利用して「上位帯を陽性、
裾帯を陰性」と当てているだけ**の可能性が高く、層内での判別力はまだ弱い。

### 方向ヘッドについて

修正前は binary と完全に同一だったが、修正後は **binary+方向のほうが
わずかに良い**（AUC 0.597 vs 0.582、上位100位帯では 0.593 vs 0.550）。
差は CI 内なので断定はできないが、少なくとも害はなく、上位帯で 4pt 差が
出たのは示唆的。

### 線分抽出への影響

`extract_segments.py` の出力も改善:

| | 線分数 | 中央長 | 最大長 | 交差/塊フラグ |
|---|---|---|---|---|
| 修正前 | 144 | 15.8 px | 2,890 px | 37 |
| 修正後 | 680 | 27.9 px | 1,246 px | 355 |

マスクが画像全体を覆わなくなったので連結成分の巨大融合が減り、最大長が
2,890 → 1,246 px に。ただし 680 本中 355 本がまだ交差/塊としてフラグされ、
連結成分ベースの分割は依然弱い。参考までに同タイルの Hough 候補は
ラベル用設定で 24,038 本、本番設定で 12,003 本。

### 結論と次の一手

背景を教えるのは**必要だったが十分ではなかった**。残る壁はデータ量と
みるのが自然で、学習に使えるのが 3 タイルしかない以上、過学習も
（train_loss 0.026 vs val_loss 0.98）解消していない。次は:

1. **層内での判別力を測る。** 全体 AUC は層の混合比に引きずられるので、
   上位100位帯だけ・裾帯だけでの AUC を主指標にする（すでに出力済み）。
2. **ラベルを増やす。** E07 specials の反応点から放射する飛跡は Hough の
   提案に依存しない正例源。GT 未クリックの 4 事象もある。
3. **連結成分に頼らない線分分割。** 方向場が使えるので、成分内で方向が
   不連続になる場所で切る、などが自然な次段。

## 2026-07-28 04:45 JST — LOTO 結果: CNN はセグメント単位では判別力ゼロ（AUC 0.55）。原因は「背景を教えていない」こと

LOTO 24 run（4 検証タイル × 3 seed × 2 arm、LSF queue h、job 54866034）が
完了。`eval_segments.py` によるセグメント単位・leave-one-tile-out の結果は
**過去の報告を覆すもの**だった。

### 結果（全 fold・全 seed 統合、n=1536 セグメント）

| arm | precision | recall | 予測陽性率 | **AUC** |
|---|---|---|---|---|
| binary のみ | 43.9% [41.4, 46.4] | 98.5% | 97.7% | **0.549** |
| binary+方向 | 43.9% [41.4, 46.4] | 99.0% | 98.2% | **0.539** |

基準率（track の割合）は 43.6%。**precision が基準率と一致し、recall が
99%、予測陽性率が 98%** ——つまりモデルはほぼ全部を track と答えている。

層別でも同じ:

| 層 | 基準率 | precision | AUC |
|---|---|---|---|
| rank ≤ 100 | 70.4% | 70.3〜70.5% | 0.555 / 0.561 |
| rank > 1000 | 14.6% | 14.6〜15.4% | 0.519 / 0.430 |

**どの層でも precision = 基準率**。これは「閾値がずれている」のではなく
**スコア自体に情報が無い**ことを意味する。念のため AUC（閾値非依存の
順位付け能力）を測ったが 0.43〜0.56 で、偶然（0.5）と区別できない。

方向ヘッドの有無で差は無い（precision は完全一致、recall は 0.5pt 差で
CI 内）。**方向ヘッドは binary 側を悪化させないことは確認できた**が、
そもそも binary 側に判別力が無いので恩恵も測れない。

### 原因: 背景を一度も教えていない

学習済みモデルに実タイルを通すと、**スライスの 78.6%（binary arm では
97.5%）を「飛跡」と判定**する。確率の中央値は 0.563 / 0.757、最小値でも
0.34 / 0.18 で、画像のどこに対しても「飛跡ではない」と自信を持って
言わない。

理由は損失の設計にある。マスクは `-1`（未レビュー＝損失から除外）が
**画素の 99%** を占め、`0`/`1` が付くのは人間がクリックした Hough
セグメントの上だけ（1 タイルあたり 40,016 画素 = 0.95%）。つまり:

- モデルが見る負例は **「junk と判定された Hough セグメント」だけ**で、
  これは薄い線状の物体である
- **何もない乳剤（背景）が負例として一度も提示されていない**
- 結果、背景を棄却する能力が育たず、推論時に画像の大半を飛跡と答える

「未レビューを 0 にすると偽陰性を注入する」という当初の判断
（`real_label_dataset.py` の docstring、2026-07-18）は Hough **候補**に
ついては正しいが、**どの Hough 候補からも遠い、粒子すら無い画素**まで
一律に `-1` にしてしまったのが行き過ぎだった。

### 次にやるべきこと（優先順）

1. **背景を負例に加える。** どの Hough 線分からも一定距離以上離れた
   画素は安全に `0` としてよい。これは 1 行程度の変更で、いまの
   「全部 track」状態を直す最有力手段。
2. その上で LOTO を再実行し、AUC が 0.5 から動くかを見る。動かなければ
   データ量（実ラベル 4 タイル、学習に使えるのは 3 タイル）が本質的な
   壁ということになる。
3. 過学習も深刻: train_loss は 50 epoch で 0.024 まで落ちるのに
   val_loss は 1.79 まで悪化し、最良チェックポイントは epoch 6〜9。
   3 タイルでは記憶しかできていない。

### 付随して確認できたこと

- `extract_segments.py`（CNN 出力 → 線分）は動作する。学習済みモデルで
  方向一致度の中央値 0.950（未学習 0.837）。ただし上記のとおり
  マスクが画像の大半を覆うため、連結成分が巨大に融合し 2,890 px の
  線分が出るなど、**現時点の出力は物理的に無意味**。背景を教えて
  マスクがまともになって初めて意味を持つ。
- **過去の「precision 78〜84%」は画素単位・固定検証タイル 1 枚の値**で、
  判別力を測っていなかった。モデルが候補画素をほぼ全部 track と塗る
  ため、画素 precision は画素レベルの基準率をなぞるだけになる。

## 2026-07-28 02:15 JST — 重要: ワークサーバは1ユーザ4コアに制限されている

Method C の LOTO 学習が異常に遅い件を追い込んだ結果、**kekcc のワーク
サーバは CPU を食う対話プロセスを cgroup `/user.slice/restricted_user`
に移し、`cpu.max = 400000 100000` ＝ 合計 4 コアに制限する**ことが判明。
128 コアのノードでも、そのユーザが走らせる全プロセス合計で 4 コア。
`nr_throttled` は 265 万回、累積 throttle 時間は約 1.7e13 μs。

**症状が紛らわしい**: 学習は最初の 2〜3 エポックだけ正常速度（22 秒）で、
その後 370 秒前後に落ちる。落ちている間もマシン全体は 96% アイドル、
I/O 待ちゼロ、スワップ活動ゼロ、メモリ 540 GB 空き。並列数を 24 → 12 → 6
と下げても同じところで落ちるので、当初は自分同士の競合と誤診した。
`ps` の `%CPU` はプロセス生涯の平均なので、これも判断を誤らせた
（瞬時値は `top` で見ること）。

### 影響（Method C 以外）

本日 20:xx に記録した「**Method A の全域再解析はワークサーバ 16 worker で
約 6.5 時間**」は throttle が効く前の測定で、**4 倍ほど楽観的**。
4 コア上限下では 16 時間規模になる。ワークサーバは編集と短時間テスト用で
あって本番計算用ではない、と考えを改める必要がある。

### 結論と対応

- **重い計算は LSF に投げる。** バッチジョブは独自の割り当てを受けるので
  この cap の対象外。
- Method C の LOTO 24 run は queue `h` の配列ジョブとして投入し直した
  （job 54866034、`-n 4 -M 4000 -W 04:00`）。学習のピークメモリは
  約 1.3 GB で 4 GB 制限に十分収まる。queue は `h` が最も空いていた
  （pending 78 / running 522。`l` は 113k、`s` は 32k pending）。
- 逆に **Method A は LSF に投げられない**（1 worker 6.75 GB > 4 GB）。
  全域再解析をやるなら、view ごとに逐次書き出してメモリを 4 GB 以下に
  下げる改修が先。ワークサーバで押し切る案は 4 コア上限で現実的でない。

計測の教訓として、**cw07 で測った性能値は throttle 前後で意味が変わる**
ので、今後ベンチマークを取るときは `cat /proc/self/cgroup` で自分が
`restricted_user` に入っていないか確認すること。

## 2026-07-28 01:00 JST — Method C: 方向ヘッド追加、セグメント単位評価、そして cv2 の未使用 import が学習を約100倍遅くしていた件

ユーザー方針「反応点の前に、飛跡抽出（セグメンテーション）を古典と ML の
2手法で確立する」に沿って Method C を進めた。

### 1. Hough は binary だけでなく「線分」を教師にできる

議論の出発点は、CNN の教師が古典 Hough 由来であることの是非。結論として
**これは純粋な循環ではない**。ラベルは (a) Hough が候補線分を提案し、
(b) 人間が各候補を track/junk と判定する、の2成分からなり、(b) は Hough が
表現できない情報なので、生徒が教師を超えうる。循環が効くのは訓練ではなく
**評価**の側だけ——同じ生成器由来のラベルで測ると「教師との一致率」しか
分からない。

そのうえで、Hough は角度・長さ・端点という幾何情報も無料でくれる。現状の
CNN は binary しか学ばないので出力が画素マスクで終わり、飛跡情報に戻せず
`find_vertices` に届かない（実際、CNN 出力を線分に変換するコードは両
リポジトリのどこにも存在しなかった）。そこで**方向ヘッドを追加**した。

### 2. 実装

- `real_label_dataset.py`: `theta` プレーンを追加出力。線分の端点から
  `atan2` で角度を求め、`mask==1` の画素にのみ書く。交差部は長い方が勝つ
  （`tracks` は長さ降順なので、セグメント ID 昇順で走査し上書きしない）。
- `real_dataset.py`: 角度は**倍角 (cos 2θ, sin 2θ) で符号化**。線分に
  頭尾の区別が無く θ と θ+π が同一だからで、θ を直接回帰すると折り返しに
  偽の不連続が入る。損失は L2 正規化後のコサイン距離
  (`masked_orientation_loss`)、track 画素のみで平均。
- `train_real.py`: 出力 3 チャンネル（ch0 = 従来どおりの binary ロジット、
  ch1-2 = 倍角の cos/sin）。**ch0 の意味は方向ヘッドの有無で変わらない**
  ので、binary だけ使う利用者は残りを無視できる。`--orient-weight 0` で
  1 チャンネルの binary 専用モデルにもなる。

### 3. cv2 の未使用 import が backward をデッドロックさせていた（重大）

学習が異常に遅く、1 エポックが 22 分経っても終わらなかった。切り分けた
結果、**計算でもデータ供給でもなく `backward` が停止**していた:

| 条件 | forward | backward |
|---|---|---|
| cv2 非ロード、torch 16 スレッド | 0.12 s | 0.19 s |
| cv2 ロード、torch 16 スレッド | 0.23 s | **返らない** |
| cv2 ロード、torch 1 スレッド | 1.17 s | 1.51 s |

OpenCV と torch がそれぞれ OpenMP ランタイムを持ち込み、1 プロセスに 2 つ
載ると torch の並列 backward が固まる典型例。`cv2.setNumThreads(0)` では
解消しない。流入経路は
`train_real.py` → `train.py` → `dataset.py` の `import cv2` で、
`real_dataset.py` にも**一度も使われていない `import cv2`** があった。

共有定数を cv2 非依存の `model_defaults.py` に切り出して連鎖を断ったところ、
**1 エポック 22 分超 → 13.6 秒（約100倍）**。これまでの「CPU 学習は遅いから
GPU が要る」という前提自体が誤りだった可能性が高い。macOS/MPS で 1 回
約 2 時間という記録も、同じデッドロックを踏んでいた疑いがある。

### 4. 評価をセグメント単位に（`eval_segments.py` 新規）

従来の val_precision/val_recall は**画素単位**。しかし人間が判定したのは
Hough セグメント単位で、1 セグメントの全画素は 1 つの判断を共有する。
検証タイル V00000011 は 40,016 画素あるが、独立な単位は **63 セグメント**
しかない。信頼区間を画素で計算すると ±0.4pt、セグメントで計算すると
**±9.1pt**——**約25倍の過小評価**。過去に報告された seed 間 6.6pt の
ばらつきは、この ±9.1pt の内側に完全に収まる。モデルが不安定だったのでは
なく、誤差棒を間違った分母で計算していた。

さらに、ラベルの母集団が代表的でないことも実測した。ラベル作成時の Hough
パラメータ (thr=8/ml=10/mg=20) では 1 タイルあたり **24,038 本**の候補が
出るが、人間が見たのは **63 本 = 0.26%**。しかも分布が二峰性で、
上位100位以内が 38 本（track 率 71%）、1000位より下が 24 本（track 率 4%）。
つまり報告される precision はレビューの抽出比に依存しており、24,038 本
全体に適用したときの値ではない。`eval_segments.py` は Wilson 信頼区間と
**層別（上位100位 / 1000位超）**の両方を出す。

なお本番 Hough (thr=35/ml=30/mg=40) は同じタイルでスライスの **52.5%** を
「飛跡上」と塗る（ラベル用は 29.9%）。抽出手法の確立という観点でも
`hough_mg=40` は要再検討。

### 5. LOTO を実行中

`--val-tile` を追加し、4 タイル × 3 seed × 2 arm（binary / binary+方向）
= 24 run を cw07 で並列実行中（`scripts/run_loto.sh`、各 50 epoch）。
4 fold を統合すると 512 セグメントになり、95% CI は約 ±3〜4pt。これが
現在のラベル量で到達できる分解能の上限で、過去に主張された 3.6pt /
2.4pt の差はこの床と同程度以下——おそらく「差は無い」が結論になる。
結果は次のエントリに記録する。

## 2026-07-27 21:00 JST — 訂正: specials_x20 の 11/13 は E07 乾板。実 E07 の GT が使える

ユーザーより重要な訂正。`specials_x20/` の 13 ディレクトリのうち
**11 は E07 乾板**で、実 E07 の本物の反応点データとして使える:
D005, D013, IBUKI, IRRAWADY, MINO, T004, T004_3body, T004_center,
T011, T011_100, T011_200。**E373 は KISO と NAGARA の 2 つだけ**で、
これらは当面優先度を下げる。

これにより STATUS.md に書いていた制約 2 件が**撤回**になる:

- 「`specials_x20` は E373 乾板であり E07 ではない。パラメータ最適化の
  対象にしてはいけない」→ 誤り。11/13 は E07。
- 「実 E07 には確認済みの反応点が 1 件も無い。原理的に埋まらない」
  → 誤り。`tests/specials_gt.json` の実測 9 事象のうち **7 事象が
  E07**（D005, D013, IBUKI, IRRAWADY, MINO, T004, T011）。

前景密度が約 1.8〜2 倍違うという 2026-07-15 の実測自体は有効だが、
あれは **KISO(E373) と E07 全面探査データの比較**であって、
specials 全体の性質ではなかった。STATUS.md 新設時にこの限定条件が
落ちて一般化されたのが誤りの経路と思われる。

**効いてくるところ**: 実 E07 の GT 付きデータでパラメータ最適化と
評価ができる。Method C（CNN）の評価不能の根本原因は検証セットの
小ささ（実ラベル 4 タイル中 1 枚＝55 クロップ）だったので、ここに
直接効く。E07 の T004_3body / T004_center / T011_100 / T011_200 は
データはあるが GT 未クリックなので、クリックすれば GT を増やせる。

ユーザーの方針: 次は**飛跡の binary segmentation（Method C）を進める**。

## 2026-07-27 20:10 JST — kekcc 引き継ぎメモの実機検証と環境整備

macOS セッションが残した `HANDOFF_kekcc.md` を kekcc 実機（cw07）で
照合した。**ノートの最大のブロッカーは誤りで、逆に GPU 前提の計画が
成立しないことが判明**した。以下は実測に基づく訂正。

### バッチシステムは使える（ノートの記載は誤り）

ノートは「`bsub`/`sbatch`/`qsub` が PATH に無い、これが分からないと
計画自体が成立しない」としていたが、cw07 では
`/opt/lsf/10.1/linux2.6-glibc2.3-x86_64/bin/bsub` が存在し、
LSF 10.1（cluster `centralcluster`）が動いている。ノートの調査は
cw02 で行われたので、login node 差か PATH 設定の問題と思われる。
`python -m module.pipeline.cli_submit_kekcc --dry-run` も正常完走し、
queue `s` / 135 ジョブ（`e07full[1-135]`、約15 view/ジョブ）/ 2 コア /
4000 MB / walltime 1h の bsub コマンドを生成する。
**Method A の全域投入はすぐ実行できる状態。**

### GPU は使えない（三重にダメ）

`bqueues -l g`（"for using gpu server jobs"）を確認した結果:

1. `USERS: shogo kmura ce_ibm/` — **hayashu にアクセス権が無い**。
   `bqueues -u hayashu` が返すのは `s l h p a` のみで `g` は含まれない。
   （ce_ibm グループに `a-hayash` がいるが別人）
2. STATUS が `Closed:Inact_A`（閉鎖・非アクティブ）
3. 唯一の GPU ホスト `ccg01` が `unavail`（ダウン）

→ **CNN（Method C）の LOTO 評価を kekcc に移す動機は大きく下がった。**
GPU 短縮は望めず CPU 実行になる。kekcc の価値は候補2（Method A の実
fullscan 全域投入）の側にある、と優先順位を修正すべき。

### 投入可能な queue の制限（全て MEMLIMIT 4 GB）

| queue | RUNLIMIT | TASKLIMIT | 備考 |
|---|---|---|---|
| `s` | 300 分 (5h) | 12 | 現行の投入先。混雑（pending 44,315 / run 3,199、上限 3,200・ユーザ 800）|
| `l` | 2400 分 (40h) | 12 | |
| `h` | **11520 分 (8日)** | 12 | 長時間 CPU 学習向き |
| `p` | 2400 分 | 2/4/64 | 並列 |
| `a` | 2400 分 | 1 | GRA、JL/U 4 |

CNN を CPU で回すなら `h`（8日）が現実解。ただし **MEMLIMIT 4 GB /
TASKLIMIT 12** は効くので、`torch.set_num_threads()` を 12 以下に
合わせること（既定では 64 スレッドを認識してしまう）。

### リポジトリは古くない（ノートの記載は誤り）

ノートは「kekcc は `baa81a2` で大幅に古い、`git pull` 必要」としていたが、
実際は HEAD `f2b77d4`、origin/main と同期、working tree クリーン。
`~/work/e07/fullscan` は gpfs 実体へのシンボリックリンクで別クローン
ではない。`e07-ml-binary-segmentation` が未クローンなのは記載どおり。

### 環境整備（ユーザー承認のうえ実施）

ノートは「torch / sklearn / cv2 いずれも無し」としていたが、これは
system python3.9 と `/opt/anaconda3` しか見ておらず、conda `myenv` を
見落としていた（cv2 4.12.0 は元からあった）。`myenv` に不足分を追加:

- `scikit-learn 1.6.1`、`scikit-image 0.24.0`
- `torch 2.8.0+cpu`、`torchvision 0.23.0+cpu`（GPU 無しのため CPU ビルド）
- **numpy は 1.26.4 に固定して導入**し、conda 版 opencv 4.12.0 を壊さない
  ようにした（`myenv` は conda と pip の混成環境なので numpy を動かすと
  cv2 が壊れるリスクがある）

検証: 上記8パッケージすべてインポート成功、`matlab_export`（skimage
依存）を含むプロジェクト全インポート OK、`pytest -m "not slow"` が
**52 passed / 35 deselected** で従来のベースラインと完全一致。

→ これで Method A（cv2 + sklearn）も Method C（torch）も kekcc の
`myenv` で動く土台ができた。

## 2026-07-27 — ドキュメント体制の見直し: `STATUS.md`新設、
本番パラメータをyaml一元化（コードで構造的に解決）、
調整ログのアーカイブ分離

kekccセッションとmacOSセッションを行き来する運用で「引き継ぎが
毎回面倒。analysis-note.mdさえ書いていれば足りるか？」という
ユーザーの問いから。結論は**足りない**——analysis-note.mdは
「履歴」であって「現在の状態」ではなく、3,785行に達していて、
新セッションは上から数エントリ読んで作業を始めるため、
下に埋もれた有効な制約を見落とすか、既に無効化された結論を
拾ってしまう。実際このセッションだけで3回発生した:

1. `test_specials.py`のdocstringが「ほとんど失敗する」のまま残存
2. 本番パラメータが5箇所に分散し、どれが正か書いた場所が無かった
   （`hough_mg`と`grain_radius`で同種のバグを2回）
3. 「grain_radius修正でCNNが改善」と報告→シード検証で撤回

**対応3件**:

**(1) `STATUS.md`を両リポジトリに新設**——「今どうなっているか」
だけを書き、**追記ではなく上書き**運用（変遷は`git log -p`で追える）。
手法ごとの状態、測定条件込みの現在の最良数値、本番パラメータの値と
正典の在り処、効いている制約、次の候補。analysis-note.mdは履歴として
そのまま残す（役割が違うので併存）。両CLAUDE.mdに運用ルールを明記
（セッション開始時に最初に読む／無効化する変更と同じコミットで更新／
矛盾時はanalysis-note.mdの新しい方が正）。

**(2) 本番パラメータをコードで一元化**——`module/pipeline/finder.py`
が`config/default.yaml`の`viewer`ブロックを読むよう変更
（`_load_viewer_cfg()`、yamlが無い場合は`_FALLBACK`にフォールバック）。
これまで残っていた最後の不一致（finder.py `thr=20/ml=25` vs
yaml `thr=35/ml=30`）も自動的に解消。**文書で注意喚起するのではなく、
ずれるという事象自体を構造的に消した**——同じ種類のバグを2回踏んだ
ことへの対応。高速テスト52件は変更後も通過。

なおyamlを読まない例外は意図的に残す:
`server/labeling.py`（2026-07-23に本番値へ統一済み）、
`matlab_export.py`の`_NOISE_V2_*`（Method B用、分類器の学習データと
整合させるため別値）、`diag_common.py`の`TRACK_CFG`（yamlのミラー、
手で同期——将来yaml読み込みに寄せる余地あり）。

**(3) 調整ログのアーカイブ分離**——`discussion.md`(5,851行)/
`discussion_ja.md`(5,349行)が肥大化し大半が現在価値を失っていたため、
2026-07-11より前を`discussion_archive_2026H1{,_ja}.md`へ**内容無改変で**
切り出し。現行ファイルは657行/703行に縮小。エントリ見出し数で
検証済み（155→26+129、159→26+133、欠落ゼロ）。CLAUDE.mdの
「追記専用・再編集禁止」規約に例外条項（ユーザー明示依頼時のみ、
逐語アーカイブ、内容の編集・削除は禁止）を追記。

**検証完了**: finder.pyのデフォルト変更（thr 20→35, ml 25→30）後も
`specials_x20`は**35/35通過**——既知9事象の検出に影響なし。
副次効果として実行時間が41分27秒→29分23秒（**29%短縮**）。
thrが厳しくなって候補セグメント数が減り、④の全ペア交点計算が
軽くなったためとみられる。

## 2026-07-24 (2) — grain_radius=15版分類器で疑似ラベル64タイル
全再生成、CNN再学習でval_loss過去最良を更新(precision/recallの
トレードオフは変化)

`scripts/generate_pseudo_labels.py`で64/64タイル成功（前回NFS
不安定で7/24しか処理できなかった分の完全版）。track:junk比は
550:4177(11.6%、grain_radius修正前とほぼ同じ比率)。219件が
トリアージでフラグされ人手レビュー待ち。

この疑似ラベルセット + pos_weight自動算出(1.73)でCNN再学習:

| 設定 | precision | recall | val_loss(最良) |
|---|---|---|---|
| 実ラベルのみ(4タイル) | 78.3% | 96.2% | 0.515 |
| 実+疑似64(pos_weight無し) | 81.5% | 83.8% | 0.523 |
| 実+疑似64(pos_weight=1.77、grain_radius=10版疑似ラベル) | 80.3% | 92.5% | 0.479 |
| **実+疑似64(pos_weight=1.73、grain_radius=15版疑似ラベル)** | **83.9%** | **84.9%** | **0.465(全実験中最良)** |

val_loss(モデル選択の実際の基準)は更新されたが、precision
+3.6pt/recall -7.6ptというトレードオフが発生。

**【訂正・原因の再検証】**: 当初「grain_radius修正でMethod A
分類器の判定基準が変わり、疑似ラベルの質を通じてCNNに伝播した」
と推測したが、実際に新旧疑似ラベル(grain_radius=10版 vs 15版)の
画素レベルtrack:junk比を同一7タイルで比較したところ、**ほぼ
変化していなかった**(合計pos_frac 32.9%→33.8%、タイルごとの
増減も系統的でない)。つまりクラス不均衡の変化という単純な説明
では今回のprecision/recallシフトを裏付けられない。より細かい
レベル(個々のセグメントの判定が具体的に入れ替わった)か、
あるいは**学習自体の再現性のばらつき**(`train_real.py`は検証
タイル分割こそシード固定しているが、モデル初期化・DataLoaderの
シャッフルはシード固定していない)が実際の原因である可能性が
高く、「教師の修正が生徒に伝播した」と断定するのは時期尚早。
真の効果を見るには同一データでの複数回実行によるばらつき測定が
必要——現時点では未実施。Method A分類器(同一検証タイルLOTO
84.4%/96.4%)にはまだ届かないが、precisionは84%近くまで接近した
という結果自体は事実として記録。

**インフラ注記**: セッション中盤で`~/mnt/kek_e07`のNFSマウントが
再度不安定化。ユーザーが`~/out-of-sync/e07/fullscan/`へE07データ
の直接コピーを開始（NFS経由に代わるローカルコピー、進行中）。
コピー完了後`fullscan-image/E07`のシンボリックリンク先をこちらに
切り替える予定——今回のCNN再学習自体はローカル保存済みnpzのみを
使うため無停止で完了した。

## 2026-07-24 — CNN疑似ラベル学習: トリアージ機構をスクリプト化
(`scripts/generate_pseudo_labels.py`)、pos_weightでクラス不均衡を
補正しrecall回復(83.8%→92.5%、val_loss過去最良)

**(1) トリアージ機構を再利用可能なスクリプトに整備**:
`module/track_classifier.py`に`verdict_for()`(確信度→track/junk/
None)、`flag_suspicious()`(特徴量と判定が矛盾する場合に理由文字列
を返す、2026-07-22/23のヒューリスティック——track判定でn_grains/
footprint幅が低すぎる、junk判定なのに長すぎる、の2条件)を追加。
`scripts/generate_pseudo_labels.py`（新規）はタイルリストを受け取り、
Hough検出→クラスタリング(footprint幅版)→分類→疑似ラベルnpz出力
（`e07-ml-binary-segmentation/src/real_label_dataset.py`と同じ
`-1`/`0`/`1`規約）に加え、疑わしい判定を`results/
pseudo_label_review/flagged.json`に蓄積する。2タイルでスモーク
テスト済み、既存の手動スクリプトと同じ出力を確認。

**(2) CNN側: 疑似ラベルのクラス不均衡(track 13% vs 実ラベル43%)が
recall低下(96%→89%→84%、タイル数4→10→67)の主因と判明していた
問題に対処**: `real_dataset.py`の`masked_bce_loss`に`pos_weight`
パラメータを追加(track画素の損失項を重み付け)。`train_real.py`は
学習用タイル(実ラベル訓練分+疑似ラベル全部)の画素レベルtrack:junk
比から`pos_weight`を自動算出(上限20)、検証専用の`evaluate()`は
意図的に重みなしのまま(モデル選択の一貫性のため)。

既存67タイル(実3+疑似64、前回と同一データ)で再学習:

| 設定 | precision | recall | val_loss(最良) |
|---|---|---|---|
| 実ラベルのみ(4タイル) | 78.3% | 96.2% | 0.515 |
| 実+疑似64タイル(pos_weight無し) | 81.5% | 83.8% | 0.523 |
| **実+疑似64タイル(pos_weight=1.77自動算出)** | **80.3%** | **92.5%** | **0.479(過去最良)** |

recallが83.8%→92.5%まで回復、val_lossも全実験中で過去最良。
クラス不均衡が主犯だったという診断を裏付ける結果。Method A分類器
(同一検証タイルでLOTO 84.4%/96.4%)にはまだ届いていない。

**次**: (2026-07-23の) grain_radius=15・labeling.py Houghパラメータ
整合を反映した状態で`scripts/generate_pseudo_labels.py`により
疑似ラベルを再生成し、pos_weight補正と合わせた効果を確認する。

## 2026-07-23 — `grain_radius`も本番yaml値(15)とfinder.py既定値(10)が
不一致だった(mgと同じパターン)。修正でLOTO precision +2.1pt、
recallは無変化。ラベリングツールのHoughデフォルトも本番値に統一

CNN疑似ラベルのトリアージ(#30、503pxの飛跡でn_grains=1)を調べる
過程で発見。`config/default.yaml`の`grain_radius: 15`に対し、
`finder.py`の`_GRAIN_RADIUS`既定値は10のまま——`_HOUGH_MG`と全く
同じ「yaml値とコード既定値の不一致」パターン。

512件の手動ラベルでLOTO再検証(半径10 vs 15):

| | precision(平均) | recall(平均) |
|---|---|---|
| radius=10(旧既定値) | 83.4% | 83.1% |
| **radius=15(yaml本番値)** | **85.5%** | **83.1%(不変)** |

全4タイルでrecallは完全に不変、precisionだけ改善(1タイルは
+4.6pt)——mgのときと同様、ほぼタダの改善。n_grains平均も
track 4.37→5.50、junk 3.45→4.26と増加し分離が向上。
`finder.py`の`_GRAIN_RADIUS`を10→15に変更。高速テスト52件は
変更後も通過。

**ラベリングツール(`module/server/labeling.py`)のHoughデフォルトも
本番値に統一**: `thr=8/ml=10/mg=20`(2026-07-15にレビューUIの
使い勝手のために独自チューニングした値)から`thr=35/ml=30/mg=40`
(本番値)に変更。理由: 分類器の学習データ(旧ラベリングツール値で
検出したセグメント)と適用対象(本番パラメータで検出したセグメント)
の母集団が違う、という2026-07-19から未解決だった問題への対応。
元々のチューニング意図(「候補を絞ってレビューしやすく」)は
本番の厳しめのthr/mlでも同様に満たされるはずなので、レビュー
体験の悪化は想定していない。

**次にやること(ユーザー要望: 並行してCNN側のフィードバックも
随時確認)**: この2つの修正(grain_radius、labeling.py整合)を
踏まえて疑似ラベルを再生成し、CNNを再学習して効果を確認する
予定。ただしNFSマウントの不安定さがあるため、都度の再生成は
コストが高く、まとめて確認するタイミングを検討中。

## 2026-07-22 (3) — `hough_thr`/`hough_min_line`スイープ: どちらも
recallへの影響は小さく、現行値(thr=35/ml=30)で妥当。③のHough
パラメータ最適化はここで一区切り

(2)で`max_gap`だけ検証し、`thr`/`min_line`自体は未検証と記録して
いたのを解消。同じピクセルrecall手法(mg=40固定)で両方をスイープ。

**thrスイープ**(ml=30, mg=40固定、8〜55、3タイル平均——
V00001339がマウント不安定でスキップ):recall 90.4%(thr=8)〜
92.1%(thr=35)で**わずか1.7ポイントの差**。raw候補数はthrを
上げるほど単調減少(14,041→8,548)。**現行thr=35はほぼピークで妥当**。

**min_lineスイープ**(thr=35, mg=40固定、10〜50、3タイル平均——
V00001012がマウント不安定でスキップ):recall 93.2%(ml=40)〜
94.2%(ml=10)で**1ポイントの差**。raw候補数はmlを上げるほど単調
減少(11,422→7,415)。**現行ml=30(recall 93.9%)は妥当**。

**結論**: `max_gap`が唯一recallを支配するパラメータだった
(44%→92%の変動幅)。thr/min_lineはどちらもrecallにほぼ影響せず、
候補数を減らす「ほぼタダの」調整弁——現行値(thr=35, ml=30)を
変更する理由はない。③(Hough検出パラメータ)の最適化はここで
一区切りとし、次段階(Method Aの分類器をCNN(`e07-ml-binary-
segmentation`)の疑似教師として使う実験)に進む。

**インフラ注記**: 今回のスイープ中、`~/mnt/kek_e07`(fuse-t NFS)が
不安定で断続的にslice読み込みが失敗した(3重マウントの発見・整理を
含む)。4タイル中1タイルが毎回どちらかのスイープでスキップされて
おり、上記の平均値はいずれも3/4タイルに基づく。

## 2026-07-22 (2) — 【訂正】本番Houghパラメータの誤認: 実際は
`config/default.yaml`(thr=35/ml=30/mg=5)が真の本番設定だった。
正しいパラメータで再スイープし、`mg=40`の結論を再確認

ユーザー指摘「重要な解析パラメータはyamlにしてなかった？」を受けて
判明: 直前のエントリ((1))で「本番」として使っていた
`thr=20/ml=25`は`module/pipeline/finder.py`のPython関数デフォルト
に過ぎず、**実際のバッチ解析(`analyze_cli.py`、KEKCC v6パイプライン)
は`config/default.yaml`の`viewer`ブロック(`hough_thr=35,
hough_ml=30, hough_mg=5`)を読んでいた**。さらに調査すると
Houghパラメータの定義箇所は実質5箇所あり、値もバラバラ:

| 箇所 | thr | ml | mg | 用途 |
|---|---|---|---|---|
| `config/default.yaml`(→`analyze_cli.py`, `app.py`ビューワ既定値) | 35 | 30 | 5→**40** | **真の本番** |
| `diag_common.py`(`TRACK_CFG`、上記のミラー) | 35 | 30 | 5→**40** | 診断スクリプト共通設定 |
| `finder.py`(`find_tracks`のPython既定値) | 20 | 25 | 4→40(先に変更済み) | 明示指定しない呼び出し全般(`test_specials.py`等) |
| `labeling.py`(手動レビューUI) | 8 | 10 | 20 | 人間が見やすいよう緩めた値、意図的に別物 |
| `matlab_export.py`(`_NOISE_V2_*`) | 8 | 10 | 20 | labeling.pyと合わせた値(分類器の学習データと整合) |

**正しい本番パラメータ(thr=35/ml=30)でmax_gapスイープをやり直した**
結果(4タイル平均、ground truthは(1)と同じ512件のtrueセグメント
ラスタライズ):

| max_gap | recall | raw候補数 |
|---|---|---|
| 5(旧本番) | 45.8% | 559 |
| 20 | 81.0% | 4,983 |
| 30 | 89.3% | 9,880 |
| **40** | **92.7%** | 10,173 |
| 60 | 95.4% | 7,923 |
| 80 | 97.2% | 6,008 |

偽ブリッジ診断(長さ>300px かつ 粒密度<0.02、V991タイル)も
再確認: mg=40では4本(疑い1本)とmg=20/30と同水準、mg=60から
急増(22本/疑い7本)、mg=80で深刻(83本/疑い37本)——(1)の結論と
同じ形状のカーブで、**`mg=40`という結論自体は変わらず**。

**対応**: `config/default.yaml`の`hough_mg`を5→40、
`diag_common.py`の`TRACK_CFG["hough_max_gap"]`を5→40に変更。
`finder.py`は(1)で既に40に変更済みでそのまま。高速テスト52件は
両方の変更後も通過。

**未解決の設計課題として残った点**: `finder.py`(thr=20/ml=25)と
`config/default.yaml`(thr=35/ml=30)のthr/ml自体の不一致は今回
手を付けていない——今日検証したのはmg軸だけで、thr/ml軸は未検証。
単一の設定ソースに統一する(例: finder.pyのデフォルトをyamlから
読む)価値はありそうだが、大きめの変更になるため今回は見送り、
ユーザー判断待ち。

## 2026-07-22 — Hough `max_gap`スイープ: 本番デフォルト(mg=4)は
本物飛跡ピクセルの56%を検出し損ねている。mg=40を推奨値に

ユーザー観察「raw houghの時点で精度あまりよくないねえ。だいぶ
落としてるし、1つの線が細切れになっている印象」を受けて調査。

**新規の教師データ不要な検証手法**: 既存512件のセグメント単位
true/false決定のうち、trueのものだけを幅4px(`e07-ml-binary-
segmentation`の`real_label_dataset.py`と同じ規約)でラスタライズし
「本物飛跡ピクセル」の正解マスクを作成。これに対し`find_tracks`が
検出したセグメントをラスタライズしたマスクとの重なりでrecallを
測定——手ラベリングを追加せずにHoughパラメータの定量評価が可能に
なった。thr=20/min_line=25(本番値)は固定し、`max_gap`のみ4〜120で
振った(4タイル平均)。

| max_gap | recall | raw候補数 | 長い(>300px)偽ブリッジ疑い(V991) |
|---|---|---|---|
| 4(現行本番) | 44.0% | 702 | 0 |
| 20 | 81.9% | 6,871 | 3(疑い1) |
| 30 | 89.0% | 12,091 | - |
| **40** | **92.2%** | 12,495 | **2(疑い1、mg=20と同水準)** |
| 50 | 94.7% | 11,243 | - |
| 60 | 96.1% | 9,925 | 12(疑い6) |
| 80 | 97.0% | 7,701 | 68(疑い36) |
| 120 | 98.5% | 4,717 | 208(疑い151) |

**現行の`_HOUGH_MG=4`(`finder.py`)は本物飛跡ピクセルの56%を
検出し損ねている**——これが③のフラグメンテーション問題の主因と
みられる。raw候補数はmax_gap=30近辺でピークに達した後、mg=120まで
単調減少する(Hough自身が断片を1本の長い線として直接検出するように
なるため)。ただし大きすぎるgapは無関係な点同士を繋ぐ「偽ブリッジ」
を生む(セッション序盤にmg=100で発見した2495pxの偽線と同種の問題)
——「長さ>300px かつ 粒密度<0.02」を偽ブリッジの簡易診断指標として
導入し、mg=60を境に急増することを確認(可視化`/tmp/
e07_mg80_check.png`で目視確認、mg=80は無秩序に交差する長い赤線が
多数出現し明らかに悪化)。

**推奨**: `_HOUGH_MG`を4→**40**に変更。recallを44%→92.2%まで
倍増させつつ、偽ブリッジの水準はmg=20と同程度に抑えられる。
まだコード変更は未適用(ユーザー確認待ち)。

## 2026-07-21 — `track_classifier.py`の`width_px`特徴量を
`footprint_w_px`に置き換え、LOTO精度が全4タイルで同等以上に改善

ユーザー提案「長さ/幅比も大事だが、絶対値も見るべき。実際の飛跡は
決まった幅を持つはず」を受けて調査・実装。

**旧`width_px`の正体**: `Track.width_px`(`module/pipeline/
finder.py`の`_measure_tracks`)は「線分近傍(grain_radius=10px以内)
の粒重心の、線からの垂直距離の標準偏差」であり、**画像上の実際の
断面幅を直接測っていない**。512件の手動ラベルで確認したところ、
TRACK中央値1.63px vs JUNK中央値1.83pxとほぼ同一——**判別力ゼロ**
だった。n_grains=2のような低粒数セグメントでは統計的に不安定
（2点のstdはほぼ意味を持たない）ことが原因とみられる。

**新`footprint_w_px`を実装**(`module/track_classifier.py`、
`_footprint_width`関数): 二値マスクを直接、線分に沿った各サンプル
点で垂線方向に前景ピクセルの連続長を測定(`_FOOTPRINT_MAX_SEARCH_PX
=15`)、中央値を取る。512件で測定した結果、TRACK中央値5.00px
(1.45μm@0.29μm/px) vs JUNK中央値6.00px(1.74μm)と、**物理的に
解釈可能な絶対値としての差**が出た(本物の方が細い)。

**長さ/新footprint幅の比**も試したが、単一特徴量としての判別力は
長さ単独(74.4%)とほぼ同じ(75.4%)——強く相関しており独立した
新情報ではなさそう。ただし新footprint幅自体は他特徴量(cov_frac/
max_gap/n_grains)と組み合わせた分類器で有効と判明(下記)。

**FEATURE_NAMESの`width_px`を`footprint_w_px`に置換**し、4タイル
LOTO再検証(旧`width_px`版とのapples-to-apples比較、
`/tmp/loto_validate.py`):

| held-outタイル | precision(旧→新) | recall(旧→新) |
|---|---|---|
| V00000011 | 81.8%→84.4% | 96.4%→96.4% |
| V00000991 | 72.7%→73.5% | 88.9%→92.6% |
| V00001012 | 87.0%→90.8% | 59.3%→58.5%(-0.8pt) |
| V00001339 | 82.8%→84.8% | 72.7%→84.8%(+12.1pt) |
| 平均 | 81.1%→83.4% | 79.3%→83.1% |

**全4タイルでprecision同等以上、recallも3/4で改善**(V00001339は
+12.1pt)。`module/track_classifier.py`のFEATURE_NAMES/
extract_featuresを新特徴量に更新済み(このモジュールは分類器を
毎回`results/manual_labels`から学習し直す設計のため、永続化された
モデルファイルの更新は不要)。`Track.width_px`自体
(`finder.py`/`cluster.py`の`_track_quality`が使用)は未修正—
`cluster_tracks`の代表選択スコア(`mean_intens*sqrt(length)/width`)
が同じ不安定な`width_px`で割っているため、断片化除去の質にも
影響している可能性がある。次の一手候補: `cluster.py`の
quality scoreも`footprint_w_px`ベースに直すか検討。

ライブスコアリング(`/label_uncertain_segments`)への速度影響:
1スライス692セグメントで0.200秒(0.29ms/セグメント)、
96,000候補換算で**約27.7秒**(旧実装の約21.6秒から+28%)——
許容範囲内。

## 2026-07-19 10:30 JST — 線分検出段(③)の実態調査:
生Houghは9割前後がn_grains≤2のノイズ濃厚、分類器フィルタは
部分効果のみで不安定

ユーザー提案「いきなり反応点に行くのではなく、まずは直線検出
方法の議論から始める」を受けて、③(Hough線分検出)の生の出力を
調査。`fullscan-image`のsshfsマウントが切断されていたため、
ユーザーがfuse-t NFS経由で`~/mnt/kek_e07`に再マウント。マウント
先の構造がsshfs版と異なり(`~/mnt/kek_e07`直下が旧
`fullscan-image/E07/`相当、E373サイドカーなし)、
`fullscan-image`をシンボリックリンクから実ディレクトリに変更し
`fullscan-image/E07 -> ~/mnt/kek_e07`のシンボリックリンクを
配置して対応(discussion.md/discussion_ja.mdに記録)。

**③生Hough検出の実態**(`find_tracks`デフォルトパラメータ
hough_thr=20/min_line=25/max_gap=4、specials_x20の3事象):

| 事象 | スライス数 | 総セグメント数 | 平均/スライス | 長さ中央値 | n_grains≤2 |
|---|---|---|---|---|---|
| D005 | 200 | 97,765 | 489本 | 34px | 89.2% |
| KISO | 60 | 38,236 | 637本 | 34px | 98.7% |
| T004 | 200 | 141,858 | 709本 | 34px | 87.0% |

1スライスあたり500〜700本、9割前後がn_grains≤2(実質ノイズ濃厚)
——2026-07-19 09:00エントリで見つかった④の候補爆発(347〜380件/
タイル)の直接の原因が③にあることを裏付ける。

**既存分類器(512ラベル学習済み、`track_classifier.py`)で③を
フィルタしてから④をやり直した結果**:

| 事象 | raw→分類器通過 | マージ後候補(前→後) | 正解の順位(前→後) |
|---|---|---|---|
| D005 | 97,765→17,473 (17.9%) | 347→226 | 10位→5位(改善) |
| KISO | 38,236→11,452 (30.0%) | 196→122 | 7位→3位(改善) |
| T004 | 141,858→17,980 (12.7%) | 380→246 | 10位→**25位(悪化)** |

D005/KISOは改善したがT004は悪化——分類器が正解vertexを構成する
セグメントの一部まで誤って弾き、相対的に他の偽ピークが浮上した
とみられる。**重要な懸念**: この分類器は`module/server/
labeling.py`のレビューUIで使うHoughパラメータ(hough_thr=8,
min_line=10, max_gap=20、人間が見やすいよう緩め)で検出された
セグメントで学習されているが、`find_tracks`の本番デフォルト
(hough_thr=20, min_line=25, max_gap=4)は全く別物——学習時と
適用時でセグメント母集団の性質が異なる(ドメインシフト)可能性が
高く、T004の悪化はこれが一因と推測。次に確認すべきは、
学習用ラベル収集時のHoughパラメータと実際に適用するパイプラインの
Houghパラメータを一致させて再学習・再検証すること。

## 2026-07-19 09:00 JST — 【重要な訂正】(14)の「35テスト全通過」は
recallの検証に過ぎず、precisionは未検証・実際には非常に低い

ユーザーからの指摘:「テストを通過したとしても、候補点が膨大な
場合は、拾いまくっているだけかもしれない。その可能性はない？」
——これを受けて実測したところ、その通りだった。

`_run_pipeline_merged`(Method A、`find_vertices`+
`merge_vertex_slices`)を3事象で実行し、マージ後の候補数・正解の
順位を計測:

| 事象 | raw候補(per-slice) | マージ後候補数 | 許容誤差内候補数 | 全候補中n_tracks最大 | 正解候補のn_tracks順位 |
|---|---|---|---|---|---|
| D005 | 1458 | 347 | 13 | 15 | 10位 |
| KISO | 756 | 196 | 8 | 11 | 7位 |
| T004 | 1553 | 380 | 15 | 14 | 10位 |

1事象(1タイル)だけでマージ後200〜380件もの候補が出ており、
`test_special_vertex_position`が検証しているのは「許容誤差
(200px/30μm)以内に候補が**1つでも**あれば合格」という基準——
つまりrecallのみ。しかもn_tracks(track数)で候補をランク付けする
と、正解は6〜9件の**他の(偽の)候補より下位**に埋もれる。
`_MIN_N_TRACKS=5`という閾値だけでは全く判別力が足りていない。

**訂正**: (14)で「既知9事象すべてで頂点位置を検出」と報告したのは
事実だが、これは「未知のfullscanタイルに適用すれば実用的な候補
リストが得られる」ことを意味しない。既知の正解位置と照合できる
specials_x20だからこそ「合格」と判定できただけで、正解が分から
ない実データに適用すれば、1タイルあたり数百件の偽候補が出て、
結局人間による選別が必要になる、との結論。

**含意**: これまで議論していた物理ベースのアイデア(dE/dxによる
粒子識別、MCシミュレーションによる頂点運動学的整合性チェック、
段階的フィルタ構成)が、「あれば良い追加機能」ではなく
**「精度を実用レベルまで上げるために必須の最終段フィルタ」**
であることが定量的に裏付けられた。次の一手候補: 正解に一致する
候補と、それより上位にランクされた偽候補(D005で9件、KISOで6件、
T004で9件)の特徴量を比較し、何が両者を分けるか(n_tracks以外の
軸で分離できるか)を調べる。

## 2026-07-18 (14) — `specials_x20`検証: Pure Pythonパイプラインが
既知9事象すべてで頂点位置を正解許容誤差内で検出

ユーザーの質問「実際ダブルハイパー核事象を拾うにはどうしたら
いいんだろうか」を受け、既存の`tests/test_specials.py`
（`@pytest.mark.slow`、書かれたのは2026-06-14で当時は「ほとんど
の事象で失敗する」と明記されていた）を再実行。対象は
`specials_x20/`配下の確定ΛΛハイパー核事象（D005, D013, IBUKI,
IRRAWADY, KISO, MINO, NAGARA, T004, T011、ground truthは
`tests/specials_gt.json`、許容誤差 XY 200px / Z 30μm）。

パイプラインは`module.matlab_export`（MATLAB連携）とは別の、
Pure Python経路: `find_tracks`→`find_vertices`→
`merge_vertex_slices`（`min_tracks=3, max_ep=150px,
max_ep_frac=0.5, min_intens=10, min_len_px=50px,
beam_angle_cut=0, min_angle_spread=0`）。

**結果: 35テスト全通過**（`test_special_vertex_detected`/
`test_special_vertex_position`/`test_special_reader_loads` ×
9〜13事象、533秒）。`test_special_vertex_position`は9事象すべてで
正解位置の許容誤差内に頂点を検出——ドキュメント文字列の「ほとんど
失敗する」という記述と矛盾。

原因調査: `module/pipeline/finder.py`は2026-07-11に
「fix: OpenCV 5 HoughLinesP output shape compatibility」で更新
されている（`lines[:, 0]` → `lines.reshape(-1,4)`、macOS移植検証
時にこのセッションで実施した修正）一方、`test_specials.py`自体は
2026-06-14のまま。**仮説**: このOpenCV5互換性バグがHough検出その
ものを静かに壊しており、当時の「ほとんど失敗」はvertex探索ロジック
の欠陥ではなくこのバグが原因だった可能性が高い。同修正がこの
テストの再実行では未検証のまま残っていたため、今回偶然
再現・確認できた形。`test_specials.py`の古いdocstringは更新済み。

**戦略的な意味**: MATLAB連携パイプライン（`export_hits_grid`→
MATLAB `detectlseg`/`integrate_smallregions`/`detectbunki`、
KISO個別デバッグに多くの時間を割いた経路）とは別に、**より単純な
Pure Pythonの頂点探索が既知9事象全てで実証済み**という結果。
次の一手候補: この検証済み検出器を（正解が分かっている
specials_x20ではなく）実際のfullscanタイル全体に対して走らせ、
未知の候補を探す一次スクリーニングとして使う。ユーザーへ報告済み、
具体的な実行計画はまだ未確定。

## 2026-07-18 (13) — 3トラック目「教師なし学習」を試行・実装統合作業まとめ

ユーザー: 「だんだん疲れてきた。これやっぱり人が必要？」→
「できることを進めてみてください。conventional+houseでML使わない
方も。また、MLの別のアイデアだけど、教師をMCで作るか、教師なし
学習は無理かな？」→「OK,MCは一旦なし。教師なし学習で飛跡とゴミを
分類できないか進めてみて。」

複数の作業を実施:

**(1) `track_classifier.py`を`module/server/`から`module/`直下へ
移動**——`matlab_export.py`（コアパイプライン）からも使うため、
サーバー専用の位置に置くのは不適切と判断。`detect_lite_tracks`/
`LiteTrack`を新規追加（binary+fog画像から直接Hough検出、
find_tracksのI/O・座標変換オーバーヘッドを回避）。

**(2) `remove_unaligned_noise_v2`を`matlab_export.py`に実装**——
小さい連結成分の生存条件を「任意のHough線に整列」から「分類器/
閾値がtrackと判定したHough線に整列」に変更。`method="threshold"`
（非ML、cov_frac>=0.725かつmax_gap<=19px、512件でグリッド
サーチしF1最良、precision 58.7%/recall 89.7%）と
`method="classifier"`（学習済み分類器、LOTO precision 73〜93%）
の両方を実装。`export_hits_grid`に`denoise_method`パラメータ
（off/legacy/threshold/classifier）として配線、CLIにも
`--denoise-method`/`--labels-dir`を追加。動作確認: 実タイルで
classifier方式が131,501ヒット(2分45秒)、目視でも長い実飛跡は
両手法で保持されつつノイズが減ることを確認。全52テスト
(pytest)通過。

**(3) Frangi vesselnessフィルタ（教師なし、classical image
processing）を試作**——`skimage.filters.frangi`、1スライス
2.35秒。目視では長い実飛跡が連続的な応答として浮き出て、周辺の
粒状ノイズは抑制される良い兆候。ただし孤立した明るい粒にも
ある程度反応してしまい、単独では綺麗に分離できない。既存6特徴量
への7番目の特徴量として追加してLOTO再検証したが、**改善効果は
無かった**（4タイルとも±0.03以内の誤差範囲）——cov_frac/max_gap
と情報が重複しているとみられる。

**(4) 教師なしクラスタリング（KMeans/GMM）で飛跡/ゴミを分類できる
か検証**——4タイル全体の未レビューHough候補プール（95,387件）で
KMeans(2)・GaussianMixture(2)を学習（ラベル不使用）、512件の
既存ラベルで答え合わせ。結果: **KMeans precision 48.6%/recall
91.5%、GMM precision 46.1%/recall 97.3%**——ラベル分布の偏りを
考えると、ほぼ当てずっぽうに近い。クラスタ中心の差を確認したところ
`max_gap_px`と`cov_frac`が最も効いており（教師ありと同じ特徴量が
効いている）、「見るべき特徴」は合っているが「境界の引き方」が
人間の判断と一致しない。width_px/cov_frac/max_gapの3特徴量に
絞っても改善せず（precision 50.3%/recall 43.9%）。

**結論**: 教師あり分類器がLOTO precision 73〜93%止まり（100%に
遠い）という事実自体が、true/falseの特徴量分布がかなり重なって
いることを示しており、教師なし学習（境界を知らない）ではこの
重なりを人間の判断通りに分割できない。**ゼロからの学習という
意味では、何らかの人間によるラベルが本質的に必要**という結論。

TODO: 半教師あり（label propagation等、512件のラベル+大量の
未ラベルデータを組み合わせる）を次の選択肢として検討可能。

## 2026-07-18 (12) — `/label_mix`に戻して38件追加（512件到達）。悪化傾向にわずかな歯止め、完全回復はまだ

`/label_mix`に戻して継続、実ファイル差分で38件追加（合計512件、
true223・false289）。内訳22true/16false(58%がtrue)——直近2ラウンド
の不確実性サンプリング（84〜86%がfalse）から一転、`/label_mix`の
自然な分布（長さ降順で純度が高い）に戻ったことでバランスが改善。

なお、UIのカウンタ表示（ユーザー申告「150」）と実ファイル差分
（38件）が一致しない件: `/label_mix`の再開ロジックは
ラウンドロビン順で「まだ判定していない最初の項目」を探すため、
`/label_uncertain`で判定済みの（順序上は散らばった位置にある）
項目を自動でスキップしながらpos（キュー内位置）を進める。この
pos自体は判定件数と一致しない——実データの整合性に問題は無い。

leave-one-tile-out（512件）:

| held-outタイル | r1(374) | r2(424) | r3(474) | r4(512) |
|---|---|---|---|---|
| VX0011_VY0000 | .864/1.00 | .864/.950 | .826/.950 | .818/.964 |
| VX0001_VY0022 | .850/.944 | .818/1.00 | .783/.857 | .727/.889 |
| VX0022_VY0022 | .769/.846 | .917/.579 | .934/.530 | .870/.593 |
| VX0034_VY0029 | .905/1.00 | .905/.864 | .792/.731 | .828/.727 |

VX0022_VY0022のrecallに小幅回復の兆し（0.530→0.593）。ただし
VX0001_VY0022はprecisionがさらに低下（.783→.727）——完全な
回復とは言えず、round1の水準にはまだ届いていない。
`/label_mix`をこのまま継続する方針。

## 2026-07-18 (11) — 不確実性サンプリングを2ラウンド続けた結果、LOTO性能が悪化傾向——一旦`/label_mix`に戻ることを提案

`/label_uncertain`でさらに50件追加（合計474件、true201・
false273）。今回も新規50件のうち42件(84%)がfalseで、前回
(86%)と同様の偏り。

leave-one-tile-out性能の推移（3ラウンド分）:

| held-outタイル | round1(374件) | round2(424件) | round3(474件) |
|---|---|---|---|
| VX0011_VY0000 | .864/1.00 | .864/.950 | .826/.950 |
| VX0001_VY0022 | .850/.944 | .818/1.00 | .783/.857 |
| VX0022_VY0022 | .769/.846 | .917/.579 | .934/.530 |
| VX0034_VY0029 | .905/1.00 | .905/.864 | .792/.731 |

（precision/recallの順）。round2で懸念したrecall低下傾向が
round3でも継続し、今回はprecisionも4タイル中3タイルで低下。
`class_weight='balanced'`（クラス不均衡補正）を試したが
ほぼ改善せず（VX0022_VY0022: recall 0.530→0.567程度）——単純な
クラス不均衡の問題ではなさそう。

**仮説**: 不確実性サンプリングが2ラウンド連続でfalse側に偏った
結果（84〜86%）、「中間的な特徴量（coverage・widthなどが微妙な
値）」の学習データがfalseに偏りすぎ、分類器が「迷ったらfalseと
言う」ルールに過学習している可能性。同じような中間的特徴量を
持つ本物の飛跡のサンプルが不足していると考えられる。

**対応**: 不確実性サンプリングだけに頼るのを一旦中止し、
`/label_mix`（タイル横断・長さ降順、偏りの少ない自然な分布）に
戻ることを提案。局所的な不確実性だけを追いかけると、サンプル
される領域が偏り、かえって分類器を歪めるリスクがあることが
2ラウンド連続で確認できた——active learningの効果は前提条件
（十分な母集団の多様性・データ量）が整ってから見るべき教訓。

TODO: `/label_mix`でベースとなる分布を厚くしてから、
`/label_uncertain`を再度試す。あるいは不確実性サンプリングと
`/label_mix`を交互に使うハイブリッド方式も検討。

## 2026-07-18 (10) — `/label_uncertain`で50件追加（424件到達）。precision改善・recall悪化のトレードオフを確認

`/label_uncertain`で50件レビュー。内訳: true 7件・**false 43件
(86%)**——分類器が「五分五分」と判断した候補は、実際はほぼ
ゴミだった。旧分類器が境界付近で本物寄りに楽観的すぎたことを
示唆。

再学習後のleave-one-tile-out（424件、true193・false231）:

| held-outタイル | precision | recall（前回比） |
|---|---|---|
| VX0011_VY0000 | 0.864(→) | 0.950(1.000から↓) |
| VX0001_VY0022 | 0.818(0.850から↓) | 1.000(0.944から↑) |
| VX0022_VY0022 | **0.917**(0.769から↑) | **0.579**(0.846から↓) |
| VX0034_VY0029 | 0.905(→) | 0.864(1.000から↓) |

一番データ量の多いVX0022_VY0022でprecisionが76.9%→91.7%へ
大きく改善。ただし追加50件がfalse側に偏っていた（86%）ため、
分類器全体が「trueと言うにはより厳格」に傾き、recallが
（特にVX0022_VY0022で84.6%→57.9%と）低下した。想定内の
トレードオフだが、下がり方はやや大きい。

TODO: 次のラウンドでは、false寄りの不確実性サンプリングに偏り
すぎないよう、true側の境界事例（分類器が高確信でtrueと言って
いるが実際は際どいもの、あるいは逆に低確信でfalseと言っている
が実際はtrueなもの）も意識してバランスを取る。あるいは
`/label_uncertain_segments`のスコアリングにクラスバランスを
考慮した補正を入れることも検討。

## 2026-07-18 (9) — 不確実性サンプリング（active learning）モード `/label_uncertain` を追加

ユーザー提案: 「取り急ぎ回帰分類が動きそうなら、それを元に、
人の分類が必要な、たとえば分類信頼度が低い場合を優先して集めて
手でラベルするのはどう？」——2026-07-18(8)でleave-one-tile-out
検証により実用段階と判断した分類器を使い、判定確率が0.5に近い
（＝分類器が最も迷っている）未レビューセグメントを優先的に
レビューさせるモードを追加。

新規`module/server/track_classifier.py`: 特徴量抽出（6特徴量、
2026-07-18(6)と同じ: length_px, n_grains, width_px, mean_intens,
cov_frac, max_gap_px）とロジスティック回帰の学習・推論を
`labeling.py`から独立させ、テスト可能な形に切り出した。

**高速化**: 素朴に実装すると1セグメントごとに二値画像を
`min/max`スライス+`.any()`で調べるループになり、タイルあたり
2万件超のセグメントでは実用的な速度が出ない。二値画像を1回だけ
`cv2.dilate`（半径2px）してから、各セグメントの線上サンプル点を
**まとめてfancy indexingで参照**する方式に変更——1回きりの
画像処理＋配列参照で、ループはgap長計算のみに縮小。実測: 4タイル
・合計約96,000セグメントの特徴量抽出＋分類器学習＋推論で約21.6秒
（1セッション開始時に1回だけなので許容範囲）。

新エンドポイント: `/label_uncertain`（ページ）・
`/label_uncertain_segments`（分類器を全ラベルで学習→
`_MULTI_TILE_SOURCES`の未レビューセグメント全件をスコアリング→
不確実性降順で上位1000件を返す）。ランキングは**ページ読み込み時
のスナップショット**（1クリックごとに再学習・再ランキングは
しない——順番が毎回変わると混乱するため。更新したい場合は
リロードで再学習される）。

動作確認: `p(track)=0.500`の最上位候補を目視したところ、実際に
「近くの本物の飛跡に近い、明るいが微妙な粒」——判断に迷う妥当な
境界事例が正しく上位に来ていることを確認。requirements.txtに
`scikit-learn>=1.4`を追加。

今後は`http://127.0.0.1:8123/label_uncertain`で、分類器が最も
迷っている候補を優先してレビューできる。

## 2026-07-18 (8) — leave-one-tile-out検証: 手作り特徴量ロジスティック回帰は本物の汎化を確認（precision 76.9〜90.5%）

`/label_mix`導入後、374件（4タイル: VX0022_VY0022 299件、
VX0011_VY0000/VX0034_VY0029/VX0001_VY0022が各25件前後）に到達。
2026-07-18(6)の6特徴量ロジスティック回帰を、単純な5分割CVでは
なく**leave-one-tile-out**（1タイルを完全に除いて訓練、そのタイル
だけでテスト）で再検証:

| held-outタイル | n | precision | recall |
|---|---|---|---|
| VX0011_VY0000 | 25 | 0.864 | 1.000 |
| VX0001_VY0022 | 25 | 0.850 | 0.944 |
| VX0022_VY0022 | 299 | 0.769 | 0.846 |
| VX0034_VY0029 | 25 | 0.905 | 1.000 |

**本物の汎化を確認**——訓練時に一度も見ていないタイルでもprecision
77〜91%、recall 85〜100%を達成。単一タイル評価だった2026-07-18(6)
の84.6%/84.0%とオーダーは近く、過大評価ではなかったことも裏付け
られた。

一方、同時にe07-binary-segmentation側で検証したCNN（生画素から
ゼロで学習、生成画像による目視確認）は、タイル単位の指標こそ
改善したが実画像では依然ショートカット学習が残っていた
（詳細はe07-binary-segmentationのanalysis-note.md参照）。少数
タイルのデータでも「既に絞り込まれたHough候補を分類する」
古典特徴量アプローチの方が、「画素からゼロで構造を学習する」
CNNより遥かにデータ効率が良いことが明確になった。

**結論**: この分類器を`remove_unaligned_noise`の代替/追加として
実装する価値が十分にあると判断。次のステップとして検討する。

## 2026-07-18 (7) — 複数タイル混合レビューモード `/label_mix` を追加

ユーザー: 「それならいろんなタイルのデータを混ぜて表示してよ」
——CNN・ロジスティック回帰どちらも単一タイルの過学習リスクが
指摘された流れを受け、複数タイルを1つのレビューセッションで
混ぜて出せるようにした。

`module/server/labeling.py`に`_MULTI_TILE_SOURCES`（現行の
VX0022_VY0022に加え、AREA00グリッド内で離れた3タイル:
VX0011_VY0000（端）, VX0034_VY0029, VX0001_VY0022、いずれも
idx=29）を追加。新エンドポイント`/label_mix`
（ページ）・`/label_mix_segments`（各ソースのセグメント数取得）。
クロップ表示・判定保存・再開ロードは既存の
`/label_segment_crop`・`/label_decide`・`/label_load`を
そのままソースごとに呼び出す形で再利用（新規エンドポイント最小限）。

**順序**: 単純に1タイル分やってから次、ではなく、**ラウンドロビン
で複数タイルを交互に**提示する方式にした（tile1-seg1, tile2-seg1,
tile3-seg1, tile4-seg1, tile1-seg2, ...）——セッションが短く
終わっても全タイルに触れられるようにするため。

**保存形式は変更なし**: 判定は引き続きタイル・スライスごとの
既存ファイル形式（`results/manual_labels/<tile>__z<idx>.json`）に
保存されるため、`real_label_dataset.py`・`train_real.py`は
無変更で複数タイルのラベルを自動的に拾える。

動作確認: `/label_mix`・`/label_mix_segments`とも200、新タイルの
クロップ表示も正常（VX0011_VY0000は視覚的にも別タイルと分かる
密度・ノイズパターンの違いが確認できた）。

今後は`http://127.0.0.1:8123/label_mix`でレビューを継続する。

## 2026-07-18 (6) — 300件ラベルで再確認・複数特徴量のロジスティック回帰でprecision 84.6%/recall 84.0%（要複数タイル検証）

ラベルが299件（true 131・false 168、longest-first順で進むにつれ
false比率が増加）に到達。前エントリのカバー率/最大ギャップ長を
再計算——傾向は安定（カバー率89.7% vs 70.3%、最大ギャップ7.7px
vs 13.8px、200件時点とほぼ同じ）。閾値スイープでは単純な単一
閾値では綺麗に分離できないことも確認（T=10pxでrecall58%/false
除去83%程度が上限）。

**複数特徴量での分類器を試行**: `length_px, n_grains, width_px,
mean_intens, cov_frac, max_gap`の6特徴量でロジスティック回帰
（scikit-learn、`pip install scikit-learn`で追加導入、requirements
未追加=まだ探索的解析のみ）。5分割交差検証で**precision 84.6%
/ recall 84.0%**——単一閾値はもちろん、元の古典ノイズフィルタ
（2026-07-14評価でprecision 20〜23%）を大幅に上回る。

係数（標準化後）: cov_frac +2.20（最強の正の予測因子、ギャップ
発見を裏付け）、length_px +1.96、n_grains +0.70、mean_intens
+0.56、width_px -0.375（大きいほどfalseらしい、整列度仮説と整合）、
max_gap +0.32（cov_fracと相関が強く多重共線性で符号が不安定な
可能性、要注意）。

**重要な留保**: これも単一タイル（VX0022_VY0022）のみのデータ。
2026-07-18(5)エントリでCNNが単一タイルの輝度ショートカットを
学習してしまった前例があるため、この84.6%/84.0%も**複数タイルで
再検証するまでは確定値として扱わない**。ただし使っている特徴量
（ギャップ・整列度など）は生輝度そのものより物理的解釈のある
量なので、CNNの場合よりは過学習リスクはやや低いと考えられる。

TODO: 複数タイルのラベルが揃い次第、tile単位でtrain/val分割して
再評価。良ければ`remove_unaligned_noise`をこの分類器ベースの
判定に置き換える方向で検討。

## 2026-07-18 (5) — 「連続性が悪い」＝線上のギャップを定量化——true/falseで明確な差を確認（有力な追加フィルタ候補）

前エントリのwidth_px（垂直方向のばらつき）比較に対し、ユーザーが
意図を訂正: 「連続性が悪い、はギャップがあるという意味。点々と
不連続に飛んでいる部分が選ばれているように見える」——垂直方向
ではなく、線の**長さ方向**の途切れ（gap）についての指摘だった。

`preprocess()`が返す二値前景マスク上で、各セグメントの線分に
沿って1px刻みでサンプリングし、(1)カバー率(サンプル点の半径2px
以内に前景画素がある割合)、(2)線上の最大連続ギャップ長(px)、を
計測して比較（true 113件・false 93件、`hough_max_gap=20`時点）:

| | true | false |
|---|---|---|
| カバー率 | 89.1% | 68.8% |
| 最大ギャップ長(px) | 8.2(中央値8.0) | **14.1(中央値14.1)** |

**明確に確認**: falseセグメントは平均で約2倍長いギャップを含み、
現在のHough `max_gap=20`の許容値に近い（＝Houghがぎりぎり許容する
最大級の隙間を飛び越えて無関係な粒同士を繋いでしまっているケース
が多いと考えられる）。ユーザーの直感は正しかった。

TODO: `remove_unaligned_noise`または新規フィルタとして、線上の
カバー率/最大ギャップ長にしきい値を設ける追加ノイズ判定を検討。
このデータだと閾値10px前後（true側の分布にほぼ収まり、false側の
大半を排除）が候補になりそう——ただしまだ200件規模なので、
ラベルが増えてから正式に閾値を決める。

## 2026-07-18 (4) — ラベリングUIに生画像パネルを追加（3枚構成）。200件ラベルでtrue/false統計を確認——整列度は仮説通り、輝度は逆だった

**UI変更**: ユーザーから「本当の生画像も一番左に表示してほしい
（合計３枚）」との要望。`_segments()`が`raw_img`も返すように変更、
`_segment_crop_png(raw_img, fog_img, t)`で
[生画像 | fog除去後(無地) | fog除去後+ハイライト]の3パネル構成に
変更（`module/server/labeling.py`）。生画像パネルは周辺減光まで
そのまま見えることを確認。

**200件ラベル時点でのtrue/false統計チェック**: ユーザーの
「飛跡が連続して繋がっていないのに、輝度が大きい粒度が大きい
ものが選ばれてしまっているように見えた」という印象を、貯まった
ラベル（true 109・false 91）で定量確認。`Track`オブジェクトの
`width_px`(セグメント軸に対する垂直方向のばらつき=整列度)・
`mean_intens`(平均輝度)を比較:

| | true (n=109) | false (n=91) |
|---|---|---|
| length_px | 158.9 | 121.3 |
| n_grains | 4.2 | 4.4 |
| width_px | 1.69 | **2.08** |
| mean_intens | **14.40** | 9.59 |

**部分的に一致、輝度は逆**: 「連続性が緩い」はwidth_pxで支持される
（false の方が軸からのばらつきが大きい＝整列度が低い）。一方
「輝度が大きい」は逆で、**true(本物飛跡)の方がセグメント平均輝度
が高い**（連続して露出した銀粒の並びなので物理的にも自然）。
ユーザーが感じた「輝度」は、セグメント全体平均ではなく局所的に
1〜2粒だけ突出して明るい/大きい粒の存在を指していた可能性がある
（`mean_intens`は平均なのでそういう局所的突出は薄まる）。

TODO: `width_px`を古典ノイズフィルタ（`remove_unaligned_noise`）の
追加判定基準として使えないか検討。局所的な「突出して明るい粒」を
検出する指標（例: セグメント上の grain 輝度の最大値/分散）も
`Track`に追加すれば、ユーザーの元の印象をより直接検証できる。

## 2026-07-18 (2) — 方針決定: 2トラック並行（1. 古典+Hough、2. 生画像→ML→MATLAB）。ラベル→学習データ変換パイプラインをe07-binary-segmentation側に用意

ユーザー: 「1.)conventional+hough検出と、2.)生画像入力->ML->matlabの
２つの方法を並行して進めることにする」。今後、以下の2トラックを
並行して進める:
1. 古典的なfog/Otsu/ノイズ除去/Hough直線検出パイプライン
   （本リポジトリ）——手動ラベリングツールで得た実E07正解データで
   検証・調整する。
2. 学習ベースの二値分割モデル（`e07-binary-segmentation`）——
   **入力は生画像（fog除去前）**、Kasagi氏のbinary_segmentationと
   同じ設計（ユーザー明示指示）。

両トラックとも、本リポジトリのWebラベリングツール
（`module/server/labeling.py`、`results/manual_labels/*.json`）で
得られる同じ手動ラベルを共有データソースとする。現時点で15
セグメント分（全てtrue、最長順のためまだfalse無し）ラベリング
済み。

`e07-binary-segmentation/src/real_label_dataset.py`（新規）を
作成し、ラベルJSON→（生画像, スパースマスク）の変換パイプラインを
実装・動作確認済み。マスクは3値（-1=未レビュー/無視、0=ゴミ、
1=飛跡）——longest-first順のレビューはほとんどのスライスで未完了
であり、「まだクリックしていない」を「背景」として扱うと、
未レビューだが実は本物の飛跡である候補に誤った負例ラベルを
注入してしまうため、明示的に判定済みの画素のみを学習対象とする
設計にした。false判定がまだ0件のため、スクリプトは「これだけでは
分類器を学習できない、レビューを続けてほしい」という警告を出す。

## 2026-07-18 — Hough max_gapを60→40→20と段階的に縮小、右パネルのハイライトを半透明に、実利用フィードバックで継続調整

ユーザーの実利用フィードバックが続いた:
- 「それにしてもゴミが多いので、ちょっとだけ戻した方がいいかも。
  あと、右側の重ねた絵でも、赤線は半透明で」→ max_gap 60→40、
  右パネルのハイライトを`cv2.addWeighted`で半透明に戻す
  （side-by-side化の際に不透明にしていたのを修正）。
- 「mgもっと短く」→ さらに40→20へ。同タイルでの最長セグメント
  推移: mg=60は1308px、mg=40は964px、mg=20は540px。総セグメント
  数はmg=20で23,737（mg=60の13,769より多い＝断片化はまだ残る
  が、ユーザーの実感としてはこちらの方がノイズ候補が減って
  見えるとのこと）。

このパラメータは厳密な数値最適化ではなく、実際にレビューしながらの
感覚的なチューニングであり、`hough_mg`のURLクエリでいつでも
上書き可能。現在の既定値は20（`module/server/labeling.py`）。
さらに短くしたい場合も指示があれば追従する。

## 2026-07-15 16:25 JST — レビュー順序を長さ降順に戻す（シャッフルへの変更は誤解による過修正）。max_gap=100は誤った橋渡しを確認、60を上限として維持

ユーザーからの訂正: 「トラック純度が高いのはいいことだよ。今度は
ゴミだらけ。さっきの状態でもよかったけど、これなら私が判定する
必要あるか、と思っただけだ」。前エントリで「降順だとほとんど
綺麗なトラックに見える」を問題点だと解釈しシャッフルへ変更した
のは誤り——ユーザーの発言は単なる感想（自分の判断が本当に必要
か、という独り言）であり、降順（高純度）自体は望ましい状態
だった。`_segments()`のソートを長さ降順に戻す
（`module/server/labeling.py`）。

**もう一つの指摘**: 「1つの飛跡が別セグメントになってしまって
いるかもね」——max_gap=60でも稀に同一の実飛跡が複数セグメントに
分かれて残る可能性を確認するため、max_gapをさらに80/100/130まで
振った。結果、**max_gap=100で明確な誤った橋渡しを確認**:
最長セグメント（2495.1px）を目視したところ、実際にはどの実飛跡
にも沿っておらず、密な背景ノイズを対角線状に無理やり橋渡し
していた（crop画像で確認）。60の2番目に長いセグメント
（1301.3px）も一部やや怪しい区間を含むが、明確な誤りとまでは
言えない境界事例——これはむしろレビューで人間が判断すべき適切な
対象と判断。max_gapは60を実用上の上限として維持することとした。
断片化（同一飛跡が複数セグメントに残ること）は直線Hough検出の
構造的限界であり、レビュー時は各セグメントをそのまま個別に判定
すればよい（同じ飛跡を複数回trueと判定しても正解データとしては
問題ない）。

## 2026-07-15 16:10 JST — max_gapを15→60にさらに拡大、レビュー順序を長さ降順→固定シードのシャッフルへ変更

ユーザー: 「houghでもっと長い飛跡も見つかるんじゃない？まだ途中で
切れている。また、今の降順だとほとんど綺麗なトラックに見えるよ」。
2つの指摘に対応。

**(1) 最長セグメントがまだ短い**: 同一E07タイルでmax_gapを
15/25/40/60でスイープ:
| max_gap | セグメント数 | 上位長さ(px) |
|---|---|---|
| 15 | 22,868 | 360, 297, 228, 212, 212 |
| 25 | 22,934 | **964**, 377, 367, 367, 296 |
| 40 | 18,782 | 964, 962, 759, 569, 562 |
| 60 | 13,769 | **1308**, 1301, 1077, 1072, 1025 |

mg=60の最長セグメント（1307.8px、旧デフォルトの約5倍）を目視
確認したところ、無関係な線への誤った橋渡しは無く、1本の実飛跡に
正しく沿っていた。`_HOUGH_MG`のデフォルトを15→60に変更。

**(2) 長さ降順だと簡単な陽性ばかりになる**: 降順ソートは
「セッションが途中で終わっても本物らしい候補を先に」という
意図だったが、副作用として上位が「ほぼ全部明らかに本物」に
偏り、ノイズ・境界事例の学習に必要な多様なサンプルが得られない
指摘を受けた。`tracks.sort(key=length, reverse=True)`を
`np.random.default_rng(0).shuffle(tracks)`（固定シード、
再開時も同じ順序を維持）に変更。真の飛跡/ゴミの比率を偏り無く
サンプルできるようにする狙い。

いずれも`module/server/labeling.py`。

## 2026-07-15 15:55 JST — Hough max_gapを3→15に拡大——セグメント自体が短すぎる問題に対応

ユーザー: 「クロップだけでなく、表示されているセグメントが短い」。
前エントリのクロップ拡大は周辺文脈の問題を解消したが、根本には
`cv2.HoughLinesP`のmaxLineGap=3pxが小さすぎ、1本の実飛跡が粒間の
隙間で多数の短いセグメントに分断されている問題がある——ユーザーは
同じ実飛跡を何度も判定させられていた可能性が高い。

同一E07タイルでmax_gapを振って上位セグメント長を比較:
| max_gap | 総セグメント数 | 上位長さ(px) |
|---|---|---|
| 3(旧) | 8,822 | 264, 226, 204, 202, 173 |
| 10 | 19,422 | 272, 235, 217, 210, 192 |
| 20 | 23,737 | 540, 373, 350, 342, 318 |

クロップ画像で目視確認したところ、max_gap=15でも別の無関係な線に
誤って橋渡しされることなく、単一の実飛跡に沿って正しく伸びている
ことを確認。`_HOUGH_MG`のデフォルトを3→15に変更
（`module/server/labeling.py`）。総セグメント数は増えるが
（8,822→22,868）、長さ降順ソートで上位から見せる設計のため実質
影響なし——むしろ上位（最初に見せる分）が実際に長い本物らしい
候補になった。

## 2026-07-15 15:45 JST — レビュークロップの余白不足を修正（短い断片が長い飛跡の一部に見えない問題）

ユーザー: 「本当はもっと長い飛跡の一部だけ切り取られているように
見える」。短いHoughセグメント（gapで分断された長い実飛跡の断片
である可能性がある）のクロップ半径が小さすぎ（旧: min_half=110px
=220pxウィンドウ）、周辺への繋がりが見えず孤立した断片に誤って
見えてしまう問題。

`_CROP_MARGIN_PX`70→100、`_CROP_MIN_HALF`110→200、
`_CROP_MAX_HALF`260→350に拡大（ウィンドウサイズはおよそ2倍）。
セグメント#2000（短い断片）で再確認したところ、拡大後は周囲に
本当に繋がる線が無い＝孤立ノイズと確信を持って判定できる見え方に
改善。セグメント#1（最長）も引き続き問題なし。

## 2026-07-15 15:35 JST — ラベリングUIをクリック選択方式から逐次レビュー方式（1件ずつtrue/false）へ変更

ユーザー: 「長い飛跡をめっちゃクリックするのがめんどくさい。
セグメントを順番に自動表示してほしい、で、それをtrue/falseだけ
ぽちぽち選択したい」。従来のcanvas上クリック方式は、2048×2048の
画像内で目的のセグメント（特に長い飛跡がgapで分断された多数の
短いセグメント群）を目視で探して正確にクリックする負担が大きい
問題があった。

`module/server/labeling.py`を再度改修（連結成分→Houghセグメントの
変更は維持、UIのみ刷新）:
- セグメントを長さ降順でソートし、1件ずつサーバー側でクロップ
  ＋ハイライト（クリムゾン色の太線、半透明合成）した画像を生成。
  クロップサイズはセグメント長+70pxマージン、110〜260pxの範囲に
  クランプ（短すぎず長すぎない一定の見やすさを確保）。
- フロントは「← Junk / Track →」の2ボタン＋キーボード
  （←/F=junk, →/J=track, Backspace=一つ前に戻ってやり直し）。
  判定するとその場で`/label_decide`にPOSTし自動的に次のセグメント
  へ進む。
- 保存形式を`kept_ids`（採用のみ記録）から`decisions`
  （`{segment_id: true|false}`、全件明示的に記録）に変更。
  ページ読み込み時に前回の続きから再開（未判定の最初のセグメント
  へジャンプ）。

**動作確認**（同じE07中央タイル、8,822セグメント、長さ降順）:
セグメント#1（最長264px）は明確に本物の飛跡として、#2000
（短い、孤立した塊）は判定しやすい大きさのクロップとして表示
されることを確認。decide→load往復も確認済み。

長さ降順にしたのは、セッションが長さの都合で途中で終わっても
「本物らしい候補」から先にレビューできるようにするため（短い
候補は統計的にノイズの可能性が高い）。

## 2026-07-15 15:20 JST — ラベリング単位を「連結成分」から「Houghセグメント」へ変更

ユーザー: 「ある1画像内に、大量の飛跡セグメントがあるはずで、
そのセグメントごとに飛跡かゴミかの判定が必要」。従来の連結成分
単位のラベリングでは、密な画像（特にE07、E373の約2倍の前景密度）
で複数飛跡・ノイズが1つの巨大な塊に融合してしまい、「塊の一部は
本物、一部はゴミ」を区別できない問題があった。

`module/server/labeling.py`を全面改修:
- ラベリング対象を連結成分から`module.pipeline.find_tracks`が返す
  `Track`（`cv2.HoughLinesP`検出セグメント、既存のノイズフィルタ・
  `/view/`の"trk"表示と同じ検出器）に変更。
- ラベルマップPNGは各セグメントを太さ7pxの線として描画し
  （クリックしやすくするため）、IDをRGBに符号化する方式は
  そのまま流用。
- Hough閾値をURLクエリ（`hough_thr`/`hough_ml`/`hough_mg`）で
  調整可能にし、デフォルトは`matlab_export`のデノイズフィルタと
  同じ寛容な設定（thr=8, min_line=10, max_gap=3）——「大量の
  候補セグメント（ノイズ含む）を出して選別する」というユーザーの
  要望に合わせた。
- 保存ペイロードにHoughパラメータと採用セグメントの座標
  （`kept_segments`）も含め、後から再現・変換しやすくした。

**動作確認**（E07中央タイル VX0022_VY0022, z=29）: 8,822個の
候補セグメントを検出。背景画像に重ねて目視確認したところ、
本物の明るい飛跡ラインには黄色セグメントが正しく重なる一方、
孤立ノイズ粒にも大量の短い偽セグメントが乗っており、狙い通り
「大量の候補から本物を選ぶ」形になっている。save/load往復も
確認済み。

## 2026-07-15 15:00 JST — KISO較正済みパラメータをE07中央タイルへそのまま適用、視覚的にKISO同等以上にノイズが乗ることを確認——E07独自の正解データが無いと先に進めない

ユーザーより再確認: 「KISOは重要な参考事象ではあるが、E07の画像を
用いて基本最適化を進めたい」。方針は前エントリと同じくE07が
本体、KISOは参考のみ。

E07の代表的な非エッジタイル（VX0022_VY0022、45×45グリッドの
ほぼ中央）で、KISO(E373)較正済みのグリッド+Hough整合デノイズ
パイプライン（cell=30px, hough_thr=8, min_line=10, max_gap=3,
area_floor=30）をそのまま適用（`results/matlab/
check_e07_current_params.py`、新規）:

- 生二値化: 前景密度8.96%、成分数3,586（KISOの4〜5%からほぼ倍増、
  前エントリの推測と整合）
- デノイズ後: 前景密度8.62%、成分数2,706——**わずか25%の成分
  しか除去できていない**（KISOでの保守的フィルタは約18.8%の
  点予算を削減していたのと比べ、割合としては同程度〜やや少ない
  印象だが、絶対密度が2倍高い分、残存ノイズの実数はKISOよりも
  遥かに多い）
- 400×400pxクロップを可視化（`/tmp/e07_current_params_check.png`）
  したところ、シアンの点はKISO版パネル04と同様、背景ノイズ粒に
  かなり乗っており、視覚的にKISOと同等以上にノイズが目立つ

**結論**: KISO較正パラメータはE07にそのまま転用できない
（想定通り）。ただし今のE07タイルには正解ラベルが無いため、
「具体的にどのパラメータをどう変えるべきか」を定量的に決める
手段が無い——これが手動ラベリングツール（前々エントリ）の
出番。当面、追加のパラメータ調整は憶測になるため保留し、
ユーザーによるE07タイルへの手動ラベリングを待ってから、
それを正解データとして再検証・再調整する方針とする。

## 2026-07-15 14:30 JST — 本物のE07全面探査データをマウント。KISO(E373)との背景密度差を実測（約2倍）——今後の最適化対象はE07に切り替え

ユーザーより明確な訂正: 「KISOはE373乾板なので背景レベルが
E07と異なる。E07の全面探査が目的なので、最適化はE07に対して
行う」。これまでの `specials_x20/KISO` を使った全ての作業
（グリッド/スケルトンexport、ノイズフィルタ、合成データ生成、
手動ラベリングツール）はE373の代表データであり、本来最適化
すべきE07とは背景レベルが異なる可能性がある注意点として記録
（`specials_x20`のimage.jsonのDateTimeが2022年であることから
E07全面探査キャンペーンより前のデータと推測、specials_x20全体が
E373の可能性）。

**KEKのE07全面探査データをマウント**: 会話冒頭で試みたsshfs
マウントを再実行。`/group`直下へのマウントはsudoでの
mkdir/chownが自動許可の対象外でブロックされたため、ユーザーの
指示でホーム配下に変更:
```
sshfs hayashu@login.cc.kek.jp:/group/had/sks/E07/tohoku/fullscan \
  ~/mnt/e07-fullscan -o reconnect,ro
ln -sfn ~/mnt/e07-fullscan e07-fullscan/fullscan-image
```
（ユーザー要望によりread-onlyで再マウント）。マウント先には
`E07/`と`E373/`が並んで存在し、E07側は
`E07/MOD108/PL12/tohoku-v1/AREA00/IMAGE00_AREA00/`配下に
specials_x20と同じ.json+.spng形式のタイルが2025枚（4050
ファイル）確認できた。`module.reader.load_spng`でそのまま読める
ことも確認済み。

**背景密度の実測比較**（fog_remove→otsu_binarize→remove_noise、
同一スライス番号で比較）:
| スライス | KISO(E373) fg_frac | E07(MOD108/PL12) fg_frac |
|---|---|---|
| z=10 | 4.55% | 7.12% |
| z=20 | 4.23% | 8.76% |
| z=30 | 4.77% | 9.13% |

E07の前景密度はKISOのおよそ1.8〜2倍。これまでKISOで較正した
パラメータ（グリッドセル30px、ノイズ面積フロア、Hough閾値、
合成データのGRAIN_SPACING_PX=9.0等）は、密度が2倍近く違う
E07では最適でない可能性が高い。

**現状の課題**: 実E07タイルには`tests/specials_gt.json`の
ような既知vertex正解が無い。今後の検証は、(1) 手動ラベリング
ツール（前エントリ参照）でE07タイルに対して直接正解データを
作る、(2) KISOでの結果は参考程度に留め、E07で独立に再検証、の
方針で進める。

TODO: 合成データ生成（`e07-binary-segmentation/src/
make_synthetic_data.py`）とWebビューアのラベリングデフォルト
対象を、KISOから実E07タイル（`fullscan-image/E07/MOD108/PL12/
tohoku-v1/AREA00/IMAGE00_AREA00/`）に切り替える。ノイズフィルタ
パラメータもE07の密度で再検証が必要。

## 2026-07-15 14:00 JST — Webビューアに手動ラベリングツールを追加（実データの正解ラベル作成用）

ユーザー提案（"私がウェブでぽちぽち選択して本当に残したい飛跡か
どうか（教師）を作ることもできます"）に対応。`module/server/
labeling.py`（新規）を追加し、`module/server/app.py`の
`create_app()`に`register_labeling_routes()`で配線。

**設計**: ポジティブオンリー方式——連結成分はデフォルト「除外」、
クリックで「採用（本物の飛跡）」にトグル。密なノイズ画像では
「ノイズを1つずつ除外」より「本物の飛跡だけクリック」の方が
圧倒的にクリック数が少なくて済むため。

**仕組み**: サーバー側で対象スライスを fog除去→Otsu→remove_noise
まで通常通り処理し、`cv2.connectedComponents`で連結成分IDを
振る。IDをRGB各チャンネルにビット分解してPNG化した「ラベルマップ」
画像をクライアントに送る。ブラウザ側はこのPNGを一度
`getImageData`で読み込めば、クリック位置の画素色→ID変換が
純粋にクライアント側で完結し、成分ごとのジオメトリをサーバーから
送る必要がない。背景表示はパーセンタイルベース(2/99.5%tile)の
コントラスト伸長（単純min-max正規化だと2048×2048全体では外れ値に
潰されて真っ黒になることが判明、修正済み）。

**エンドポイント**: `/label/<path>/<idx>`（ラベリングページ）、
`/label_bg/...`（背景PNG）、`/label_map/...`（ID符号化PNG）、
`/label_save/...`（POST、`results/manual_labels/`へJSON保存、
gitignore対象）、`/label_load/...`（既存ラベルの再読み込み）。
サイドバーに「LABEL TRACKS (manual)」ボタンを追加（現在選択中の
タイル・スライスで開く）。

**動作確認**: 全ルートがHTTP 200を返すこと、save/loadの往復、
背景PNGのコントラストを確認済み。実際のクリック操作（canvas
JS）はブラウザでの対話が必要なため、ユーザー側での動作確認待ち。

ビューアサーバーを起動済み（`http://127.0.0.1:8123/view/
specials_x20/KISO/image.json`、ポート8123）。

TODO: 保存されたラベルJSON（kept_ids + 前処理パラメータ）を
実際の(img, mask)学習ペアに変換するスクリプトをe07-binary-
segmentation側に用意する（ラベルが貯まってから着手）。

## 2026-07-15 13:40 JST — 学習ベースの新規リポジトリ e07-binary-segmentation を作成（古典手法5つ全滅を受けた次の一手）

古典的ノイズフィルタが全滅したため（前エントリ参照）、学習ベースの
track/fog二値分割を試す新規リポジトリ `~/work/e07/
e07-binary-segmentation` を作成。RIKEN/Kasagi氏の非公開コード
`~/work/e07/binary_segmentation`（segmentation_models_pytorch,
Unet+DiceLoss）を参考に（コードはコピーせず再実装、ライセンス
未許諾のため）、`results/matlab/proto_synth_data.py`の合成データ
生成を600タイル規模に拡張して学習。

初回学習はheld-out合成データでprecision 85.7%と好調だったが、
実データ（KISO vertex crop）では予測画素がわずか36px
（古典手法20,303px）というほぼ完全な汎化失敗——原因は合成データ
生成時に背景として選んだcrop領域に既存の実飛跡が写り込んでいても
チェックしておらず、「本物の飛跡＝背景」という矛盾ラベルを大量に
学習させていたバグ。修正（背景crop領域のOtsu前景率が閾値を
超えたら再サンプリング）後、実データでも予測7,448px
（古典手法20,303px）と非ゼロ・視覚的にトラックラインに沿った検出
ができるようになった。定量評価はまだ（実データに画素正解ラベルが
無いため）。

詳細・TODOは `e07-binary-segmentation/analysis-note.md` 参照。
ユーザーからWebビューア（`module/server/app.py`）でのクリック
選別による実データラベル作成の提案あり、次のステップ候補。

## 2026-07-14 02:20 JST — Otsu閾値スイープとHough整合の「大成分への拡張」を検証、いずれも不発——古典手法5つ全滅が確定

ユーザーの「生画像を見てもゴミがきれいに落ちていない」という指摘を
受け、2つ追加検証。

**(1) `results/matlab/sweep_threshold_ground_truth.py`（新規）**:
合成正解データでOtsu二値化の閾値そのものをスイープ。基準閾値
（10.0）から+10動かしただけで、本物の飛跡画素の再現率が
97.8%→2.4%に崩壊。単一画素の輝度ではこれ以上閾値を絞る余地が
ほぼゼロと判明——本物の飛跡粒とバックグラウンドフォグ粒は輝度が
統計的にほぼ区別できないところまで、既存のOtsu閾値が既に最適化
されている。

**(2) 大きい連結成分（cellサイズ超）への「Hough復活」検証**:
現状 `remove_unaligned_noise` は小成分（≤cell）にしかHough整合
フィルタをかけておらず、大成分は無条件でスケルトン化される。
KISO実データのvertex crop（400×400px）で内訳を見ると、大成分は
20個中、最大のもの（area=3374, 193×400px）が既知の長い実飛跡と
推定される一方、他の多くは正体不明。合成データ全体では大成分236個
中、真の信号はわずか4個（他232個は無関係な背景フォグが連結して
偶然「大きい」形になっただけ）——ここが現状完全にノーガードだと
判明。

2つの拡張案を試したが、両方とも不発:
- `results/matlab/eval_large_component_linearity.py`（新規）:
  各大成分のスケルトンにPCAで直線をフィットし偏差を測定。
  **信号（4個）のほうがノイズよりむしろ偏差が大きい**（最大偏差
  中央値: 信号13.8px vs ノイズ8.5px）——vertex/分岐点付近の信号は
  定義上まっすぐではないため、直線性チェックは見たい構造そのものを
  弾いてしまう。全閾値でprecision 0〜1.8%、改善なし。
- `results/matlab/eval_large_component_hough.py`（新規）: 大成分の
  スケルトン画素ごとにHough整合率を測定。信号・ノイズとも整合率
  中央値93〜97%とほぼ同じ——Houghは単なる直線検出器なので、大きい
  （＝伸びた）成分ならノイズの塊でも偶然直線的な部分を持ちやすく、
  判別力ゼロ。precision 1.7〜2.7%（フィルタ無しと同等）。

**結論**: elongation・isolation・小成分Hough整合・二値化閾値・
大成分への直線性/Hough拡張——試した古典的識別子は合計5つとも
頭打ちを確認。「もう一つ良い古典特徴を探す」路線はここで一区切り。
合成正解データ（既知マスク付き）を学習データとして使う小規模な
学習モデルへ進むことをユーザーに提案し、回答待ち。学習環境は
（ユーザー確認済み）まずMac上でCPU/MPSの小規模プロトタイプから。

## 2026-07-14 01:45 JST — 合成データの正解マスクでノイズフィルタを初めて定量評価。精度は2〜23%止まり——グリッド法・スケルトン直接出力とも、現行の古典幾何フィルタでは通用しないと確定

ユーザーが規定した「04に、グリッドではなくスケルトン化した画像を
見せて」に応え、`panel04_skeleton_only.py`
（scratchpad）でKISO vertex crop領域のスケルトン（デノイズ後の
二値画像を`skimage.morphology.skeletonize`、グリッド分割なしで
そのまま重畳）を可視化。バイナリマスク上に重ねた版
（`panel04_skeleton_on_binary.png`）で判明した所見: **本物の
飛跡（中央の長い直線群）はスケルトンが正しく中心線をなぞる一方、
盤面全体に無数の小さなノイズ塊が同じようにスケルトン化され、
くねくねした短い枝・ループとして散乱している**。スケルトン化は
「太さ→中心線」の変換はするが、そもそも小成分レベルで残っている
ノイズは一切除去しない——グリッド法特有の問題ではなく、現行の
`remove_unaligned_noise`フィルタ自体の残存ノイズが露呈した形。

これを受け、`results/matlab/eval_filter_ground_truth.py`
（新規）で、初めて正解ラベル付きデータでの定量評価を実施。
`proto_synth_data.py`が生成した合成データ（実背景+実grainパッチを
貼り付けた3本の合成飛跡、既知マスク付き）を使い、連結成分ごとに
「合成飛跡マスクと重なるか＝真の信号」「重ならない＝実背景の
フォグ＝真のノイズ」で正解ラベルを付与し、Hough整合・面積フロア・
現行の組み合わせフィルタの適合率(precision)・再現率(recall)を測定。

**結果（画像全体、1267小成分中 signal=31 / noise=1236）**:
| フィルタ | kept | precision | recall |
|---|---|---|---|
| フィルタ無し | 1267 | 2.4% | 100.0% |
| Hough整合のみ | 778 | 3.1% | 77.4% |
| 面積フロアのみ | 887 | 1.6% | 45.2% |
| 現行（整合 OR 面積フロア） | 949 | 2.5% | 77.4% |
| 整合 AND 面積フロア | 716 | 2.0% | 45.2% |

**合成vertex近傍のみ（r<300px、MATLABの1領域スケールに近い、
158小成分中 signal=31 / noise=127）でも**:
| フィルタ | kept | precision | recall |
|---|---|---|---|
| フィルタ無し | 158 | 19.6% | 100.0% |
| Hough整合のみ | 105 | 22.9% | 77.4% |
| 現行（整合 OR 面積フロア） | 119 | 20.2% | 77.4% |

局所領域でも最良のprecisionは23%止まり——本物1点につきノイズ
3〜4点が混入する計算。これまでの「aligned成分は面積中央値が
114pxとunaligned成分の20pxより大きい」といった間接的な傾向値では
実際の識別力を裏付けられていなかったことが、正解データで初めて
明らかになった。

**結論（ユーザーの指摘と一致）**: グリッド法・スケルトン直接出力の
どちらを選んでも、現行の古典幾何フィルタ（elongation, isolation,
Hough整合、面積フロア——いずれも単一スライス内の2D特徴）では
乳剤バックグラウンドフォグを実用的な精度で分離できない。3つの
幾何学的識別子を試して3つとも同程度の性能で頭打ちになったことから、
「もっと良い閾値・特徴を1つ探す」路線は限界に近いと判断。

z方向（隣接スライス）の連続性も`analyze_z_continuity.py`で試したが、
等方的な近傍探索半径を小さくすると急峻な飛跡（dip角30°以上）を
誤って「不連続」と判定し、半径を大きくすると（実飛跡が動きうる
最大10-18px相当）isolation距離と同じ理由（画像自体が密で何にでも
近い）で識別力が崩壊——この軸も単純な等方探索では崩壊することを
確認、深追いはしていない。

TODO: 古典幾何フィルタは事実上手を尽くした。次の現実的な一手は
既存の合成データ（正解マスク付き）を学習データとして使い、
小規模なsegmentation_models_pytorch U-Netを訓練し、単一スライスの
幾何ではなく局所テクスチャ/文脈から信号・ノイズを分離する方向へ
本格的に舵を切ること。ユーザーへ提案済み、応答待ち。

## 2026-07-14 01:05 JST — サマリーArtifactの画像品質問題を修正（パネル04が古い画像だった件+cv2描画のジャギー）

ユーザーから「04のグリッド化後の黄色の点群は全くだめ、スケルトン化後の
シアンが一番綺麗」との指摘。原因を2つ特定した。

**原因1（本質的）**: `kiso_cent_vertex_crop.datauri` の生成時刻が
2026-07-12 14:37で、スケルトン化コミット（`cca63da`、17:25頃）にも
Hough整合ノイズ除去コミット（`da3d5a8`、19:06頃）にも先行していた。
つまりArtifactのパネル04は、素のグリッド分割（連結成分もスケルトンも
デノイズも無し）という、既に置き換え済みの最も古い段階の画像を
ずっと表示し続けていた。

**原因2（表現手法）**: 既存の可視化は`cv2.drawMarker`等によるraster
描画で、アンチエイリアスが無くジャギーが目立つ。ユーザーが「一番
綺麗」と評したスケルトン画像は元々matplotlibで描いていたため、
見た目の差が生まれていた。

**対応**: `results/matlab/regen_panels.py`（新規、gitignore対象の
results/配下）を作成し、以下3枚を現行パイプライン
（`weighted_grid_hits` + `remove_unaligned_noise`、cell=30px）の
実データからmatplotlibで再生成:
- パネル04 (`kiso_cent_vertex_crop`): シアン`#5fd0c4`のリング+中心点、
  スケルトン画像と同一配色に統一。KISO vertex crop内173点。
- フィルタ確認図 (`filter_vertex_vis`): green=aligned kept, red=
  removed(conservative), orange=area floorで保護、をcv2マーカーから
  matplotlib散布図に置き換え。
- detectbunki分岐グループ図 (`vertex_groups_overlay`): Group1（橙）
  Group5（黄）の実ポリライン座標を`vertex_groups_export.mat`から
  matplotlibで描画、既知vertexを白十字で重畳。

Artifact 2件を同一URLで再公開（`kiso_vertex_pipeline_qa` /
`e07_summary.html`）。画像は生成し直したがコードには変更なし——
根本原因は「画像の作り直し漏れ」であり、`weighted_grid_hits`等の
ロジック自体は健全。

**教訓**: 中間生成物（datauri画像）の鮮度をコミットタイムスタンプと
突き合わせずにArtifactへ使い回すと、パイプラインを改善しても古い
画像が残り続ける。今後は可視化を差し替えた際、参照している全
Artifactのパネルを棚卸しして再生成漏れがないか確認する。

TODO: 直接のスケルトン点群（間引き無し）をMATLAB入力にする案は
計算コスト面で不可（既出の見積り、~150万点/タイル・detectlsegで
505日相当）のため見送り、現行のgrid+skeleton+denoise（cell=30px）
路線を継続。次は他の既知イベント（IBUKI, IRRAWADY, NAGARA）での
再現性確認。

## 2026-07-12 21:58 JST — Group1/5の折れ線を可視化——きれいな収束ではなく「密集した塊」、スキャナー選別向け候補として妥当

前エントリのGroup1（21本）・Group5（10本、既知vertexから14.5px）の
実際のポリライン座標を、チェックポイントから高速再実行
（`export_vertex_groups.m`、detectlsegはスキップ、10秒未満）して
取得し、既知vertex周辺の画像に重ねて可視化した。

**正直な所見**: 教科書的な「3本のトラックがきれいに1点収束」という
絵ではない。特にGroup1（橙）は多数の短い折れ線が複雑に交差した
「密集した塊」。Group5（黄）は vertex（白十字）から複数方向に
線が伸びており、既知のトラック構造とある程度整合するが、これも
単純ではない。

**この結果をどう位置づけるか**: これはむしろ、ユーザーが当初
設計した「MATLABの出力は反応点候補としてスキャナーによる人選別へ
進む」というワークフローに合致する。アルゴリズムだけで完璧に
1点へ解決するのではなく、**人間が見て判断すべき「候補」**として
妥当な形——過大評価も過小評価もせず、この段階の成果物として
適切と判断。Artifactに追加（同一URL）:
https://claude.ai/code/artifact/8f9a90a2-7186-41e2-992c-3e80fd078241

**現状の到達点**: 1つの局所領域（5×5、既知vertexを含む）での
単発成功であり、タイル全体・他イベントでの再現性は未検証。次の
自然なステップはこのテストを他のspecials_x20イベント（IBUKI,
IRRAWADY, NAGARA等、いずれもn_clicks≥2で複数トラック収束が
期待できる）や、より広い領域規模で再実行し、一般性を確認すること。

## 2026-07-12 21:52 JST — MATLAB修正が成功。初のフルパイプライン完走（detectlseg→integrate_smallregions→detectbunki）、既知vertex近傍で有望な分岐候補を検出

`resamplingpoly` への防御的ガード適用後、同じ既知vertex周辺5×5局所
テストを完全に再実行。**今回の一連の調査で初めて、MATLABの
フルパイプラインが実データで最後まで完走した**。

**実測結果**:

| 段階 | 結果 |
|---|---|
| detectlseg_smallregion | 3,263.5秒、4,840セグメント（前回と同一データにつき想定通り一致） |
| integrate_smallregions | **クラッシュせず完走**、8.0秒、2,381折れ線 |
| detectbunki | 完走、2.0秒、1,815分岐グループ（3本以上の折れ線を持つもの107グループ） |

**既知vertex（KISO, vx=1096/vy=1028/z_slice=10）近傍80px以内に
位置する分岐グループ**（テストスクリプトの表示は3本以上の折れ線を
持つグループに限定、107件中）:

| グループ | 折れ線数 | vertexまでの距離 |
|---|---|---|
| Group 1 | **21本** | 34.8px |
| Group 5 | 10本 | **14.5px** |
| Group 12 | 7本 | 29.6px |
| Group 31 | 5本 | 20.8px |
| Group 41 | 4本 | 44.0px |
| Group 98 | 3本 | 14.5px |

特にGroup 1（21本収束）・Group 5（14.5px）は、`specials_gt.json` の
クリック精度（±50-100px）・テスト許容誤差（±200px）の範囲内に
十分収まる。これは前段処理（グリッド化+スケルトン化+Houghノイズ
フィルタ）とMATLAB側の最小限の防御的修正の組み合わせで、
**既知のΛΛ超核vertex候補が実際に検出できる**ことを示す最初の
具体的な証拠。

**注意点（過大評価しないために）**:
- 1つの局所領域（5×5、既知vertexを含む）でのテストであり、全タイル
  での再現性は未検証
- Group 1/5がKISOの既知vertexに実際に対応する3本の本物のトラック
  由来かは未確認（折れ線の実座標・方向を目視確認する必要がある）
- 「107グループ中3本以上」という表示条件は自分で設定した閾値であり、
  1,815グループ全体を見ているわけではない（サイレントな見落としを
  避けるため明記）

**次のTODO**: Group 1/5の実際の折れ線ジオメトリを可視化し、KISOの
既知3トラックと整合するか確認。問題なければ全256領域規模での
再現性確認へ進む。

## 2026-07-12 20:57 JST — MATLAB側に最小限の防御的修正を適用（ユーザー承認、選択肢1）。実測検証を再実行中

前エントリの分岐点で、ユーザーが選択肢1（`pixellist2poly` への
最小限の防御的ガード）を選択。実装にあたり詳しく調査した結果、
**当初想定していた修正箇所は不十分と判明**し、より適切な場所に
変更した。

**調査で判明したこと**: クラッシュは `resamplingpoly`
（`integrate_smallregions.m` 91行目から呼ばれる内部関数）が、
折れ線候補の近傍（距離<TH=2）に実データ点が1つも無いまま
`pixellist2poly(x1,...)` を空の `x1` で呼んでしまうことが原因。
`pixellist2poly` 自体は他のどこからも呼ばれていない
（`grep -rn "pixellist2poly("` で1箇所のみ確認）。

**当初案（`pixellist2poly` 内で空データなら `poly=zeros(0,Dim)` 等を
早期returnする）を試作したが、机上検証で不十分と判明**: その戻り値を
受け取る呼び出し元 `integrate_smallregions.m` 107-110行目に
`lseg(:,:,i) = polylines{i}([1 end],:)` という**無条件の**（長さ0
チェックより前の）インデックスアクセスがあり、`polylines{i}` が
0行になっているとそこで同種のクラッシュが単に移動するだけと判明。
この案は実装後に破棄した（`pixellist2poly.m` は変更せず元に復元
済み、`diff` で完全一致を確認）。

**実際に適用した修正**: `integrate_smallregions.m` の
`resamplingpoly` 内、`pixellist2poly` 呼び出し直前に
`if isempty(x1)` ガードを追加。空の場合は `pixellist2poly` を
呼ばず、**`polylines{i}` の元の候補座標データはそのまま残し**
（後続の無条件インデックスアクセスに対して安全）、`lpoly2(i) = 0`
だけ設定して次のループへ。既存コード（`integrate_smallregions.m`
118行目 `if lpoly(i)==0 % 長さ0の線分は飛ばす continue`）が
**元々この「長さ0のポリラインはスキップする」という規約を想定
済み**だったため、この修正は新しい規約を持ち込むのではなく、
既存の設計意図が正しく機能するようにするものである。通常時
（`x1` が空でない場合）の挙動・出力は一切変更していない。

**安全対策**: `pixellist2poly.m` / `integrate_smallregions.m` とも
編集前に `.orig-20260712` サフィックスでバックアップを作成
（`~/work/e07/matlab/` はgit管理外のため）。MATLAB `checkcode` で
構文チェック済み（既存のスタイル警告のみ、致命的エラーなし）。

同じ既知vertex周辺5×5局所テストを再実行中（バックグラウンド、
detectlsegから再実行のため約50-90分見込み）。今回は `lseg` を
中間チェックポイントとして保存するようテストスクリプトも改良し、
今後の再テストを高速化できるようにした。

## 2026-07-12 19:57 JST — denoise版5×5局所テスト完走: detectlsegは大幅高速化したが、integrate_smallregionsのクラッシュは解消せず

前エントリのdenoise版局所テストが完走。結果はまだら:

**detectlseg_smallregion は大きく改善**（全25領域、実測）:

| 指標 | skeleton-only（旧） | +denoise（新） | 差分 |
|---|---|---|---|
| 総処理時間 | 5,153.3s | 3,240.6s | **-37.1%** |
| 検出セグメント数 | 5,768 | 4,840 | -16.1% |

点数削減率（約-18%）よりセグメント数の削減率（-16%）は近いが、
処理時間の削減率（-37%）はさらに大きい——プロファイルで判明した
「コストはNよりMに強く効く」という構造と整合的で、ノイズ点の除去が
無意味な短いセグメント生成を効率よく減らしている、という仮説を
支持する結果。

**しかし `integrate_smallregions` は前回と全く同一のエラーで
再クラッシュ**（`pixellist2poly>subfunc2` 行136、空の点群への
インデックスアクセス）。ノイズ削減・スケルトン化という前段側の
改善だけでは、このクラッシュの根本原因を解消できないことが実証
された——**「前段処理だけで直せる」という当初の想定は誤りだった**
と結論。この特定のクラッシュは `resamplingpoly`/`pixellist2poly` 側
の、断片化した線分から実体のない折れ線候補が生成されうる、という
再現性のあるロジック上の欠陥である可能性が高い。データの量や質を
どれだけ改善しても、この特定の分岐に入る限り再現する。

**判断が必要な分岐点**: 前段（Python）側の改善はここでほぼ限界。
選択肢は (a) `pixellist2poly` に最小限の防御的ガード
（空の点群なら早期return、等）をMATLAB側に1箇所だけ加える、
(b) `integrate_smallregions`/`detectbunki` を今は使わず、
`detectlseg_smallregion` の出力（線分候補）までを成果物として
スキャナー選別に回す設計に変更する、(c) 他の対応、をユーザーと
相談して決定する。

## 2026-07-12 19:06 JST — Houghノイズフィルタをmodule実装・実測開始（点数減以上に処理時間減）、合成データ較正完了、micro-samも却下

**(1) 保守版Houghフィルタを `module/matlab_export.py` に正式実装**
（`remove_unaligned_noise()`、`export_hits_grid(..., denoise=True)` が
デフォルトで適用、CLIに `--no-denoise` を追加）。KISO再エクスポート
（denoise版）で108,671ヒット（旧133,183から24,512削減=18.4%、
試算通り）。

前回クラッシュした既知vertex周辺5×5局所テスト
（`test_detectbunki_local.m`）をdenoise版データで再実行開始
（バックグラウンド、~86分見込み）。**序盤2領域の結果が有望**:

| 領域 | 点数（旧→新） | セグメント数（旧→新） | 時間（旧→新） |
|---|---|---|---|
| row7/col7 | 930→732 (-21%) | 232→175 (-25%) | 165.9s→68.1s (**-59%**) |
| row7/col8 | (未計測) | (未計測) | 75.3s |

点数の削減率（-21%）より処理時間の削減率（-59%）の方がはるかに
大きい——プロファイルで判明した「コストはセグメント数Mに比例」
という仮説と整合し、**ノイズ点の除去がMを不釣り合いに大きく
減らしている**可能性を示唆。`integrate_smallregions` のクラッシュが
解消するかは全25領域完了後に判明（TODO、継続監視中）。

**(2) 合成データのグレイン間隔を実測で較正**: 参照トラック
（882px、既出）の輝度プロファイルにピーク検出（`find_peaks`）を
適用し、110個のグレイン様ピークから間隔を実測——中央値9.00px、
平均9.49px。プロトタイプで使っていた仮の値8.0pxとほぼ一致し、
妥当だったことを確認。この値で正式採用し、さらに各トラックに
軽微な角度ドリフト（多重散乱を模した緩い湾曲、curvature=1.5°/
グレイン）を追加——直線すぎる不自然さを解消。Artifactの合成データ
比較画像を更新。

**(3) `micro-sam` を調査（エージェント委任、完了）。UCSに続きこちらも
非推奨と判断**。理由はUCSと異なる: 重み自体はZenodoで公開・
ダウンロード確認済み（vit_b_lm.pth ≈375MB、MITライセンス、Apple
Silicon MPS対応も `micro_sam/util.py` に明記）だが、**タスク設計が
根本的にミスマッチ**。LIVECell/TissueNet/DeepBacs等、離散オブジェクト
（細胞・核・オルガネラ）のインスタンス分割用に訓練されており、
連結した線状構造（トラック）の密な二値分割という私たちの課題とは
性質が異なる。AIS（Automatic Instance Segmentation）デコーダの
foregroundチャンネルを流用すれば技術的には密マスクが作れるが、
線状構造での実績はなく、SAMエンコーダの計算コストに見合わない。

**エージェントの推奨（UCS・micro-sam両方の調査から収束）**: 大型の
汎用基盤モデルを探すより、**Kasagi氏も使っていた
`segmentation_models_pytorch`（公式ライブラリ、理研フォークではない）
でImageNet事前学習エンコーダから軽量U-Netを自前データで学習する**
方が現実的。これは系統(2)の合成データ生成と自然に合流する——
次のステップとして具体化する。

**次のTODO**:
- denoise版5×5局所テストの完走を待ち、セグメント数・クラッシュ有無・
  vertex検出を確認
- 合成データセットを一定規模（数百〜数千枚）生成し、
  `segmentation_models_pytorch` U-Netの学習を試作
- 生成した合成データ+実データ古典パイプライン出力（弱ラベル）を
  組み合わせた学習データセット設計を検討

Artifact更新: https://claude.ai/code/artifact/8f9a90a2-7186-41e2-992c-3e80fd078241

## 2026-07-12 18:49 JST — 3系統並行調査の第1ラウンド完了（Hough フィルタ実証・合成データprototype動作確認・UCS却下＋micro-sam発見）

前エントリ（3系統並行開始）の続報。

**(1) Hough整合性フィルタ、vertex近傍で可視化検証**: 保守版
（非整合 AND 面積<30px）を既知vertex周辺400×400pxで可視化。緑=整合
（保持）、赤=非整合+小=保守版でも削除、橙=非整合だが面積≥30px=
積極版のみ削除。**橙のマーカーが複数、目視できるトラック線上に
乗っている**ことを確認——単純な「非整合なら削除」（積極版、28.6%
削減）は実信号を削る危険があり、保守版（18.8%削減）の方が安全という
判断を裏付けた。detectlsegでの実測再検証は未実施（TODO）。

**(2) 合成データ: コピー&ペースト方式のプロトタイプが動作確認**。
GAN等のドメイン変換を使わず、実データの孤立小ブロブ（面積8-15px、
単独グレインらしいもの）をパッチとして500個収穫し、実背景
（KISO slice30）上に生成した直線パス（3本、(900,900)で収束するよう
配置、間隔8px+ジッター）に沿って貼り付け。背景・グレイン質感とも
100%実データなので、GANによるスタイル変換という最も重い工程を
まるごと回避できる。視覚的に自然な仕上がりを確認（Artifact参照）。

未較正・簡略化した点（今後の課題）: グレイン間隔8pxは実測値からの
粗い見積もり（正確な較正が必要）、トラックは直線のみ（実際の
多重散乱による湾曲は未モデル化）、背景に実際の（ラベルなしの）
本物のトラックが写り込んでいる可能性がある（学習に使う場合ノイズ
ラベルになる、要対策）。

**(3) 公開モデル調査（エージェントに委任、完了）**: **UCS（SAMベース
汎用曲線構造セグメンテーション）は非推奨と判断**。理由: (a)
ファインチューン済みadapter重みが未公開——GitHub Issue #1
（2025-12-23、HuggingFace中の人からの重み公開依頼）が7ヶ月間
未対応のまま、(b) LICENSEファイルが存在せず法的にクリーンでない、
(c) train.py/test.pyのチェックポイントパスが著者個人のサーバパスの
まま。ベースSAM本体（Meta公式、ViT-B/L/H）は生存確認済みだが、
公開adapter重みが無い以上「軽くfine-tuneするだけ」というシナリオは
成立しない。

**代替候補として `micro-sam`
（github.com/computational-cell-analytics/micro-sam、Nature Methods
2024）を発見**。顕微鏡画像専用のSAM派生で、BioImage.IO/HuggingFace
に学習済み重みを複数公開、小規模データでのfine-tuningチュートリアル
も整備済み。エマルジョン画像も広義の顕微鏡画像であり、UCSより
遥かに現実的な出発点。次点候補 `vesselFM`（3D血管、CVPR25、
Open RAIL++-M非商用ライセンス）も参考記録。

**次のTODO**:
- Hough保守版フィルタを実際にKISOへ適用し、detectlsegの実行時間・
  セグメント数への影響を測定（本命はMの削減——ノイズ塊が無意味な
  短いセグメントとして検出されている可能性）
- 合成データのグレイン間隔・背景選定を精緻化
- `micro-sam` の実際のセットアップ・チュートリアル調査、
  エマルジョン画像での小規模fine-tuning実現性の検証

Artifact更新（同一URL、新セクション追加）:
https://claude.ai/code/artifact/8f9a90a2-7186-41e2-992c-3e80fd078241

## 2026-07-12 18:44 JST — 長さ方向の間引きは省略不可と確認、ノイズ削減の3系統調査（古典/合成データ/公開モデル）を並行開始

**細線化のみ・長さ方向の間引きなし、は試したが不可**（ユーザー提案の
検証）: KISO全体で164万点、最大領域22,069点、detectlseg見積もり
**約505日**——生ピクセルほどではないが非現実的。セルサイズを段階的に
細かくしても（10px:136h, 15px:36h, 20px:14h, 30px:5.4h）、現行の30pxが
実測2.52-5.5hに近い妥当な落とし所であることを再確認。太さはスケルトンで
解決済み、長さ方向の間引きは detectlseg のアルゴリズムコストを抑える
ために必須、という結論。

**MATLAB高速化の検証は撤回**: `pdist(x,@distfun1)` を組み込み
`pdist(...,'euclidean')` に置換するベンチマークで25-55倍速いことを
確認したが、実際にプロファイルを取ると `subfunc1`（pdist含む）は
全体の**0.4%**（266.5秒中1.13秒）に過ぎず、支配的なのは `subfunc2` の
端点反復最適化ループ（99.6%、`lseg2L` が1領域で36,252回呼ばれる）。
このコストは点数Nよりも**検出されるセグメント数M**に比例する構造。
MATLABコードは一切変更していない（ベンチマークのみ、適用せず）。

**`integrate_smallregions` の実クラッシュを確認**: 既知vertex周辺5×5
領域（25領域、実測5,153秒）で `lseg`→`integrate_smallregions` を実行
したところ、`pixellist2poly>subfunc2` で「空の点群への添字アクセス」
エラーでクラッシュ。断片化した線分から実体のない折れ線候補が生成される
エッジケースで、シミュレーションデータでは想定されていなかったと推測。

**トラック幅の実測**: `isaline6`(TH=2)/`subfunc2`(TH=1.5) の直線判定
許容誤差に対し、実トラックの局所（40px窓）垂直方向半幅は実測6-8px
（3-4倍超過）。これが過剰断片化・クラッシュの根本原因と特定。

**対策: スケルトン化**（`skimage.morphology.skeletonize` を
`requirements.txt` に追加）。30pxを超える connected component を
セル分割前に1px幅の中心線に細線化。位置はスケルトン加重重心、
`n`（密度指標）は元ブロブのセル内ピクセル数を維持。ヒット総数不変
（133,183件）。同一トラックで局所半幅が 8.12/6.63/2.24px →
2.70/2.51/1.56px に改善（約2.6-3倍）。可視化2枚（全長概観+6倍拡大
詳細、青=元ブロブ輪郭/シアン=スケルトン/黄十字=最終点）を作成し
Artifactに追記: https://claude.ai/code/artifact/8f9a90a2-7186-41e2-992c-3e80fd078241

**「前段でトラック検出をしているのでは」への整理**: スケルトン化は
画像レベルの形状クリーンアップ（fog除去・Otsu・ノイズ除去の延長）で
あり、物理的なトラック/vertex識別（MATLAB側の役割）を肩代わりする
ものではない、という整理でユーザー合意。

**ノイズ削減の探索（3手法試行、うち2つは不発）**:
1. 伸長度（形状）による判別 → **不発**。面積との相関ほぼゼロ(-0.09)、
   丸い孤立塊が「ノイズ」か「単独グレイン」か形だけでは判別不可
2. 孤立度（最近傍構造までの距離）による判別 → **不発**。中央値21px
   以内に何かしらの構造があり、乳剤画像自体が密なため分離力なし
3. **Hough整合性による判別 → 有望**。per-slice `cv2.HoughLinesP`
   （thr=8,ml=10,mg=3）で検出した直線群からの垂直距離<3pxで判定。
   整合する塊の面積中央値114px、しない塊は20px——明確な分離。
   小塊（≤30px）のうち43%（37,179/86,369）が非整合、全体点数の
   28.6%相当。ただし**vertex近傍のスポットチェックで面積114-173pxの
   無視できない塊も非整合判定**され、efficiency最優先方針に反する
   リスクを確認。面積下限（<30px）を併用した保守版なら18.8%削減
   （24,512個）に留める。可視化で確認したところ、保守版が正しく
   実信号（トラック線上の塊）を残していることを確認
   （`/tmp/filter_vertex_vis.png`、緑=整合/保持、赤=非整合+小=保守版
   でも削除、橙=非整合だが面積≥30=保守版のみ保持）。

**RIKEN Kasagi氏のコード `binary_segmentation` を発見・解析**
（`~/work/e07/binary_segmentation`、理研とは独立研究のため学習済み
重みは共有不可）: `segmentation_models_pytorch` ベースのU-Net系
二値セグメンテーション（`in_channels=1`→`classes=1`、Dice loss）。
入力=生グレースケール画像(1024×1024にSPNGを4分割)、出力=画素単位の
トラック二値マスク——私たちの `fog_remove→otsu_binarize→remove_noise`
を学習ベースで置き換えるもの。学習データはMCシミュレーション+
pix2pixHD GANドメイン変換で生成（`gray_to_track`データセット、
理研内部クラスタ）。`common_image_proc.py` に私たちと全く同じ
ガウスぼかし+差分+閾値の古典実装があり、理研チームも同じ古典手法の
限界に直面してML化したことがうかがえる。

**次の一手: 3系統並行調査を開始**
1. （進行中）古典: Hough整合性フィルタの保守版を採用するか、
   さらに既知vertexでの再現性を検証してから決定
2. （TODO）ML用合成データ: 実背景（トラック無し領域を実データから
   切り出し）+ 簡略化した幾何モデル（`specials_x20`の角度・多重度
   統計を参考にした手続き的トラック生成、実測済みグレインサイズ/
   輝度分布でテクスチャ付与）を試作。Geant4+GAN相当のフル
   ドメイン変換は不要という想定
3. （進行中、エージェントに調査依頼）公開モデル: "UCS: A Universal
   Model for Curvilinear Structure Segmentation"（SAMベース、8種の
   線状構造ドメインで学習、GitHub: kylechuuuuu/UCS）が有力候補。
   学習済み重みの実際の入手可否・ライセンス・ファインチューン手順を
   調査中

## 2026-07-12 17:25 JST — スケルトン化（中心線抽出）を実装、視覚的・定量的に検証

前エントリで見つかった「トラック幅がTHの3-4倍」「`integrate_smallregions`
がクラッシュ」問題への対応。ユーザーとの議論の結論: 前段でトラック幅を
中心線に潰すのは「物理的な track/vertex 検出」の肩代わりではなく、
fog除去・Otsu・ノイズ除去と同じ「画像レベルの前処理」の延長線上にある
という整理で実装に着手。

**実装**: `scikit-image`（`skimage.morphology.skeletonize`）を依存に
追加（`requirements.txt`）。`weighted_grid_hits()` を改修:
30pxを超える connected component は、セル分割の前に二値マスクを
1px幅のスケルトン（中心線）に細線化。ヒットの**位置**はスケルトン
ピクセルの輝度加重重心から取るが、**`n`**（密度指標）は元のブロブの
セル内ピクセル数のまま維持——物理的な意味（局所グレイン密度）を保持
しつつ位置だけをクリーンにする設計。ヒット総数は完全に不変
（133,183件、位置のみ変更）。

**定量検証**: 例の882px長トラック（1つの connected component だけを
正しく単離して測定——バウンディングボックスでの測定は他の無関係な点を
巻き込むため不可、と教訓化）の局所（40px窓）垂直方向半幅:

| 指標 | スケルトン化前 | 後 |
|---|---|---|
| 局所最大値の平均 | 8.12px | 2.70px |
| 局所最大値の中央値 | 6.63px | 2.51px |
| 局所平均値の平均 | 2.24px | 1.56px |

約2.6-3倍の改善。ただし完全にTH=1.5-2px以内には収まっていない
（中央値2.51pxはまだ僅かに超過）——セル分割自体が30pxウィンドウ内で
残差を生むため、根本解消ではなく大幅な緩和。

**視覚化**: 同じ882pxトラックについて、(A) 全長概観と (B) 6倍拡大詳細の
2枚を作成。青=元のブロブ輪郭（実際の太さ）、シアン=スケルトン、
黄十字=最終エクスポート点、を重ね描き。詳細図で「太い塊→1px線→
その上の点」という流れが明確に見える。既存Artifactに追記（同一URL）:
https://claude.ai/code/artifact/8f9a90a2-7186-41e2-992c-3e80fd078241

**未検証**: `integrate_smallregions` のクラッシュがスケルトン化で
実際に解消するかは、まだ実測していない（前回の5×5局所テストの
再実行が必要、約86分）。

高速テスト再検証済み（52/52）。KISO再エクスポート済み
（133,183ヒット、5.3秒）。

## 2026-07-12 15:25 JST — detectlseg_smallregion のアルゴリズム確認、点削減方式をハイブリッド（component + local grid）に改良

**`detectlseg_smallregion.m` の中身確認**（参考記録、MATLAB本体は無変更）:
2段階構成。(1) `subfunc1`: 点群の**全ペア距離**を `pdist` で計算 →
最小全域木（MST）を構築 → 直線状でない部分の枝を切る、という階層的
クラスタリングで初期線分に分解。(2) `subfunc2`: 各線分をSVDで軸推定 →
軸から一定距離以内の点を取り込んで成長 → 重複する線分同士を統合 →
端点を微調整、を収束するまで繰り返す。`pdist` の全ペア距離計算が
点数Nに対しN²のペア数になるのが支配的なコスト——145,830点（生ピクセル
最大領域）だと約106億ペア。実測フィットの指数（N^2.887〜3.06）は、
これに加えて `subfunc1`/`subfunc2` の反復ループがそれぞれ追加のN²
オーダーの処理を複数パス行っていることと整合する。

**点削減方式の再検討（ユーザーとの議論）**

用途の確認: Python前処理 → MATLAB候補選出 → スキャナーによる人選別 →
その結果を教師データとしてフィードバック、というワークフロー。MATLAB
側は変更せず、前段処理だけで対応する方針を再確認。

検討した案:
1. **Hough検出でトラック方向を先に求め、線に沿って再サンプリング**
   （ユーザー提案）。per-slice で `cv2.HoughLinesP` を試したところ、
   z投影済み画像向けのデフォルトパラメータ（thr=20,ml=25,mg=4）では
   1スライスの前景ピクセルの24-30%しかカバーできず、閾値を緩めると
   （thr=8,ml=10,mg=3）77-83%まで改善する一方、1スライスあたり
   5,000本超の重複線分が検出され、これを1本のトラックに統合するには
   結局 `cluster_tracks`/`link_tracks` 相当の重複除去ロジックが
   必要になり複雑化することが判明。今回は見送り、シンプルな案を優先。
2. **採用: connected-component（分離のみ）+ 塊内固定グリッド
   （ハイブリッド）**。`weighted_grid_hits()` を全面改修:
   `cv2.connectedComponentsWithStats` で連結性に基づき grouping
   （形状/直線クラスタリングではなく単純な連結性判定）した上で、
   各連結成分が `cell` px（デフォルト30px）を超える場合のみ内部を
   固定グリッドで再分割。これにより:
   - 別々の（繋がっていない）トラックが同じ点に混入しない
   - 長い連続トラックも塊内で分割されるため1点への collapse なし
   - コストはほぼ純グリッド方式のまま（KISO: 133,183点 vs 130,364点、
     最大領域1,201 vs 1,111 — 数%増程度）

**視覚的実証**: KISO slice10 で「1スライスだけで671組」の
近接するが別成分（<25px、隣接しない）のペアを検出。うち1例
（隣接する30pxセル境界をまたぐ複数成分）を6-7倍拡大して比較した
ところ、**旧グリッド方式（赤）の点が構造のない暗い空白領域に複数箇所
浮いており**、**ハイブリッド方式（黄）の点は全て実際の明るいトラック
構造上に位置する**ことを確認。これは前段（2026-07-12 14:xx台の
vertex収束密度チェック）で見つかった「グリッドが収束シグナルを弱める」
という懸念の直接的な原因の可視化になった。

**現状**: `module/matlab_export.py` は改修済み、高速テスト52/52通過、
KISOで再エクスポート済み（133,183ヒット、4.3秒）。点数・最大領域密度が
純グリッド方式とほぼ同じなので detectlseg 全域検証（約5.5-6h見込み）は
まだ実行していない——密度がほぼ変わらないため runtime はほぼ同じと
予想されるが、実測での再確認はユーザー判断待ち。

## 2026-07-12 02:53 JST — 固定グリッド方式のフルタイル実測が完了、見積もりとほぼ一致

前エントリ（固定グリッド方式への切替）の実測検証。KISO 全256領域を
通して `detectlseg_smallregion` を実行。

**結果**: 130,364ヒット → **19,802.2秒（5.50時間）**、検出セグメント
30,179件。事前のべき乗則フィットによる見積もり（~6.01h）とほぼ一致
——外挿モデルの信頼性を裏付ける結果。connected-componentモードの
実測2.52hより長いが、長いトラックの collapse バグを修正した代償として
妥当な範囲。

既知vertex（KISO, vx=1096/vy=1028/z_slice=10）周辺80px・z±8スライス
以内のセグメント数は81件（旧connected-componentモードでは113件）
——同程度の密度でトラック構造が検出されており、グリッド方式でも
vertex周辺の解析に必要な情報は保持されている。

**現時点のまとめ**: MATLAB 側（`detect_tracks.m`, `detectlseg_smallregion`
等）は一切変更せず、前段（`module/matlab_export.py`）だけで
392時間→392時間台のオーダーから 5.5時間まで実用化。長いトラックの
構造破壊バグも解消。次の課題は 2025タイル全体へのスケール（1タイル
5.5hなら全タイルは非現実的——並列化やタイルあたりの計算資源確保が
必要）と、`detectbunki`（分岐点検出）による実際の vertex 再構成。

テスト: 高速スイート引き続き 52/52 通過（今回はエクスポート側の変更
なし、MATLABのみ実行）。

## 2026-07-11 21:19 JST — connected-componentクラスタリングの致命的バグを発見、固定グリッド方式に全面置換

ユーザーからの一連の質問「クラスタリングはしないでいい」「全ピクセル
情報のまま渡すとやばい？」「03（二値化）までで渡すのもダメ？」を受けて
方式を再検討。

**発見したバグ**: KISO tile 全体でブロブの bounding-box 最大延長を
調べたところ、**延長882px**（面積はわずか4845px）の細長い connected
component が存在した。これは1本の連続したトラック（グレインが途切れず
繋がっている）で、`weighted_centroids()`（前回実装）はこれを**1点に
集約してしまう**——線・分岐構造の情報が完全に失われる致命的なバグ。
延長100px超のブロブは全体の約1.1%（1,123/101,479）存在し、無視できない
頻度。Web ビューアで実際に可視化して確認（1本の連続トラックに黄色い
十字が1つだけ）。

**「全ピクセルのまま」「二値化until（03）まま」の再検証**: どちらも
`export_hits`（pixel モード）と等価——KISOで12,359,763ヒット。実測
データ（centroidモード完走時の157領域の実測値、対数回帰で
`k=2.887, A=4.23e-7`）で外挿すると detectlseg 全領域で**約3,668,848
時間（≈15万日）**。一方、生ピクセルモード自身の実測2点
（731pt→14.87s, 2991pt→291.48s）による自己無矛盾フィット
（`k=2.11`）では**約15,238時間（≈635日）**。フィット方法で2桁差が
出るほど N=145,830（最大領域の実密度）への外挿は不安定だが、
どちらにしても完全に非現実的という結論は変わらない。

**採用した方式: 固定グリッドによる間引き（クラスタリングなし）**

`module/matlab_export.py` の `weighted_centroids()`
（connected-component ベース）を廃止し、`weighted_grid_hits()`
に全面置換。二値マスクの前景ピクセルを固定サイズ（`_GRID_CELL_PX=30`、
0.29μm/px換算で約8.7μm）のグリッドセルに位置だけで割り当て、
セルごとに輝度加重重心を1点出力。`cv2.findContours` 等の形状・連結性
判定を一切使わない——ユーザーの「クラスタリングはしないでいい」という
方針に合致。長いトラックでも `cell` px ごとに再サンプリングされ続ける
ため、1点への collapse が起きない。

セルサイズは KISO 全体での試算スイープから選定（N³系フィットで
detectlseg 時間を外挿）:

| cell | 総ヒット数 | 最大領域点数 | 検出時間見積 |
|---|---|---|---|
| 10px | 522,292 | 4,991 | 504 h |
| 15px | 321,359 | 2,937 | 108 h |
| 20px | 225,154 | 1,984 | 35 h |
| 25px | 166,998 | 1,424 | 13 h |
| **30px** | **130,364** | **1,111** | **~6 h**（実測2.52hに近い） |

30pxを採用: 128px領域あたり約4サンプル/トラックを保証しつつ、実測済み
2.52hに近い水準。実際にKISOで再エクスポート（130,364ヒット、4.2秒）し、
以前1点に潰れていた882px長のトラックが**468点**に分割されて残っている
ことを Web ビューアと直接集計の両方で確認。

**Web ビューア**: `cent` ステップをブロブ輪郭表示から、うっすらとした
グリッド線 + 黄色い十字（各グリッドヒット）の表示に変更。「クラスタ
形状」の含意を完全に排除。

**未検証**: 実際に全256領域を通した detectlseg の実測（推定6h）。
バックグラウンドで開始する。

高速テスト再検証済み（52/52）。

## 2026-07-11 21:03 JST — 04パネルの誤解を解消: 実ブロブ形状の輪郭表示に変更

ユーザーから「04の絵はどう見たらいいのか」「クラスタリングは MATLAB
側でやってくれているのでは」との質問。回答:

- **MATLAB は今回一切クラスタリングしていない**。`mabiki(pl,3)` は
  `kiso_centroid_full_test.m` で意図的にスキップ（centroid export が
  既に ~1点/グレインなので不要と判断済み、2026-07-11 18:29 エントリ
  参照）。線分クラスタリングを行う `detectlseg_smallregion` も今回は
  未実行。04パネルはあくまで Python 側 `weighted_centroids()` の出力
  ＝MATLABに渡す直前の生の点群であり、グレイン単位への集約は完全に
  Python 側の仕事。
- 04パネルが「手動で引いたクラスタ境界」のように見えた原因は、
  ブロブ面積を `√area/2` の円半径で表示していたため、vertex 付近で
  複数トラックが融合した大きいブロブ（面積3000px超）が目立つ円になり、
  誤解を招いていた。

**対応**: `module/matlab_export.py` の `weighted_centroids()` が輪郭
（`cv2.findContours` の生ポリゴン）も返すよう拡張
（`(cx, cy, area, contour)` の4要素タプルに変更）。`module/server/app.py`
の `cent` ステップは、面積比例の円の代わりに**実際のブロブ形状の輪郭**
（赤）+ **重心位置の十字マーカー**（黄）を描画するよう変更。輪郭1つに
十字1つが対応することが視覚的に自明になり、トラックに沿って輪郭が
細長く伸びている様子も見えるようになった。

KISO 既知vertex周辺で再確認: 輪郭がトラック方向に沿って伸びており、
各輪郭に対応する十字が1つずつ配置されていることを確認。Artifact
（同一URL）を更新: https://claude.ai/code/artifact/8f9a90a2-7186-41e2-992c-3e80fd078241

高速テスト再検証済み（52/52）。

## 2026-07-11 20:14 JST — 重心を輝度加重に変更 + Web ビューアに可視化ステップ追加

ユーザー指摘2点への対応: (1) 二値形状だけの重心は不十分で輝度も使うべき、
(2) 生画像 → MATLAB 入力点群までの過程を既存 Web ビューア（`/view/`）で
確認したい。

**輝度加重重心（`module/matlab_export.py`）**

`weighted_centroids(binary, intensity)` を追加。従来の
`cv2.moments(cnt)`（輪郭点だけを見る形状重心）をやめ、各ブロブの
bounding box を fog 除去後強度画像でマスク・重み付けして
`cv2.moments(weighted, binaryImage=False)` で加重重心を計算。
KISO slice10 で幾何重心との差を検証: 平均シフト 0.49px（中央値面積49px
のブロブ）、最大シフトは面積3075のブロブで **14.98px**——大きい/歪んだ
グレインクラスタほど効果が大きいことを確認。`export_hits_centroid()`
をこのヘルパー使用に切替。ヒット数・所要時間は変化なし（KISO:
101,479ヒット、約5秒）。

**Web ビューア可視化（`module/server/app.py`）**

`/view/` の Processing Pipeline に新ステップ「Grain Centroids (MATLAB)」
（5番目、Hough/Tracks の前）を追加。fog 除去後のグレースケール画像を
背景に、二値化後の各ブロブへ黄色い円（半径=√面積/2）と輝度加重重心の
赤い点をオーバーレイ表示。`_process()` を再構成し、`thr` 適用後も
fog 除去画像を保持（従来は `current` を上書きしていたため背景として
使えなかった）。

**確認結果**: KISO 既知vertex（vx=1096, vy=1028, z_slice=10）周辺
400×400px をサーバから直接取得し確認。fog除去後の画像で複数のトラック
様の線がほぼ同一点に収束しているのが目視できる（既知vertex座標と一致）。
centroid オーバーレイでもその収束線に沿って重心が並んでおり、density
削減がランダムな間引きではなく実際のトラック構造を保持していることを
視覚的に確認できた。4段階（raw / fog / binary / centroid）比較を
Artifact として残した:
https://claude.ai/code/artifact/8f9a90a2-7186-41e2-992c-3e80fd078241

テスト: 高速スイート再検証済み（52/52）。ローカルサーバ
（`python -m module.server specials_x20 --port 8123`）で対話的にも確認可。

## 2026-07-11 18:29 JST — MATLAB 入力テスト第2回: 重心化エクスポートで検出が実用時間に

方針: MATLAB 側（detectlseg_smallregion / mabiki 等）は変更せず、Python 側
の前段処理（エクスポート方式）を再考する。KISO タイル通しテスト（region
137 = 既知 vertex 領域まで含む全 256 領域）で検証。

**変更内容（`module/matlab_export.py`）**

第1回テストの元凶は「二値化後の生ピクセルを1つずつヒットとして書き出す」
方式そのものだった、と判明。1つの粒子（グレイン）クラスタが二値マスク上で
数十ピクセルの塊になっていても、そのピクセル数だけヒットが重複生成され
ていた。`detect_tracks.m` が本来想定する「1 hit = 1 粒子」という物理的
意味とも合っていない。

対策として `export_hits_centroid()` を追加（デフォルト化、`--mode
{centroid,pixel}` で旧方式も選択可）: スライスごとに二値化 → connected
component（`cv2.findContours`）ごとに1つの重心ヒットとして出力。`n` は
blob 面積（グレインサイズ / クラスタ多重度の proxy）。輝度カットと違い
どのヒットも捨てず「重複を1点に正規化する」だけなので、efficiency 最優先
方針と衝突しない。

**KISO タイルでの効果**

| 指標 | pixel（旧方式） | centroid（新方式） |
|---|---|---|
| 総ヒット数 | 12,359,763 | 101,479（**122倍減**） |
| 最大領域の点数 | 26,962 | 838（**32倍減**） |
| detectlseg_smallregion 全256領域 | 推定 392 h | **実測 9,067.5 s（2.52 h）** |

**実測**: KISO タイル全256領域を通し完走（初の完走）。検出セグメント数
24,799。事前の N³ フィット外挿（2.48 h、region 14/15 の実測 2 点から
推定）と実測 2.52 h がほぼ一致 — detectlseg のコストは領域内点数に対し
おおよそ N² ではなく **N³ 程度**とみてよい。

**既知 vertex の確認**（`tests/specials_gt.json` KISO: vx=1096, vy=1028,
z_slice=10）: 対応する小領域（region 137, row9/col9）は 772 点・191
セグメント検出。vertex 座標から半径 80px・z ±8 スライス以内に位置する
セグメントが **113 個**存在し、空/デタラメではなく妥当な密度でトラック
セグメントが検出されている。ただしこれは detectlseg 止まりの確認であり、
`detectbunki`（分岐点検出）以降の物理的な vertex 再構成はまだ試していない。

**結論・次の一手（未決定）**

- centroid モードをデフォルトに変更済み。今後 specials_x20 の他タイルで
  再現性を確認する価値あり（今回は KISO 1件のみ）。
- detectlseg 後段（`detectbunki` 分岐点検出、`integrate_smallregions`
  領域統合）はまだ未実行。region 137 のセグメント群から実際に KISO の
  既知 ΛΛ vertex 構造が再構成できるかは次の検証課題。
- 2.52 h/タイルは 2025 タイル全体のバッチ化にはまだ重い
  （単純計算で ~5,100 h ≒ 212 日、並列化前提）。centroid モードでも
  detectlseg 自体のアルゴリズム改善（pdist → KD-tree 等）や領域サイズ
  縮小の検討はいずれ要る可能性があるが、「MATLAB はあまり変えない」
  方針のもとでは優先度を下げ、まずは前段側でどこまで詰められるか
  （例: fog_ksize・noise_amin 等のパラメータ調整でさらに blob 数を
  減らせるか）を先に見る。

## 2026-07-11 15:29 JST — MATLAB 入力テスト第1回: KISO 実データで detectlseg がスケールしない

macbook + MATLAB R2026a で、実データを初めてグラフ検出器に投入。

**手順**

- `run.py matlab-export specials_x20/KISO/image.json` →
  `results/matlab/KISO_pl.mat`（12,359,763 ヒット、20 MB、7 秒。
  loadmat 往復確認済み。x/y: 1–2048、z: 1–60、n: 4–135）。
- テストドライバ `results/matlab/kiso_lseg_test.m`（未コミット）で
  detect_tracks.m のステージ2相当（mabiki(pl,3) → 16×16 小領域 →
  detectlseg_smallregion）を領域ごと時間計測付きで実行。

**結果**

- mabiki(pl,3): 7.7 秒で 12.36M → 2.46M 点。メモリも問題なし
  （懸念していたフル 3D 配列は 60 スライスなら許容範囲）。
- detectlseg_smallregion が実データ密度でスケールしない:
  731 点 → 14.9 秒、2,991 点 → 291 秒（ほぼ N²）。
  1 万点超の領域が 117/256 個あり、N² 外挿で全領域 **約 392 時間**。
  領域14（約1万点）が 55 分経っても終わらず実測とも整合。1 時間で
  中断（13/256 領域完走、そこまでの検出は 253 セグメント）。
- 多重度カットは効かない: 実データは 3×3 ブロックがほぼ埋まる
  （nn=9 が最大ビン、472k ブロック）。nn≥5 でも見積 119 時間。

**間引き・カットの試算**（N² 外挿、Python 側で近似計算）

| 条件 | 点数 | 最大領域 | 見積 |
|---|---|---|---|
| mabiki3（現行） | 2.46M | 27.0k | 392 h |
| mabiki6 | 978k | 10.2k | 61 h |
| mabiki9 | 594k | 6.2k | 22 h |
| n≥40 + mabiki3 | 233k | 5.8k | 5.7 h |
| n≥60 + mabiki3 | 43k | 2.0k | 0.3 h |
| n≥40 + mabiki6 | 132k | 2.9k | 1.8 h |

輝度分布: 中央値 16、75% 24、90% 35、99% 59。n≥40 は上位 ~10% のみ
残す強いカットで、efficiency 最優先方針と要すり合わせ（暗い MIP
トラックを落とすリスク）。

**次の候補（未決定）**

1. 輝度カット妥当性の検証: KISO 既知 vertex 周辺領域だけで
   カット強度を変えて detectlseg を回し、トラックが残るか確認（安価）。
2. 前処理側で絞る: `--noise-amin` 増などノイズ除去強化。
3. アルゴリズム側: pdist O(N²) を rangesearch/KD-tree に置換、
   または領域 128px → 64px（総時間 ~1/4）。MATLAB コードの
   所有権と要相談。

## 2026-07-11 14:16 JST — メモを analysis-note.md に統一（Notion 廃止・逆時系列化）

ユーザー判断でノート運用を変更:

- Notion（image-pre-processing DB）への転記は廃止。メモはこのファイル
  `analysis-note.md` 1 本に統一。
- 旧 `ANALYSIS.md`（英語）/ `ANALYSIS_ja.md`（日本語ミラー）を削除し、
  日本語版の全 43 エントリをここへ移行。英語原文は git 履歴で参照可能。
- エントリは**最新が上**（逆時系列）。見出しに時刻も入れる:
  `## YYYY-MM-DD HH:MM JST — タイトル`。本日以前のエントリは時刻不明の
  ため日付のみ（2026-07-11 の 3 件はコミット時刻・作業ログから補完）。
- リファレンス・未解決課題セクションはファイル上部に維持。
- README / CLAUDE.md / AGENTS.md の該当ルールも更新。

## 2026-07-11 14:09 JST — ツーリング: pyenv + requirements.txt 移行、エージェント設定ファイルの管理外化

午前の macOS 移植エントリの続き。ユーザー判断で環境管理方式を変更。

- `AGENTS.md` / `CLAUDE.md` を git 管理外に（gitignore、ローカルには保持）。
  マシンごとのエージェント指示であり、プロジェクトコードではないため。
- 依存関係を `pyproject.toml` の `[project]` から `requirements.txt` に移行。
  `pyproject.toml` は pytest/ruff 設定のみ。未使用の console scripts
  （`e07analyze`, `e07merge`, `e07view`）は削除。操作面は従来どおり
  `run.py` / `python -m module.*`。
- Python 3.14.6 を pyenv でビルドし `pyenv local` で選択。venv なし、
  editable install なし（`module` はリポジトリルートから解決）。
- pyenv の Python で非 slow 52/52 通過を確認（slow スイートは執筆時点で
  再実行中。同一ツリー・同一依存バージョンで本日午前に 35/35 通過済み）。

## 2026-07-11 13:58 JST — macOS 移植: 環境検証と OpenCV 5 互換対応

kekcc からリポジトリを macbook（`~/work/e07/e07-fullscan`）へ移設し、
一通り検証した。目的は、sshfs でマウントしたスキャンデータに対して
ローカルで開発・実行できるようにすること。

**やったこと**

- ローカル venv（Python 3.14, numpy 2.x, OpenCV 5.0）を構築し
  `pip install -e ".[dev,server]"` を実行。
- editable install が失敗: `[build-system].build-backend` が存在しない
  `setuptools.backends.legacy:build` になっていた。標準の
  `setuptools.build_meta` に修正。kekcc ではクリーンな venv から
  pip インストールしていなかったため顕在化していなかった。
- `pytest -m "not slow"`: `tests/test_tracking.py` で 9 件失敗。全て
  `module/pipeline/finder.py:52` の
  `TypeError: cannot unpack non-iterable numpy.int32`。原因は OpenCV 5 で
  `cv2.HoughLinesP` の返り値形状が `(N, 1, 4)` から `(N, 4)` に変わり、
  `lines[:, 0]` が axis 1 の squeeze ではなく最初の「列」の抽出になった
  こと。`lines.reshape(-1, 4)`（4.x/5.x 両対応）に修正。
- gitignore されている `specials_x20` symlink を復元
  （`ln -s ../specials_x20`）し、
  `E07_SPECIALS_DIR=$PWD/specials_x20 pytest -m slow` で slow スイートを
  実行。

**結果**

- 非 slow 52/52、slow 35/35 全て通過（slow スイートは macbook で約 8.5 分。
  kekcc のバッチキュー経由より反復開発に便利）。
- 修正2件は未コミット。macOS 側の追加変更とまとめてレビュー後にコミット
  予定。

**次のステップ**

- sshfs で kekcc のスキャンデータをマウント（`~/mnt/kek_e07`、FUSE-T
  導入済み）し、kekcc 絶対パス `/group/had/sks/E07/tohoku/fullscan` を
  指したままの `fullscan-image` symlink を張り替える。
- kekcc は OpenCV 4.x、ローカルは 5.0 のため、両バージョンの固定または
  CI テストを検討。

## 2026-07-11 13:35 JST — MATLAB エクスポート試作をコミット；ToDo 整理

2026-06-23 の試作（`module/matlab_export.py`、`run.py matlab-export`、
README / 日記 / discussion の更新）はユーザー確認待ちで未コミットのまま
だった。本日ユーザー承認が出たので、一式を 1 つの feature コミットとして
`main` に載せ、`origin/main` へ push する。2026-06-23 以降コードの変更は
なし。本エントリはコミットの記録と ToDo の現状整理。

### ToDo（持ち越し）

- **密度／計算量。** タイル V00000004 のエクスポートは 22,072,518 ヒットで、
  MATLAB 検出器がチューニングされたシミュレーションの約 1000 倍。対策の
  配分を決める：Python 側ノイズ除去の強化（公開済み `--noise-*` フラグ）
  vs. `mabiki(pl, 3)` を超える MATLAB 側ダウンサンプリング。
- **MATLAB 実走。** `V00000004_..._pl.mat` を
  `e07/matlab/detect_tracks.m` に入力し、`detectlseg_smallregion`
  （128×128×80 領域ごとに `pdist` O(N²)）の実行時間・メモリを計測して
  から複数タイルへ展開する。
- **出力置き場の規約。** 試作 `.mat` はセッションの scratchpad にある。
  リポジトリ側の規約（例: `results/matlab/`）を決め、その後に複数タイル
  一括エクスポートを追加。
- **パイプライン側の積み残し（本スレッド以前から）。** 重粒子起因の偽
  バーテックスに対する角度広がりフィルタ込みのバーテックス候補レビュー、
  `hough_ml=30` の再実行。

## 2026-06-23 — MATLAB グラフ検出器向けエクスポート（試作）

fullscan の出力は `e07/matlab` に置かれたグラフ理論イベント検出器
（`detect_tracks.m` ＋ヘルパー: `detectlseg_smallregion`,
`integrate_smallregions`, `detectbunki`, `mabiki` 他）へ繋ぐ予定。この検出器は
3D ヒット点群から最小全域木＋線分近似で飛跡を構築し、分岐をグループ化する。
Hough パイプラインとは別アプローチ。その stage-1 入力を生成する必要がある。

### インターフェース（ユーザーと決定）

検出器の stage-1 入力はヒット画素リスト `pl = {x, y, z, n, sheet, id}`
（x,y はピクセル、z はスライス番号）。以降の stage は
`dspl = mabiki(pl, 3)` の x,y,z しか使わない。ユーザー決定: `pl` を `.mat` に
直接書出し；ブロック3 の `mabiki` ダウンサンプルは MATLAB 側に任せる；
まず1タイルで試作。

Hough 経路との重要な違い: Hough は 9 スライス窓を 1 枚に z 射影（`zpj`）して
z を捨てる。グラフ検出器は z 方向全体を要するため、**各スライスを個別に
二値化**（`module.preprocess` の fog 除去 → Otsu → ノイズ除去を再利用）し、
前景画素をすべて 3D ヒットとして出力する。シミュレーションの `Ev_*.xlsx` の
Summary/飛跡別シートは実データにない正解情報なので、`sheet`/`id` は 0 の
プレースホルダ、`n` は fog 除去後の強度を持たせる。

### 実装

`module/matlab_export.py`（`export_hits` ＋ `save_mat`、薄い CLI）。
`run.py matlab-export` として配線。座標は 1-based（x = col + 1, y = row + 1,
z = slice + 1）で MATLAB の (1, 1, 1) 原点と `x > lb` / `x <= ub` の小領域
分割に合わせる（0-based だと最初の行/列が落ちる）。出力は `scipy.io.savemat`
で `pl`（N×6 float64）＋ `variablenamespl`、圧縮あり。

### 試作結果とスケーリングの注意

タイル V00000004（2048×2048×58）: **22,072,518 ヒット**、x,y ∈ [1, 2048]、
z ∈ [1, 58]、n ∈ [3, 119]；圧縮 `.mat` ≈ 38 MB；`myenv`（cv2+scipy）で約 27 秒。
`scipy.io.loadmat` で往復を検証。

注意: これは MATLAB が調整されたシミュレーションイベント（2048×2048×80 で
~10⁴ 点）より約 1000 倍密。`mabiki(.,3)` で ~9× 削減されるが、
`detectlseg_smallregion` は 128×128×80 の各小領域内で `pdist`（O(N²)）を使う
ため、MATLAB 側のダウンサンプル、場合によっては Python 側のより強い
ノイズ除去（`--noise-*` フラグを公開済み）が、グラフ段を現実的にする上で効く。
ユーザーに共有済み。エクスポート契約自体の欠陥ではない。

---

## 2026-05-31 — close 後の共同レビュー：monitor next-step、legacy docs、2-space

push 後、Codex の再レビューで cleanup に対する follow-up が4件出た。ユーザーは
Claude と Codex が双方納得するまで反復するよう指示。4件すべてを対応し、双方が
sign-off した（discussion 2026-05-31 15:34–18:09）。

### 指摘と修正

1. (挙動) `module/pipeline_status._next_step()` が隔離済み ΛΛ-pair スクリプト
   （find_pairs / find_crossview_pairs / filter_xview_pairs）を指していた。
   ΛΛ-pair は 2026-05-14 に個別頂点検出へ supersede 済みなので、
   `vertices_merged_v6.parquet` 後は "vertices ready: review with run.py
   crops / review / click" を返すよう変更。未使用の pairs/xview/xconn ローカルを
   削除。`scripts/monitor.py --pipeline` で実動作確認。
2. (docs) README「Vertex Pair Search」「Cross-View ΛΛ Pair Search」を
   「(legacy)」表記＋来歴 blockquote にし、コマンドパスを全て `scripts/legacy/`
   へ。物理内容（KISO 結果、v5/v7 注記、出力列）は保持。
   `scripts/run_pipeline_v6.sh` の Step 6-7 も同様に legacy 表記＋
   `scripts/legacy/` パス修正。
3. (docs) README の crop オプション表で `--zpj-half` / `--zpj-mode` を
   「(ignored, back-compat)」とし、`module/review/_cli_crop_vertices.py` の
   コード NOTE と整合。
4. (style) 13ファイルが 4-space で 2-space 規則違反だった。薄いラッパー8個と
   残り5本体（analyze/cli.py, clustering/_link.py,
   diagnostics/{bg_cost_spread,step5_compat,lowsp_spread_radius}.py）を 2-space
   化。5本体は multiline-string 内部を保護する tokenize ベースの halve ＋
   bracket 継続行の visual-indent 整列で変換し、各ファイルを HEAD との
   `ast.dump` 等価でガード。よって挙動保存を証明できる。

加えて README の古い「Package Structure」tree を更新（preprocess.py,
pipeline_status.py, review/, diagnostics/, _cli_* 規約, utils ヘルパーを追加。
compact に保つ）。

### 検証と sign-off

tree 全体 min-indent == 2、odd-indent 0 行（scripts/legacy/ 除く）；
module+scripts 全 `py_compile` OK；diagnostics import smoke OK；
`pytest -m "not slow"` 52 passed, 35 deselected。Codex は独自の最終パス
（git diff --check, py_compile, 5ファイルの AST 等価, legacy grep, 4つの
help/pipeline surface）を実施し、README の wrapper 文を `_cli_*` を超えて
一般化する1点の修正の後、構造・整理に納得と明言。双方 sign-off。

---

## 2026-05-31 — Phase 3: CLI 本体を module/ へ、review パッケージ、Codex sign-off

2026-05-30 の scripts-surface cleanup の続き。薄いラッパー方針を徹底し、
`scripts/*.py` が実ロジックを持たない（各約7行で `module/` へ委譲）形にした。
全工程で挙動は不変。

### なぜ

run.py が dispatcher になった後も重い本体は `scripts/` に残り、パッケージが
実体の真の所在になっていなかった（操作 surface とロジックが2つの木に分裂）。
本体を `module/` へ移すことで `module/` が自己完結し、`scripts/` は純粋な
入口層（ラッパー＋文書化された shell/recipe 入口＋legacy 隔離）になる。

### 変更（family 別、コミット順）

- Family 1 (70733ff): clustering CLI 本体 ->
  `module/clustering/_cli_find_vertices.py`, `_cli_merge_vertices.py`。
- Family 2a (5d47af3): merge_chunks 本体 ->
  `module/merge/_cli_merge_chunks.py`。
- Family 2b (6fe5033): KEKCC submit 本体 ->
  `module/analyze/_cli_submit_kekcc.py`, `_cli_submit_vertex_kekcc.py`。
- Family 3 (e22feb5): 新 `module/review` パッケージ；review CLI 本体 ->
  `_cli_crop_vertices.py`, `_cli_vertex_map.py`, `_cli_review_crops.py`,
  `_cli_click_vertex.py`。
- Family 4 (d3e8fac): live-job monitor 本体 -> `module/utils/job_monitor.py`；
  scripts/monitor.py はラッパーに縮小。（pipeline overview 本体は 2026-05-30 の
  status/monitor 統合で既に `module/pipeline_status.py` に存在。）
- Codex レビュー対応 (edd2dce): CLAUDE.md と AGENTS.md の subpackage 行に
  `module/review` を追加し、`module/pipeline_status.py`（pipeline overview）と
  `module/utils/job_monitor.py`（live-job monitor 本体）を monitor/status
  ヘルパーとして明記。`scripts/status.py --help` を修正し、-h/--help で
  pipeline overview を surprise-run せず deprecation note + docstring を表示し
  exit 0 とした。

### end state

`scripts/` = README.md、薄い Python ラッパー（find_vertices, merge_vertices,
merge_chunks, submit_kekcc, submit_vertex_kekcc, crop_vertices, vertex_map,
review_crops, click_vertex, monitor, status[deprecated]）、文書化された
shell/recipe 入口（analyze.sh, kekcc_job.sh, kekcc_vertex.sh,
run_pipeline_v6.sh）、legacy/。日常操作は run.py、monitor concept は1つ、
重いロジックは `module/` 内。

### 検証と Codex sign-off

pytest -m "not slow" -> 52 passed, 35 deselected（約49s）、clean tree。
Codex は 2026-05-31（edd2dce の後）に最終 structural sign-off を行い、
AGENTS.md/CLAUDE.md が module/ 構造と monitor ヘルパー分離に合致すること、
`scripts/status.py --help` が overview を実行しなくなったこと、run.py/monitor.py
が意図した surface を表示することを確認した。Codex はこの最終確認で full pytest
suite を再実行せず、Claude 報告の `pytest -m "not slow"`（52 passed,
35 deselected）を再実行なしで受け入れた。残り structural blocker なし。README に
run.py を指す簡潔な「Operation Surface」注記を追加し、既存の scripts/*.py 例は
互換パスとして残した（本格的な README 書き直しは Codex 合意で延期）。

---

## 2026-05-30 — scripts-surface cleanup: run.py + scripts/ 縮小

操作面を再編し、scripts/ の .py/.sh 混在を解消。ユーザー決定: run.py 中心；
scripts/ に新規サブディレクトリを作らない（既存 scripts/legacy/ は維持）。
behavior-preserving。Codex 討議 2026-05-30 17:00–17:45。

### 変更

- 新規 repo-root `run.py`: 単一操作面；サブコマンドが subprocess で既存
  scripts / module entry に委譲（track, view, merge-tracks, vertices,
  merge-vertices, crops, review, map, click, submit-tracking, submit-vertices）。
  run.py に解析ロジックなし。
- 診断を package へ: `module/diagnostics/{step5_compat,lowsp_diag,
  lowsp_spread_radius,bg_cost_spread}.py`、`python -m module.diagnostics.<name>`
  で実行。
- legacy ΛΛ-pair KEKCC shell（kekcc_intra/xconn/filter_job）を scripts/legacy/
  へ移動、参照修正。
- status/monitor を1つに統合: pipeline-overview ロジックを
  `module/pipeline_status.py` へ（run(loop)/main）；`scripts/monitor.py
  --pipeline` が俯瞰（live-job フラグなしの既定）、live-job フラグは旧挙動維持；
  `scripts/status.py` は deprecation wrapper。
- 冗長な submit_kekcc.sh 削除（submit_kekcc.py が bsub をカバー）。
  kekcc_job.sh / analyze.sh の e07fullscan→module 参照漏れ修正。
- scripts/README.md 追加（縮小した surface の地図）。

### end state

scripts/ 現状: README.md、active pipeline CLI（run.py が委譲）、monitor.py
（単一モニタ）、status.py（deprecation wrapper）、LSF shell entry
（kekcc_job.sh, kekcc_vertex.sh, analyze.sh, run_pipeline_v6.sh）、legacy/。
診断は module/diagnostics/ 配下。

### 検証

各 Phase で pytest -m "not slow" 52 passed；monitor.py --pipeline と status.py
wrapper が俯瞰を表示；lowsp_spread_radius（python -m）が 2026-05-29 数値を再現。
コミット: 7f55b9c (run.py), 502ba4d (診断+legacy .sh), ed9377f (status/monitor),
3179e5b (submit_kekcc.sh 削除 + .sh 参照 + README)。

---

## 2026-05-30 — Tooling: persistent Codex discussion watcher script

ユーザーの明示依頼と、`scripts/` への新規 shell script 追加の例外許可に基づき、
`scripts/codex_discussion_watch.sh` を追加した。

- tmux向けのpersistent loopを実行する。
- 各tickで `codex exec` を呼び、`AGENTS.md`, `CLAUDE.md`, `discussion.md`,
  `discussion_ja.md`, `ANALYSIS.md`, `ANALYSIS_ja.md` から記憶を復元するpromptを
  渡す。
- prompt上で Codex を discussion-main かつ Markdown-only に保つ。
- `flock` で多重実行を避け、`timeout` でCodex実行の詰まりを防ぐ。
- `ROOT`, `CODEX_BIN`, `INTERVAL_SEC`, `TIMEOUT_SEC`, `LOCK_FILE`, `LOG_DIR`,
  `LOG_FILE` を環境変数でoverride可能。

検証: `bash -n scripts/codex_discussion_watch.sh` が成功し、実行権限を付与した。

Follow-up: 初回tmux実行で `codex exec` は `--ask-for-approval` を受け付けない
ことが判明。そのoptionを削除し、`bash -n` を再実行、detached
`codex-discuss-watch` tmux session を再起動し、watcher logで正常なno-op
discussion checkを確認した。

---

## 2026-05-30 — Coordination: persistent watcher memory rules

stateless な Codex watcher 実行（`codex exec`, cron, tmux loop）が、一時的な
model memoryではなくrepository filesから安全に文脈を復元できるよう、
`AGENTS.md` を更新した。

- package section を `module`（`e07fullscan` からrename済み）に更新し、現在の
  subpackages と共有 `preprocess` を記載。
- 単純化原則を追加: オッカムの剃刀を優先し、見えるentry pointとfilesを減らす。
  `scripts/` に新しいsubdirectoriesを作らず、diagnostics/legacy codeを日常操作面に
  出さない。
- startup memory rule を追加: 各session/watcherは `AGENTS.md`, `CLAUDE.md`,
  `discussion.md`, `discussion_ja.md`, `ANALYSIS.md`, `ANALYSIS_ja.md` を読む。
- Codex は discussion-main かつ Markdown-only editor、Claude は実装担当である
  ことを再確認。

---

## 2026-05-30 — 構造図 + 解析フロー図

ユーザー要望どおり README とは別の説明図を2つ、Graphviz で作成（整理後の構造を
反映）。Codex が整理を sign-off（discussion 2026-05-30 15:46）し、図が示すべき
5点を指定 — 両方反映。

- `docs/structure.dot` / `docs/structure.png`: ファイル/パッケージ構成。module/
  サブパッケージ（preprocess=共有 step-5、server=viewer をマーク）；scripts/ を
  active / diagnostics / infra / legacy に分類；config と tests を文脈表示。色:
  active 青/緑、viewer オレンジ、legacy グレー破線。
- `docs/analysis_flow.dot` / `docs/analysis_flow.png`: データフロー。raw z-stack
  → 共有 preprocessing（steps 1–5、step-5 境界をラベル）→ conventional
  Hough/vertex branch（find_tracks → find_vertices → merge → quality cut →
  sp / sp×nsl ranking → crops）。viewer は同じ preprocess/find_tracks を呼ぶ
  side client として描画；legacy ΛΛ-pair パスと将来の graph/ML branch は破線。

再生成: `dot -Tpng docs/<name>.dot -o docs/<name>.png`。

---

## 2026-05-30 — 整理: crop_vertices の stale オプション削除/マーク

整理スレッドの最終項目。behavior-preserving（crop は引き続き all-slice の
minimum-intensity projection を使用）。

- scripts/crop_vertices.py の未使用内部関数2つを削除: `_load_zproject` と
  `_fog_remove_max`（呼び出しなし；main は `_load_min_projection` を使用）。
  dead な `z_target` ローカルも削除。
- `--zpj-half` / `--zpj-mode` CLI 引数は未使用（`args` から読まれない）。Codex
  方針（user-facing、疑わしければマーク）に従い、CLI back-compat のため残し、
  help を「(unused; crops use all-slice min projection)」とマーク + NOTE
  コメント追加（フラグ削除はしない）。
- compile OK；残存参照なし；sys.path/SpngReader の bootstrap は残った関数に
  存在。

### 整理スレッド完了（Codex 承認待ち）

- dead code 削除（add_dip_angles）；crop stale 関数削除
- パッケージ改名 e07fullscan -> module
- legacy ΛΛ-pair パス隔離（clustering/_pairs.py, scripts/legacy/）
- active 頂点パスと active scripts が見やすくなった
- pytest -m "not slow" green；決定的診断が再現

---

## 2026-05-30 — 整理: legacy ΛΛ-pair コードの隔離

legacy ΛΛ-pair パス（2026-05-14 に個別頂点検出へ転換して旧化）を隔離し、
active な頂点パスを見やすくした。behavior-preserving。discussion 2026-05-29
20:33。

### 変更

- `find_vertex_pairs`（+ ΛΛ topology 定数）を `clustering/_vertex.py` から
  `clustering/_pairs.py` へ移動。`clustering/__init__.py` は back-compat の
  ため re-export（legacy マークのコメント付き）。_vertex.py は active パス
  （find_vertices, merge_vertex_slices）のみに。
- 6本の ΛΛ-pair scripts（find_pairs, find_crossview_pairs,
  filter_pairs_by_track, filter_xview_pairs, annotate_pairs, crop_pairs）を
  `scripts/legacy/` へ移動、ROOT を修正（`parents[1]` -> `parents[2]`）、
  provenance と実行注記を記した `scripts/legacy/README.md` を追加。`scripts/`
  には active な個別頂点 / 診断 / インフラ scripts のみ残る。

### 検証

- find_vertex_pairs の re-export 同一性 OK；`pytest -m "not slow"` 52 passed；
  legacy scripts は compile し ROOT が repo root に解決。
- Codex 方針により削除しない: pair topology は歴史的結果を生み参照されうる；
  削除はユーザー明示承認後のみ。

### 本整理スレッドの残り

- crop_vertices の stale オプション（z_target/zpj_mode）: マーク/文書化 or 削除。

---

## 2026-05-30 — 整理: dead-code 削除 + パッケージ改名 e07fullscan -> module

構造整理の継続。behavior-preserving；v6 解析挙動は不変。discussion
2026-05-29 20:32 / 2026-05-30 15:31。

### Dead code

- `add_dip_angles`（clustering/_link.py）削除: どこからも呼ばれていない；
  未使用化した `import math` も削除。残存参照なし；テスト green。

### パッケージ改名 e07fullscan -> module

- ユーザー決定（外部から import しないので、generic な import 名でも実用上の
  衝突/検索性コストはない）。
- `git mv e07fullscan module`；全32 .py（package, scripts, tests）、
  pyproject.toml（distribution 名、console entry points 3つ、packages.find）、
  README.md で `e07fullscan` -> `module`。
- CLI コマンド名（e07view/e07analyze/e07merge）は維持、module ターゲットのみ
  変更。top-level run.py は追加せず（スコープ外）。
- 本環境では pip 未インストール（PYTHONPATH 実行）なので再インストール不要；
  他所でインストール済みなら `pip install -e .` で entry points を更新。
- 過去の discussion/ANALYSIS エントリは歴史記録として `e07fullscan` 据え置き。

### 検証

- `pytest -m "not slow"`: 52 passed。
- `lowsp_spread_radius.py` 再実行で 2026-05-29 の数値再現（T011
  R25=28.5/R50=34.3、T004 3.1/3.7、D013 29.2）― 改名は pure。

### 残作業（本整理スレッド）

- legacy ΛΛ-pair コード（find_vertex_pairs + pair scripts 6本）を
  module/clustering/_pairs.py と scripts/legacy/ へ隔離、改名済みツリーで実施。
- crop_vertices の stale オプション（z_target/zpj_mode）: マーク/文書化 or 削除。

---

## 2026-05-29 — コード cleanup step 3: 診断 packaging

構造 cleanup を完了。診断スクリプトの behavior-preserving な pure refactor。

### 変更

- 新規 `e07fullscan/diagnostics/` パッケージ（`__init__` + `_common.py`）に、
  4診断スクリプトで重複していたヘルパを集約: `TRACK_CFG`（v6 config）、
  `DF_COLS`、`tracks_to_df`、`projection`、`find_tracks_cfg`。
- step5_compat, lowsp_diag, lowsp_spread_radius, bg_cost_spread を薄型化して
  これらを import；各々固有ロジックのみ保持。
- CLAUDE.md 更新: subpackages に `diagnostics` 追加、`preprocess` を共有
  branch-neutral モジュールとして注記。

### 検証

- `lowsp_spread_radius.py` 再実行で 2026-05-29 記録と数値一致（T011
  R25=28.5/R50=34.3、T004 3.1/3.7、D013 29.2/27.2）― pure refactor。
- `pytest -m "not slow"`: 52 passed。

### 構造 cleanup 完了

- #1 branch-neutral `preprocess` 抽出 ✓
- #2 tracking + server がそれを呼ぶ；dormant `noise_amax_upper` 欠落解消 ✓
- #3 診断 packaging；4スクリプト薄型化 ✓
- #4 targeted sub-vertex merge — 先送りの recall 機能（構造でない）

共有 step-5 境界が batch tracking path と viewer の両方で使う単一実装になり、
診断スクリプトが1ヘルパモジュールを共有。次の解析作業（再開時）: T011 型の
targeted sub-vertex merge（Hough-branch recall fix）。

---

## 2026-05-29 — コード cleanup step 1+2: 共有 preprocessing 抽出

Codex と合意した構造 cleanup を開始（discussion 2026-05-28 17:22 /
2026-05-29 14:28）。behavior-preserving；現行 config 下で v6 パイプラインの
解析挙動は不変。

### 変更

- 新規モジュール `e07fullscan/preprocess.py`（branch-neutral、Hough/graph
  分岐前の共有境界）: `fog_remove`、`otsu_binarize`、`remove_noise`（3分岐
  connected-component 面積フィルタの単一ソース）、`preprocess` =
  fog→Otsu→noise。
- `tracking/_finder.py`: ローカル `preprocess` 削除；`e07fullscan.preprocess`
  から import + re-export（`from tracking._finder import preprocess` を使う
  呼び出しは引き続き動く）。intensity 測定用 `fog_img` も `fog_remove` を使い、
  fog 除去の実装を1本化。
- `server/app.py`: `_process`/`_collect_stats` が fog/Otsu/noise を再実装
  しなくなり、共有関数を呼ぶ。これで **dormant な `noise_amax_upper` 欠落を
  解消**（server フィルタは以前 large-blob 分岐を欠いていた）。既定 config
  （`noise_amax_upper=0`）では server 出力は不変；差は large-blob 除去を
  有効化したときのみ ― debris tuning 前に直すと合意した fix。

### 検証

- `tests/test_preprocess.py`（新規）: 新 `preprocess` が旧 `_finder.preprocess`
  の凍結コピーと byte 一致（既定と `noise_amax_upper` 指定）、
  `remove_noise(amax_upper=0)` が旧 server 2分岐フィルタと一致、`amax_upper>0`
  で大型ブロブ除去。4/4 合格。
- `pytest -m "not slow"`: 52 passed、回帰なし。

### 先送り（Codex sequencing）

- Step 3: 再利用可能な診断ヘルパ（tracks_to_df、projection、TRACK_CFG;
  step5_compat/lowsp_diag/lowsp_spread_radius/bg_cost_spread で共通）を
  `e07fullscan/diagnostics/` へ移動し、スクリプトを薄くする。
- Step 4: targeted sub-vertex merge（T011 型の Hough-branch recall fix）。

---

## 2026-05-29 — 広い spread 半径の background-cost; T004 z-persistence

low-sp scoring スレッドを2つの bounded テストで締めた（batch 関数を直接呼出、
設計は Codex と合意、discussion 2026-05-29 11:06）。

### Background-cost チェック（`scripts/bg_cost_spread.py`）

vertices_merged_v6 から broad-catalog の n_tracks_max 8–10 頂点を 80 サンプル
（seed=7、63 が usable）、アンカー angle_spread を R=25（tight）vs R=50
（T011 回収半径）で再計算、2026-05-29 sweep と同方法。

| metric | R=25 | R=50 |
|--------|------|------|
| spread 中央値 | 29.6 | 32.2 |
| spread p90    | 38.4 | 38.2 |
| Δ(R50−R25) 中央値 / p90 | — | 2.2 / 15.4 |

- 63 のうち 27 が R=25 で sp<28；**そのうち 10 個（37%）が R=50 で ≥28 に昇格**
  （全体の 16%）。高膨張は広半径で膨らんだ near-collinear background
  （sp25 0.4→28.1、1.3→38.1、1.8→27.5）。
- **結論: R=50 のグローバル採用は不可** ― crossing/parallel background を
  カット越えに昇格させる。tight 半径を維持。

global コストなしで T011 を回収できる理由: 真の頂点アンカーで T011 は R=25 で
既に 28.5°。catalog sp が 12.7° だったのは 25px clustering が星をずれた
sub-vertex に分割したから。修正は真の頂点近傍の targeted sub-vertex merge
（tight 半径で回収、background は不変）で、global な半径変更ではない。

### T004 z-persistence

GT（z_slice 100）周辺の slice 92–108 を掃引。GT に最も近い頂点（dist ≤32px）は
全 slice で低 sp（dist ≤18px で sp 2.5–7.4；slice 102 の 14.0 は nearest が
271px 離れているため）。sp~32 の頂点は GT から 100–200px 離れた別構造で GT
頂点ではない。low-sp core は z 近傍全体で持続: **T004 は robust な graph-branch
candidate**（物理ラベルは専門家待ち）。

### scoring スレッドの結論（コード cleanup の前）

1. Hypernuclear-recall ranking = `sp`（nsl 乗数なし；nsl≥4 floor のみ）
   [2026-05-28 決定]。
2. spread-association 半径は tight（R=25）維持；global に広げない。
3. T011 型 fragmentation → targeted sub-vertex merge、cleanup 後の Hough-branch
   実装に先送り（今はやらない）。
4. D013 は low-sp 集合から除外；T004 = graph-branch candidate。

次: コード cleanup（branch-neutral preprocess 抽出 + server dedup + 診断
packaging）、behavior-preserving な別タスクとして旧/新の回帰テスト付きで。

---

## 2026-05-29 — 訂正: T004 の low-sp は algorithmic、物理ラベルは保留

2026-05-28/05-29 の low-sp エントリへの訂正（append-only 方針により上記は
残し、本エントリで限定する）。Codex の指摘（discussion 2026-05-28 22:06）を
受け、T004 を「forward-boosted topology」/ sigma-stop と記述したのは
コードが示す内容を超えていた。

訂正後の立場:

- **事実（コードから）**: T004 の clicked GT 頂点は tolerance 内に検出される
  が angle spread は低いまま（直近 ~2.5°、R≥100 で遠方トラックを取り込んで
  ようやく ~22°）、sp=28 quality cut をきれいに超えない。Hough scalar 表現
  における真性な low-sp core で、T011 の clustering-fragmentation artifact
  とは異なる。algorithmically、3つのうち T004 が graph/topology-branch
  candidate。
- **専門家に保留（コードからでない）**: その low-sp core が物理的に
  sigma-stop / forward-boosted ハイパー核トポロジーかどうかは乳剤物理の
  解釈であり、コード由来の事実ではない。ユーザー / ドメイン専門家への質問
  として flag、断定しない。

---

## 2026-05-29 — T011 spread-recovery テスト: 断片化を確認

2026-05-28 の low-sp diagnostic で提案した Hough-branch 修正を検証: T011 の
低い頂点 spread は clustering-fragmentation の artifact か? GT スライスで GT
に最も近い検出頂点をアンカーとし、endpoint-association 半径 R を掃引、最近傍
端点が R 内のトラックで angle_spread を再計算。スクリプト:
`scripts/lowsp_spread_radius.py`、プロット
`results/lowsp_diag/spread_vs_radius.png`。batch 関数を直接呼出。

| event | 検出 sp | R=25 | R=50 | R=75 | R=100 | R=150 | R=200 |
|-------|---------|------|------|------|-------|-------|-------|
| T011  | 16.7    | 28.5 | 34.3 | 34.6 | 32.5  | 33.1  | 32.4  |
| T004  | 2.5     | 3.1  | 3.7  | 5.6  | 21.5  | 24.2  | 22.6  |
| D013  | 31.8    | 29.2 | 27.2 | 31.7 | 32.3  | 33.1  | 30.1  |

### 所見

- **T011: 断片化 artifact、完全に回収可能。** R=25 で 28.5°、R=50 で ~34°
  に達する（検出 scalar sp は 12.7–16.7°）。真の多飛跡星は頂点直上にあり、
  25px の交点 clustering がそれを共線 sub-vertex に分割した。広い spread
  半径で Hough branch 内で回収できる ― T011 に graph 作業は不要。
- **T004: 真性な共線 core。** R≤75 で 3–6°、R≥100 で遠方トラックを取り込んで
  ようやく ~22° だが sp=28 cut をきれいに超えない。直近頂点は本当に共線
  （forward-boosted）― 真の graph-branch candidate。
- **D013: low-sp でない**（どの半径でも ≥27°）― positive control。

総じて、3つの「low-sp」specials のうち D013 は wrong-vertex の誤ラベル、T011
は広い spread 半径で回収可能、真性 low-sp core は T004 のみ。low-sp 事象の
recall 懸念は大半が測定 artifact。

### 広い spread 半径を採用する前の注意

このテストは広半径 spread が signal を回収する（T011）ことを示すが、コストは
未検証: 全体で広げると crossing-track / background 頂点の spread も上がり、
sp-recall purity を損ないうる。signal 側は検証済み、background 側は採用前に
catalog レベルの確認が必要。

### 次のステップ

- background コストの測定: broad-catalog の n=8–10 頂点サンプルで広半径
  spread を再計算し、sp=28 を超える数を数える。広半径が background spread を
  ひどく膨らませるなら tight 半径を維持。
- T004: graph-branch candidate（Codex 確認 / 他 z での再確認待ち）。

---

## 2026-05-28 — low-sp specials failure-mode diagnostic

3つの low-sp 確認事象（T011/T004/D013）について、clicked GT 頂点で
conventional Hough branch を段階的にウォークし、低い angle spread が
preprocessing/抽出/association の失敗か、それとも真のトポロジー限界かを判定
した。batch 関数を直接呼出、設計は Codex と合意（discussion 2026-05-28
19:04/19:11）。スクリプト: `scripts/lowsp_diag.py`、crops:
`results/lowsp_diag/`。find_tracks は v6 config（hough_ml=30）、find_vertices
既定（min_angle_spread=0）、±12 スライスでマージ、2半径（200/300px）。

| event | fg@R200 | endpts/body in R200 | near-GT spread R200/R300 | single vtx | merged vtx (±12) |
|-------|---------|---------------------|--------------------------|------------|------------------|
| T011  | 6.4%    | 38 / 38             | 32.4° / 31.6°            | d=3px n=5 sp=16.7 | d=10px n=10 nsl=8 sp=12.7 |
| T004  | 6.0%    | 17 / 17             | 22.6° / 34.0°            | d=2px n=6 sp=2.5  | d=11px n=10 nsl=10 sp=8.2 |
| D013  | 7.4%    | 48 / 48             | 29.8° / 29.0°            | d=14px n=13 sp=31.8 | d=9px n=13 nsl=12 sp=31.8 |

### 所見

- **3つとも cat 1/2/3 の hard failure はない。** 構造は preprocessing を生存
  （GT で fg 6–7%）、Hough 線は GT で端点サポート付きに抽出
  （endpoints_in == body_in → through-going でない；min_body 0.4–7.5px）、
  頂点も tolerance 内に形成（2–14px）。low-sp 問題は spread/scoring 段で
  あって、画像/Hough/association 存在の段ではない。

- **D013 は実際には low-sp でない。** GT 頂点はきれいに検出（sp=31.8°、
  n=13、nsl=12）、sp-recall で問題なく上位。旧「D013 sp=13.4°」
  （2026-05-10）は*別の* best-n 頂点を測ったもので、真の GT 頂点ではない。
  D013 は low-sp 問題集合から外れる。

- **T011 は断片化/under-association の artifact。** crop は GT に明確な
  多飛跡星を示し、近傍の端点サポート付き線は 32° に広がるが、検出頂点 sp は
  12.7° のみ。25px clustering（eps_px）+ endpoint cut が真の星をより共線な
  sub-vertex に分割している。diversity は画像に存在するが scalar 頂点 sp が
  取りこぼす。Hough branch 内で回収可能とみられる。

- **T004 は真性な low-sp core。** 直近頂点 sp=2.5°（single）/8.2°（merged）、
  near-GT spread は R200 で 22.6°、R300 で 34° に広がる ― 共線に近い core に
  大きな半径で prong（forward-boosted トポロジー）。本当の graph-branch
  candidate。

### 含意

low-sp specials の recall 懸念は、大半が測定/断片化の artifact（T011, D013）
であって、Hough 表現の根本的限界ではない。graph branch が明確に要るのは
T004 のみ。

### 次のステップ

- Hough-branch テスト（graph 着手前）: 頂点 angle_spread をより広い
  endpoint-association 半径で再計算する、または scoring 前に GT tolerance
  程度で隣接 sub-vertex をマージし、T011 が 32° の near-GT spread に向けて
  回収する一方 T004 は低いままか確認。T011 が回収するなら sp-recall ranking
  が graph branch なしで拾える。

---

## 2026-05-28 — Step-5 互換性チェック: specials_x20 vs fullscan-image

specials_x20 を conventional Hough branch の sanity-check anchor として使える
かを、両ソースを*同じ step-5 preprocessing 後*（Hough/graph 分岐前の共有境界）
で比較して検証した。2026-05-28 の scope lock に従い、**visual-review server は
使わず、batch の `e07fullscan.tracking._finder.preprocess()` を直接呼んだ**ので、
統計は batch パイプラインが見るものと完全一致（noise_amax_upper=0）。スクリプト:
`scripts/step5_compat.py`、出力: `results/step5_compat/`。

ソース: fullscan view V00001173（KISO カタログマッチを含む）、KISO、T011
（最小の low-sp special）。各々で find_tracks の中心スライス ±4 平均投影を
preprocess() に通した。

| metric                 | fullscan V00001173 | KISO        | T011         |
|------------------------|--------------------|-------------|--------------|
| shape / dtype          | 2048² uint8        | same        | same         |
| n_slices               | 58                 | 60          | 50           |
| dz (µm/slice)          | 3.00               | 3.00        | 3.00         |
| px scale (µm)          | 0.29 (config)      | 0.289       | 0.289        |
| raw proj mean / std    | 182.5 / 39.3       | 98.0 / 54.7 | 145.6 / 19.8 |
| post-step5 前景率      | 7.27%              | 6.64%       | 4.17%        |
| CC count               | 2548               | 1353        | 1532         |
| CC area 中央値 (px²)   | 62                 | 125         | 55           |

### 所見

- **幾何はソース間で完全一致**（2048² uint8、3.0 µm/slice、0.289 µm/px）。
  （注: fullscan view JSON は identity AffineP2S を持ち、物理スケールは config
  の 0.29 µm/px に由来。）
- **生輝度は大きく異なる**（取得時の露光/コントラスト、NLAB-PC13 vs PC06）―
  ただし保守的には、これだけで specials_x20 を参照データとして失格にしない。
- **step-5 後に表現が収束**: 前景率はすべて 4〜7% 帯に収まり、connected-
  component の count/area も同オーダー、トラック様構造が3ソースすべてで視覚的に
  生存（montage.png）。正規化機序は fog removal（GaussianBlur − img）に続く
  画像ごとの Otsu で、閾値が適応し生輝度差を吸収する。

### 結論

specials_x20 は*同じ step-5 preprocessing 後*なら conventional Hough branch の
sanity-check anchor として利用可能。定性/sanity 用途では step-5 を超える正規化
は不要。expert への未解決質問: NLAB-PC06 vs PC13 の光学/照明/カメラ等価性 ―
だが主な懸念（生輝度）は step-5 が吸収する。

### dormant な重複についての注記（debris チューニング前に修正）

コードレビュー（discussion 2026-05-28）で、`server/app.py` が
`_process()`/`_collect_stats()` で fog/Otsu/noise を再実装し、
`tracking.preprocess()` にある **`noise_amax_upper` 分岐を欠く**ことが判明。
`noise_amax_upper = 0` のため現状は無害だが、大型ブロブ除去を有効化した瞬間に
viewer が batch より under-clean になる。branch-neutral な preprocessing
モジュールを抽出し tracking と server の両方が呼ぶことで合意（Codex/Claude）。
これは scoring/compatibility と混ぜず、behavior-preserving な別タスク
（旧/新の回帰テスト付き）として実施する。

### 次のステップ

- low-sp specials（T011/T004/D013）: clicked GT 頂点周辺での bounded Hough
  failure-mode diagnostic（preprocessing でトラック消失 / Hough 線抽出失敗 /
  頂点マージ失敗 / 頂点はあるが scalar score 低）。

---

## 2026-05-28 — スコア式の定量化; 2チャンネルranking決定

2026-05-27に提案したscore代替案を、fullscan plate range内にある唯一の
special（KISO）に対して定量化し、recall-first preprocessing段の
ranking scoreを決定した。議論: discussion_ja.md 2026-05-28 15:48–17:01 JST。

### 各scoreでのKISO rank（vertices_quality_v6、N=10,750）

KISO anchor = V00001173内の最近傍マッチ（sp=41.4°、nsl=7、n=9）。

| Score              | KISO rank   | パーセンタイル | top-500必要条件 |
|--------------------|-------------|------------|---------------|
| `sp` のみ          | **798**     | 7.4%       | nsl≥4         |
| `sp × sqrt(nsl)`   | 4,475       | 41.6%      | nsl≥12        |
| `sp × log(nsl)`    | 4,227       | 39.3%      | nsl≥11        |
| `sp × min(nsl,10)` | 5,947       | 55.3%      | nsl≥10        |
| `sp × nsl`（従来）  | 6,188       | 57.6%      | nsl≥14        |

### 重要な発見: nslのcap/減衰はKISOを救済しない

傾いていた `sp × min(nsl,10)` はほぼ効かない（rank 5,947 対 6,188）。
KISOのnsl=7はcap値10を*下回る*ためmin(7,10)=7で恩恵ゼロ；capはnsl>10の
頂点（全体の35%）をdampするだけ。nslを掛けるscoreはすべて、z深さ方向に
中程度しか広がらない真の局所反応頂点にペナルティを与える。rankingから
nslを外す（`sp`のみ）場合のみ、KISOが使えるcandidate budget（top-800）に
入る。

nslを外しても浅いartefactでlistは埋まらない: sp≥41.4°のpool（816頂点）は
n_slicesが健全に分散（4-7: 24%、8-10: 33%、11-13: 24%、≥14: 19%）し、
n_tracks_max≥17は4%のみ（nsl≥4のquality floorが既に適用済みのため）。

### 2リスト診断（persistence biasの可視化）

- `hough_recall_sp`    = `sp` でrank、nsl≥4はfloorのみ
- `hough_broad_sp_nsl` = `sp × nsl` でrank（従来の唯一ranking）

top-N重複: 500→15%、1000→23%、2000→36%。top-500の構成:

| feature           | recall_sp | broad_sp_nsl |
|-------------------|-----------|--------------|
| sp 中央値          | 43.2      | 38.2         |
| nsl 中央値         | 10        | 18           |
| nsl ≥14           | 19%       | **100%**     |
| n_tracks_max ≥17  | **4%**    | **14%**      |

`broad_sp_nsl` top-500はnsl≥14で完全飽和し、n≥17のbackground-rich層を14%
含む；`recall_sp`はnsl分布がバランス良く、重粒子star混入もはるかに少ない。

### 決定: 両者を別々のranked viewとして維持（置換ではない）

Codex/Claude共同合意（discussion 17:01 JST）:

- **`hough_recall_sp`** — `sp`主導、nsl≥4はfloorのみ、n≥17はbackground
  flag → このrecall-first段の**hypernuclear-recallルート**。nsl乗数は
  （弱いものでも）加えない。nslこそここで避けたいbiasの源だから。
- **`hough_broad_sp_nsl`** — 従来の `sp × nsl` ranking。broad
  reaction-like / 重い核star のサーベイ用に維持。

これにより2026-05-27の結論（「sp×nslを今後のranking scoreとして確認」）は
*broad* channelのみに**限定**され、唯一のrankingではなくなる。
（過去のdiary entryはlab-notebook方針に従いそのまま残す。）

### 次のステップ

- 新規の大規模crop生成より先に、step-5（noise-removal）互換性artifact:
  fullscan 1 view + KISO + T011（最小のlow-sp special）を
  `e07fullscan/tracking/_finder.py::preprocess()` に通し、post-noiseの
  前景率、connected-componentの面積分位、matched projectionを比較。
  解釈は保守的に ― raw輝度の不一致だけでspecials_x20を失格にしない。
- low-sp specials（T011/T004/D013）: graph-branch判断の前に、clicked GT
  頂点周辺でbounded Hough failure-mode diagnostic（4カテゴリ）。

---

## 2026-05-27 21:31 JST — 博士論文に基づくranking review

ユーザーが関連する博士論文 `S.H.Hayakawa_D.pdf` を追加した。Codexは現在の
vertex preprocessing議論に関係するChapter 4とChapter 5を確認した。

重要な解釈: 論文中のevent categorizationは、単一のstar-like scoreではなく、
topologyとtrack contextを組み合わせている。具体的にはincoming trackが
distortedかstraightか、charged-particle emissionがあるか、beam trackが
vertexに見えるかを使っている。Hypernuclear productionは `sigma-stop`、
つまりendpoint近傍で乱れたstopping negative trackとcharged-particle
emissionとして分類されており、単なる高multiplicity starではない。

これは現在のrecall-first preprocessing方針を支持する。Hough-based featureは
広いimage retrievalには有用だが、物理的に重要なendpoint contextをまだ持って
いない。高い `n_tracks_max`、高い `n_slices`、見た目に強いnuclear starを
唯一のrankingで支配的にすると、KISOのような既知ハイパー核事象を落とす
危険がある。

次のalgorithmic direction: broad reaction-like、hypernuclear-recall、
background-rich reserveの複数preprocessing channelを維持する。その上で、
candidate vertices、track endpoints、track-segment edges、incoming-trackの
straightness/distortion、outgoing prongs、beam-track evidence、nearby secondary
verticesを含むgraph/topology表現へ進む。

---

## 2026-05-27 10:28 JST — ランキング変更: n_tracks_max → sp×nsl; 目視検査

### Dead end: n_tracks_max ランキング

`n_tracks_max` だけで並べると逆効果だった。
top-500（`vertex_crops_v6/`）はビームパイルアップや重粒子フェイクに
支配されていた（n_tracks_max: 82→15）。
KISOの最近傍候補（n=8, sp=36°, nsl=9）はtop-500に含まれていなかった。

### 新ランキング: score = angle_spread_best × n_slices

Codex/Claude合同議論（2026-05-14 21:56 JST）を経て、
`score = angle_spread_best × n_slices` を採用した。理由:
- 多方向への広がり（真の多プロング星型トポロジー）を評価する
- z方向での再現性（単層アーチファクトでない）を評価する
- 高トラック多重度を直接報酬せず、重粒子バイアスを回避する

新クロップセット: `results/vertex_crops_v6_sp_nsl/`（500枚）。
コマンド: `crop_vertices.py --sort-by sp_nsl --n-samples 500`。

### 目視検査結果（2026-05-27）

ユーザーが500枚全てを確認。旧n_tracks_maxランキングから**劇的に改善**:

- 反応点らしい画像（多プロング星型）が大幅に増加
- 残存バックグラウンドは2種類:
  1. **大きなゴミ・グリッドポイント**: 乳剤アーチファクトまたは
     スキャナグリッドが誤って頂点として検出されたもの
  2. **無関係な交差飛跡**: 無関係な2本の飛跡が交差しているだけで、
     物理的な反応点ではないもの

**結論**: sp×nsl を今後のランキングスコアとして正式採用。

### 次のステップ

- ゴミ・交差飛跡バックグラウンドを追加カット
  （例: 2本の最大離角、等方性指標）で抑制できるか検討する
- 目視検査結果からラベル付きカタログを生成する
- `specials_x20/` は `../specials_x20` へのシンボリックリンク（9事象の
  確認済みスペシャルイベントの外部参照画像データ、読み取り専用、
  パイプライン生成物ではない — 2026-05-27 discussion にて解決済み）

---

## 2026-05-14 — 方針転換：単体頂点検出へ

これまでのパイプラインは ΛΛ Primary/Secondary ペアの探索に特化していた。
ユーザーの指示により、ペア位相構造の要件を廃止し、個々の反応頂点を
直接検出する方向に転換した（原子核星、α崩壊、single Λ、ΛΛ primaryなど、
乳剤中に可視化されるあらゆる頂点トポロジーが対象）。

動機：ペア位相構造は特定のシグナルモデルを前提とし、それ以外の頂点を
検出できない。直接的な頂点カタログを作ることで、乳剤イベントのより
完全な全体像が得られる。

### マージ済み頂点への品質カット

`results/vertices_merged_v6.parquet`（237,029件）に以下のカットを適用：

| カット | 値 |
|-------|-----|
| n_tracks_max | ≥ 8 |
| angle_spread_best | ≥ 28° |
| n_slices | ≥ 4 |

出力：`results/vertices_quality_v6.parquet` — **10,750件**。

### クロップ生成

n_tracks_max 降順で上位500候補の画像クロップを生成。
出力ディレクトリ：`results/vertex_crops_v6/`（PNG 500枚）。

カバー範囲：n_tracks_max = 82 → 15、全45×45ビュー領域にわたる。

ファイル命名規則：
```
NNN_V{view_id}_L0_VX{vx}_VY{vy}_..._n{ntracks}_sl{nslices}_z0_x{px}_y{py}.png
```

次ステップ：500枚のクロップを目視確認し、バックグラウンドの混入状況を
評価するとともに、詳細測定候補の頂点を特定する。

---

## 2026-05-14 — discussion 監視ルールを agent ルールへ追加

ユーザーから、discussion log の監視ルールを `AGENTS.md` と `CLAUDE.md` に
反映するよう依頼があった。両ファイルに Agent Coordination セクションを
追加し、リポジトリ作業前、共有ファイル編集前、最終報告前に
`discussion.md` と `discussion_ja.md` の両方を確認することを必須化した。

このルールでは、両 discussion log を追記専用として扱い、ジョブ実行、
出力生成、スクリプト変更の前に、入力、出力、生成ディレクトリ、担当ファイルを
記録するよう定めた。README も更新し、この運用が必須ルールになったことを
明記した。

---

## 2026-05-14 — 日本語版 discussion log の追加

ユーザーの依頼を受けて、`discussion.md` に対応する日本語版として
`discussion_ja.md` を作成した。内容は Codex から Claude への初回調整
メッセージを日本語で反映し、現在の作業内容、仮定、編集中ファイル、
予定している出力ファイル名を追記するよう依頼している。

README の調整ルールも更新し、英語版 `discussion.md` と日本語版
`discussion_ja.md` の両方を明記した。これにより、英語の簡潔な
エージェント間ハンドオフを残しつつ、日本語メモをリポジトリ指示に沿った
専用の場所で管理できる。

---

## 2026-05-14 — 並行エージェント用の共有議論ログ

Claude Code も同じリポジトリで実行中のため、`discussion.md` を追記専用の
共有調整ログとして導入した。目的は、作業中の仮定、編集中のファイル、
未解決の論点を見える形にしつつ、`ANALYSIS.md` と `ANALYSIS_ja.md` の
時系列日誌形式を維持することである。

Codex から Claude への初回メモでは、作業ツリーが既に dirty であることを
確認し、v6 クロスビュー候補数の過剰増加について、低い Hough 最小長の影響、
隣接ビュー判定、座標/インデックス規約のどれが主因かを論点として提示した。
また、スクリプト変更前に意図する出力ファイル名を記録し、中間生成物の上書きを
避ける運用を提案した。

---

## 2026-05-14 — v6パイプライン：イントラビュー・クロスビューペア探索

### 背景

v6トラッキングはhough_ml=30（正しい値、v5のhough_ml=50は誤り）を使用し、
70.8Mトラック（v5の24Mの3倍）を生成。頂点統合により237,029頂点（+11%）。

### イントラビューペア（vertex_pairs_v6）

実行結果：**1,851,442ペア**（v7の1,479,220より+25%）。
次ステップ：接続トラックフィルター適用（v7パイプラインと同様）。

### クロスビューペア（vertex_pairs_xview_v6）

- 生成：23,474,643ペア（90–500μm、dz≤0.200mm）
- v1と同じ事前フィルター適用後：2,219,749ペア（v1の109,376の20倍）
  - 要因不明：primary1件あたりの候補数がv6では72.1、v1では4.7
- 境界通過フィルターへの適用が困難なため、追加の絞り込みを実施：
  - p_sp≥35°、p_n≥8、d≤400μmで**204,405ペア**に削減
  - KISOは通過（p_sp=42°>35°、p_n=11>8、d=152μm<400μm）
- 境界通過フィルター実行中（2026-05-14時点）

生成ファイル：
- `results/vertex_pairs_xview_v6.parquet`: 23,474,643件
- `results/vertex_pairs_xview_v6_filtered.parquet`: 2,219,749件
- `results/vertex_pairs_xview_v6_prefiltered.parquet`: 204,405件
- `results/vertex_pairs_xview_v6_conn.parquet`: 実行中

---

## 2026-05-14 — クロスビュー接続トラックフィルター（filter_xview_pairs.py）

### 方法

`scripts/filter_xview_pairs.py` がビュー境界通過トラックを確認:
- **Primary view**: P頂点から60px以内の端点を持ち、もう一方の端点が
  secondary方向のビュー端（300px以内）に近いトラック
- **Secondary view**: S頂点から60px以内の端点を持ち、もう一方の端点が
  primary方向のビュー端（300px以内）に近いトラック

VX/VYインデックス差からビュー端方向を決定:
高VX ↔ 高stage_x ↔ 低pixel_x (Convention C)

### 結果

| 段階 | 件数 |
|------|------|
| 事前カット後 | 29,408 |
| 境界通過フィルター後 | 2,986 |
| ΛΛ範囲（p_n≤20, s_n≤15）後 | 2,952 |
| 強候補（s_n≥5, p_sp≥30°, s_sp≥22°, d≤230μm）| 1,596 |

KISO真ペア（P=(354,1204) n=11 sp=42° → S=(1888,716) n=5 sp=23°,
d=152μm, dz=0.026mm）は全段階を通過 ✓（ΛΛ-range内順位: 988/2952）

出力ファイル:
- `results/vertex_pairs_xview_v1_conn.parquet`: 2,986件
- `results/vertex_pairs_xview_v1_conn_ll.parquet`: 2,952件 ΛΛ範囲
- `results/vertex_pairs_xview_v1_strong.parquet`: 1,596件 強候補

---

## 2026-05-14 — 接続トラックアノテーションと Tier A 候補選定

### 接続トラックプロパティのアノテーション

`scripts/annotate_pairs.py`（許容値50px）で123件の強候補全てにアノテーションを付与:

- `conn_mean_intens`: 接続トラックの平均強度
- `conn_grain_density`: 接続トラックの粒子密度
- `conn_angle_diff`: トラック方向とP→S軸との角度差
- `conn_len_ratio`: トラック長 / dist_px

主要な知見:
- `conn_angle_diff` ≈ 0° (全候補でほぼ0° — 弁別子として使えない)
- `conn_mean_intens`: 平均16.8、std=6.1。35以上は2件のみ
  (rank2: 37.1, rank11: 39.8)
- 視覚的偽陽性(rank1,4,5)のintensは普通(25.5, 13.9, 12.4) —
  重粒子トラックが複数の短セグメントに分割されるため、
  接続トラック強度単独では重粒子フォールスポジティブを識別できない

### Tier A / B / C 分類

| Tier | 基準 | 件数 |
|------|------|------|
| A | s_n≥8, p_sp≥30°, s_sp≥28°, d=90-400μm, I<38 | 25 |
| B | s_n≥6, p_sp≥28°, s_sp≥25°, d=90-500μm | 53 |
| C | 残り | 45 |

Tier A は既知specials(s_n=9-13, 両sp>25°)に最も近いパラメータ範囲。
hough_ml=50切り捨てによりトラック数が低めなのでs_n≥8を使用。

出力ファイル:
- `results/vertex_pairs_v7_strong_ann.parquet`: 123件(接続トラック情報付き)
- `results/vertex_pairs_v7_tier_a.parquet`: 25件のTier A候補
- `results/pair_crops_v7_ann/`: 123枚アノテーション付きPNG (I=印字済み)
- `results/pair_crops_v7_tier_a/`: 25枚のTier A優先検査用クロップ

### 次のステップ
1. `pair_crops_v7_tier_a/` の25枚を視覚検査
2. cross-viewペア(109,376件)への接続トラックフィルター適用
   (Ξ⁻トラックがビュー境界をまたぐ: primaryは右端に、secondaryは左端から)
3. KEKCC hough_ml=30での再実行（全スキャン対応）

## 2026-05-14 — n順序バグ修正；全9 specials 検出；cross-view フィルタ研究

### バグ修正: `find_vertex_pairs` n順序制約の削除

`e07fullscan/clustering/_vertex.py` の `find_vertex_pairs` 関数から
`if nt[pi] < nt[si]: continue` を削除した。

**背景:** 元々の意図は「一次頂点は二次頂点より多いトラック数を持つ」を強制することで、
物理的には Ξ⁻ 停止星が Λ崩壊より多くのプロングを持つことに基づいていた。
**なぜ間違っていたか:** 視野境界 (KISO) や多体崩壊では、二次頂点が切断された一次頂点より
多くのトラックを持つことがある。制約の削除は安全。ロールラベル（一次/二次）は
相対順序ではなくトポロジー（n_tracks 閾値）で定義。

**影響:** v7 ペアを正しい距離範囲 (90-500 μm) で再生成: **1,479,220 intra-view ペア**
(v6: 1,200,346 から +23%、P.n<S.n ペアがアンブロックされたため)。

### Specials パイプラインテスト: 全9イベント検出

全9つの confirmed specials_x20 イベントに対してフルパイプライン実行。
全イベントで候補ペアが生成された。

| イベント | 頂点数 | n_max | ペア数 | 備考 |
|---------|-------|-------|-------|------|
| D005 | 358 | 15 | 48,887 | — |
| D013 | 331 | 15 | 27,367 | — |
| IBUKI | 281 | 14 | 24,170 | — |
| IRRAWADY | 158 | 11 | 4,828 | — |
| KISO | 192 | 11 | 9,491 | 真のペア発見 |
| MINO | 304 | 19 | 33,562 | — |
| NAGARA | 221 | 9 | 12,236 | — |
| T004 | 380 | 14 | 47,555 | — |
| T011 | 135 | 14 | 3,919 | — |

**KISO 真のペアが specials 画像で回復:**
```
P=(1108,1090) n=6 nsl=4  z=-0.225 mm   (gt一次から 62 px)
S=(751,1589)  n=9 nsl=13 z=-0.211 mm   (gt二次から 11 px)
dist = 178 μm   (期待 194 μm、誤差 8%)
dz   = 0.014 mm
```
n順序修正前はこのペアがブロックされていた (P.n=6 < S.n=9)。

**課題:** 各 specials 画像で 4,000-50,000 ペアが生成される。
真のペアはバックグラウンドに埋もれており、ランキングが必要。

### フルスキャン: KISO のみが現在の走査範囲内

頂点カタログ (`vertices_merged_v5.parquet`) は x=[−0.001, 22.001] mm,
y=[0.002, 22.000] mm の 22×22 mm² エリアをカバー。9 specials 中
**KISO のみ**がこのエリア内 (view_x=1.75, view_y=12.88 mm)。
他の8つは走査境界から 58 mm 以上離れている。

KISO はフルスキャン cross-view ペアカタログで確認:
```
P = V00001173 (354,1204) n=11 nsl=7 sp=42.3°
S = V00001174 (1888,716) n=5  nsl=6 sp=23.5°
dist = 152 μm  (期待 194 μm、誤差 22%)
dz   = 0.026 mm
```
22% の距離誤差は v5 カタログが hough_ml=50 で構築されたため。
hough_ml=30 での再実行により改善が期待される。

### Cross-view ペアバックグラウンド抑制

18.8M cross-view ペアに段階的カットを適用:

| カット | ペア数 | KISO |
|-------|-------|------|
| 全件 | 18,822,640 | 132 |
| 隣接ビューのみ | 14,800,258 | 132 |
| + p_ntracks 6-20, p_sp≥30° | 4,208,859 | 48 |
| + s_ntracks≥4, s_sp≥20° | 3,326,423 | 48 |
| + dist 90-250 μm, dz≤0.030 mm | 211,692 | 8 |
| + p_nslices≥5, s_nslices≥4 | **109,376** | **7** |

KISO は全カットを生き残る。7つの KISO 候補は真のペアと6つのバックグラウンドを含む。
真のペアは p_ntracks×p_angle_spread の組み合わせが最大。

### v5 ペア: 致命的エラー (誤った距離範囲)

`vertex_pairs_v5.parquet` が d_min=30 px (8.7 μm)、d_max=167 px (48 μm)
(hough_ml の値を誤って使用) で生成されていたことを発見。v5 ペアは ΛΛ 解析に使えない。
v6・v7 が正しい 90-500 μm 範囲を使用。

### Intra-view 接続ペア: v6_filtered

`filter_pairs_by_track.py` を v6 ペアに適用: 1.2M から **97 ペア**が生存。
p_ntracks 10-19、p_sp 20-45°、dist 90-307 μm。KISO は含まれない (cross-view)。
この 97 ペアが視覚検査の最優先候補。

### アクション項目 (更新)

- [x] `find_vertex_pairs` の n順序制約を修正 (2026-05-14)
- [x] n順序修正で v7 intra-view ペアを再生成
- [x] 修正後に KISO が specials 画像で検出されることを確認
- [x] v5 距離範囲バグを文書化
- [x] Cross-view フィルタ研究: 18.8M → 109K (KISO 生存)
- [ ] KEKCC で hough_ml=30 によりフルパイプライン再実行
- [ ] v6 カタログで find_crossview_pairs.py を実行
- [ ] Cross-view ペアに接続トラックフィルタを適用
- [ ] 97 intra-view 接続ペアの視覚検査
- [ ] 非 KISO specials の座標オフセット決定

### v7_filtered: 接続トラックフィルタの結果

`filter_pairs_by_track.py` を v7 (min_n_primary=10、90-500μm) に適用:

```
v7_filtered:  540 ペア  (min_n_primary=10, tol=50px)
  ΛΛ範囲 (p_n:6-20, s_n:3-15, sp>=20):  398 ペア (368 ユニーク)
    ─ v6_filtered と共通:  97 ペア  ← 全 v6 候補が回収された ✓
    ─ n順序修正で新規:    301 ペア
  強候補 (p_sp>=30, s_sp>=25, p_n>=8, s_n>=4):  120 ユニークペア
```

v6_filtered の97ペアが全て v7_filtered に含まれる: n順序修正は既存の
候補を失わずに、新たに301ペアを追加した。

**次のステップ:** 120 強候補の視覚的検査 (crop_pairs.py) で真の ΛΛ
事象と重核相互作用バックグラウンドを区別する。

## 2026-05-13 — クロスビュー頂点ペア探索；KISO 復元

### 発見：KISO primary がビュー境界をまたぐ

KISO イベントの予測位置：primary 段階座標 (1.769, 12.883)、secondary (1.668, 13.048)。
フルスキャンの view 配置では：

| View | Center (mm) | KISO primary px | KISO secondary px |
|---|---|---|---|
| V00001173 | (1.499, 13.001) | (93, 617) — 左端付近 | (441, 1186) → 検出 (432, 1241) |
| V00001174 | (2.000, 13.001) | (1821, 617) — 右端付近 | (2169, 1186) — 範囲外 |

V00001173 では primary が px_x=93 (2048 px 幅の view の左端から 93 px)。
左方向のトラックが view 境界で切断されるため、hough_ml=50 では n≤5 しか検出されない。
V00001174 では hough_ml=30 を使うと (1854,630) n=5 sp=35.6 が期待位置から 37 px に検出。

**結論：KISO は V00001173/V00001174 の境界にまたがる cross-view イベント。**
単一 view 内の頂点ペア探索では復元できない。

### クロスビューペア探索：`scripts/find_crossview_pairs.py`

新スクリプトを実装：
1. Convention C で各頂点の stage 座標 (mm) を計算
2. stage 座標で cKDTree を構築
3. 各 primary 候補（n ≥ min_n_primary）について、**異なる view** の頂点を d_min–d_max mm 範囲で探索
4. Z 方向分離カット (max_dz_mm; デフォルト 0.200 mm)

重要な変更点：cross-view では物理 primary が view 境界で弱く見えるため
「primary の n ≥ secondary の n」制約を削除。
また max_dz_mm を 0.010 mm (intra-view) から 0.200 mm に緩和
（KISO の primary-secondary dz = 70.2 μm、dip angle ~21° に対応）。

### KISO cross-view 検出結果

v5 カタログ (hough_ml=50) での結果：

```
P=(432,1241) n=6 sp=24.8  V00001173  (物理的 secondary、高 n)
S=(1888,716) n=5 sp=23.5  V00001174  (物理的 primary、切断)
dist = 171.5 μm  (期待 193 μm、誤差 11%)
dz   = 70.2 μm   (dip angle ~21° と整合)
```

**KISO は cross-view ペアとして復元された。** ただし役割が逆転
（物理 secondary の方が n が大きく「P」として出力）。距離誤差 22 μm は
頂点位置誤差（primary ~35 μm + secondary ~16 μm）から生じる。

hough_ml=30 使用時、V00001174 の primary 候補は (1854,630) n=5 sp=35.6 に改善
→ cross-view 距離 = **198.0 μm**（期待 193 μm、誤差 2.6%）

### 設定変更：hough_ml 50 → 30

`config/default.yaml` を更新：`hough_ml: 30`（0.29 μm/px で 8.7 μm）。
表面頂点や view 境界 primary の短いトラックセグメント検出が改善する。
全パイプライン（2025 view × 58 スライス）の KEKCC 再実行が必要。

### クロスビューの背景とスケール

v5 カタログに対して min_n=5/3、d=90–500 μm、max_dz=200 μm で実行すると
18,822,640 cross-view ペア。系統的背景が観測される：
(px≈430,1240) と adjacent view の (px≈1890,720) のペアが多くの view 行で繰り返す
（d≈165–177 μm）。これは view 境界をまたぐ重粒子トラック（ビーム粒子等）が
両側に偽の star-vertex を作るため。
抑制策：max(n_p,n_s)≥10 かつ min(n_p,n_s)≥6 かつ両 sp≥30° → ~67,717 ペア。

### アクションアイテム（更新）

- [x] hough_min_line を 30 px に変更（config/default.yaml 更新済み）
- [ ] KEKCC で hough_ml=30 にてパイプライン全再実行
- [ ] v6 カタログで `find_crossview_pairs.py` を実行して cross-view ΛΛ ペアを探索
- [ ] 非 KISO specials の座標オフセット決定
- [ ] cross-view ペアの背景抑制（n, sp カット）開発
- [ ] KISO 検証比較画像（specials vs フルスキャン crop）実装

---

## 2026-05-13 — ピクセルスケール修正・v6パイプライン・KISO specials調査

### 重大バグ: ピクセルスケールが10倍誤っていた

`_vertex.py` 内の定数 `PX_SCALE = 3.0` μm/px は10倍誤り。正しい値はスキャナJSON
（`AffineP2S = [0.00028889, ...]`）とスキャン形状（2048 px × 0.29 = 594 μm ≈ 0.5 mm
FOV間隔）から確認された **0.29 μm/px**。

影響: 旧v5ペア探索は d=30–167 px ≈ 9–48 μm でΛΛ事象の期待値（90–500 μm）と
比べてスケールが1桁小さかった。v5の全"golden"候補は誤ったスケールに基づいていた。

修正: `_PX_SCALE_UM = 0.29`, `_D_MIN_PX = 310`（90 μm）, `_D_MAX_PX = 1724`
（500 μm）を `e07fullscan/clustering/_vertex.py` に設定。

### v6パイプライン（修正済みスケールで再実行）

| ファイル | 件数 | 備考 |
|---|---|---|
| `vertex_pairs_v6.parquet` | 1,200,346 | 全ペア d=90–500 μm |
| `vertex_pairs_v6_prefilter.parquet` | 43,013 | p:10–20, s:3–8 |
| `vertex_pairs_v6_filtered.parquet` | 97 | 接続トラックフィルタ tol=30 px |

接続トラックフィルタ（P→S区間にHoughトラックを要求）は97ペアを出力したが、
すべて荷電粒子の重トラック（単一トラックが停止・散乱）。ΛΛトポロジーではなく
重粒子を選んでしまう。根本原因: Houghセグメントの99.5%が310 px未満
（z-slice 1枚≈3 μmなので1枚当たりのトラック長は50–100 px程度）。
接続トラックフィルタは**v6で廃止**し、手法を再検討する。

### 座標系の同定

KISO specials（NLAB-PC06）とfullscan（NLAB-PC13）のステージ座標を比較し、
fullscanのピクセル→ステージ変換として以下が正しいことを確認：

```
stage_x = view_cx - (px_x - 1024) × 0.00029 mm   (x軸反転)
stage_y = view_cy + (px_y - 1024) × 0.00029 mm   (y軸同方向)
```

検証: V00001173（ビュー中心 1.499, 13.001）にKISOを当てはめると:
- 一次頂点 期待ステージ(1.748, 12.882) → ピクセル(95, 617)
- 二次頂点 期待ステージ(1.668, 13.048) → ピクセル(441, 1186)
  （最近傍検出済み頂点: (432,1241) n=6, 56 px離れ ≈ 16 μm）
- P-S距離: 666 px = 193 μm → **KISOの既知距離193 μmと完全一致** ✓

### KISOスペシャルのマッチング結果

KISOは fullscanプレート範囲内にステージ座標が収まる**唯一の**スペシャル事象
（1.748, 12.882 mm）。他の specials（D005, D013, IBUKI等）は異なる顕微鏡で
取得されており、較正データなしでは座標変換ができない。

V00001173 でのKISO対応状況:
- **一次頂点(95, 617)**: z-sliceごとに6–9本のHoughライン検出（hough_ml=25では
  長さ25–50 px）。本番閾値 hough_ml=50 ではこれらが最短ライン長未満で
  **頂点検出されない**。原因: 乳剤表面近傍（z ≈ −0.076 mm, 上位2–3 slice）で
  急傾斜トラックのz断面投影が短い。
- **二次頂点(432, 1241) n=6 sp=24.8**: 検出済み・頂点カタログに収録。
  KISO二次期待位置から 56 px ≈ 16 μm。

**KISOはv6_prefilterペアに含まれていない** — 一次頂点が未検出のため。
二次頂点はv6ペアに含まれるが、他の無関係な一次と対になっているだけ。

一次頂点ミスの根本原因: `hough_min_line = 50 px`（14.5 μm）。乳剤表面付近では
3-μm z-sliceでのトラック投影長が25–40 px（傾斜角 > 11°）になり得る。
`hough_min_line` を25–30 px に下げれば検出できるはず。

副作用リスク: 短いライン閾値はノイズヒットを増やす → n_tracks/angle_spread
分布への影響を慎重にベンチマークする必要あり。

### アクション項目

- [ ] `hough_min_line`を30 pxに下げ、確認済みspecials近傍ビューで頂点再検出テスト
- [ ] KISO以外のspecialsの座標オフセット同定（較正データまたは共通特徴による）
- [ ] 接続トラックフィルタの再設計: 一次→二次方向の粒子密度カット、
      または一次頂点での方向孤立性カット
- [ ] 高傾斜角トラック対応: ライン交差ベースの頂点探索器の追加検討

---

## 2026-05-13 — 角度広がりの頂点ペア出力への伝播

### angle_spreadカラムの伝播

`merge_vertex_slices()`関数はすでに`angle_spread_best`と`angle_spread_max`を
マージ済み頂点DataFrameに保持していた（本日earlier追加）。しかし
`find_vertex_pairs()`がこれらをペア出力に渡していなかった。修正：各ペアレコードに
`p_angle_spread`と`s_angle_spread`（`angle_spread_best`を使用）を伝播するよう変更。

パイプラインをエンドツーエンドで再実行：
1. `find_pairs.py` → angle_spreadカラム付き73,751ペア
2. `filter_pairs_by_track.py`（tol=20 px）→ 25,842フィルタ済みペア
3. Golden選択 → 1,220ペア（以前と同数、spread情報付き）

### goldencandidatesのangle_spread統計

全頂点は生産カット（min_angle_spread=20°）を通過済みなので全値≥20°。分布：

| 頂点 | 平均 | 中央値 | 25パーセンタイル | 75パーセンタイル |
|------|------|--------|-----------------|-----------------|
| Primary   | 31.8° | 32.4° | 26.7° | 37.1° |
| Secondary | 29.3° | 28.9° | 24.2° | 34.0° |

Secondary頂点はPrimaryより若干低いspread（プロング数3–8 vs 10–20と整合）。
両分布は大きく重なっており、angle_spread単独では明確な識別子にならない。

goldencandidates中の低spread割合：
- p_angle_spread < 25°: 18.4%（225/1220）
- s_angle_spread < 25°: 29.4%（359/1220）
- 両方 < 25°: 5.4%（66/1220）

両方が25°以下の頂点はゴーストである可能性があり、精査が必要。

### クロップ画像再生成

`crop_pairs.py`を更新し、n_tracksラベルの横に`sp=XX°`を表示するよう変更。
Top-200 goldenクロップを`results/pair_crops_v5_golden/`に再生成（angle_spreadラベル付き）。

**未解決問題**：
- [ ] angle_spreadとs_ntracksの組み合わせでより良い背景除去ができるか？
- [ ] 両方低spread（66ペア）は本当にゴーストか、それとも整列崩壊幾何の実事象か？

### クロップ画像のCLAHEコントラスト強調

`crop_pairs.py`でアノテーション前にCLAHE（clipLimit=2.0, tileGridSize=8×8）を適用。
z投影画像に適用してトラックと頂点構造が目視確認しやすいよう局所コントラストを強調。

rank-1候補への効果: raw平均=183 → CLAHE平均=160（コントラスト拡張）。
200枚のgoldenクロップを`results/pair_crops_v5_golden/`にCLAHE付きで再生成済み。

### noise_amax_upper: 前処理での大型アーティファクト除去

`e07fullscan/tracking/_finder.py`の`preprocess()`に`noise_amax_upper`パラメータを追加。
0より大きい値を設定すると、Houghライン検出前に面積>閾値のバイナリblob を除去する。

目的：大型銀粒子クラスタ、乳剤fold、宇宙線ミューオン残留トラックを除去。
デフォルト=0（後方互換のため無効）。YAMLで設定可能：`noise_amax_upper: N`。

KENKCCでの`e07analyze`再実行が必要（将来のタスク）。

### Teacher dataの拡充

v5マージ済み頂点からteacher crop 200枚を生成（n_tracks≥8, n_slices≥4）、
`results/vertex_crops_teacher_v5/`に保存。既存60枚と合計260枚のteacher dataset。

## 2026-05-13 — v5 全スキャン vertex ラン・ΛΛ vertex ペア探索

### v5 KEKCC ラン（KDTree 修正適用）

135 チャンク全てを KDTree union-find 修正済みコードで再実行。
パラメータは v4 と同一。

| 指標 | v4 | v5 | 変化 |
|------|----|----|------|
| マージ済み vertex（min_slices=2） | 207,259 | 212,777 | +2.7% |
| n_tracks_max ≥ 5 | 102,178 | 114,089 | +11.7% |
| n_tracks_max ≥ 10 | 2,143 | 3,933 | **+83.5%** |

高多重度 vertex の大幅増加がバグ修正の効果を実証。
vertex マップ：`results/vertex_map_v5.png`。

### Z 単位の確認

`z_mean` カラムの値は **mm 単位**（スキャナステージ座標）。
- z_step ≈ 0.003 mm = 3 μm/スライス
- 全スキャン z 範囲：−0.259〜−0.048 mm（≈ 211 μm 乳剤厚）

これにより vertex ペア探索の dz フィルタは 0.010 mm (10 μm) が適切。

### ΛΛ トポロジー vertex ペア探索

`find_vertex_pairs()` と `scripts/find_pairs.py` を実装。

探索条件：
- 同一ビュー内
- XY 距離：30–167 px（90–500 μm）
- Primary：n_tracks_max ≥ min_n_primary
- Secondary：n_tracks_max ≥ 3、n_slices ≥ 2
- dz < 0.010 mm（同一乳剤深さ）

v5 での結果（min_n_primary=5）：

| n_primary カット | ペア数 | ビュー数 |
|-----------------|-------|---------|
| ≥ 5 | 95,353 | 2,025 |
| ≥ 8 | 14,760 | 1,901 |
| ≥ 10 | 5,059 | 1,153 |
| ≥ 15 | 1,423 | 250 |

p_ntracks ≥ 10 で 5,059 候補は半自動スキャンが可能な規模。
次ステップ：上位候補の 2 頂点クロップ画像を生成して目視確認。

### ゴールデン ΛΛ 候補選択

多段階物理フィルタ:
1. XY 距離: 90–500 μm
2. Primary: n_tracks_max 10–20
3. Secondary: n_tracks_max 3–8 (ΛΛ 弱崩壊星と矛盾しない)
4. 連結トラック存在 (tol=20 px)
5. スコア = `p_ntracks × s_ntracks / log(1 + dist_px)`

結果: **1,220 ゴールデン候補、709 ビュー** (`results/vertex_pairs_v5_golden.parquet`)
- 399 ビュー: 候補が 1 つだけ（最高優先度）
- 202 ビュー: 候補が 2 つ

上位候補プロファイル: p_ntracks≈16-20, s_ntracks≈6-8, dist≈150-400 μm。
次のステップ: 上位 200 クロップを目視確認して ΛΛ トポロジーを同定。

## 2026-05-13 — KDTree クラスタリング修正; パイプライン status スクリプト

### 根本原因の特定: グリッドハッシュクラスタリングのバグ

旧実装の問題点：
intersection 点を `eps_px=25px` のグリッドセルにハッシュするだけだったため、
グリッドセル境界にかかった 2 点が 24px しか離れていなくても
別クラスタに入ってしまっていた。

星型 vertex の n 本のトラックから n(n-1)/2 個の intersection が生まれても、
25px のセルをまたいで分散すると各クラスタの n_tracks が 2–3 にしかならず
`min_tracks=3` フィルタで落ちていた。

**修正**: `cKDTree.query_pairs()` + union-find（経路圧縮）に置き換え。
eps_px 以内のすべての intersection 点が必ず同じクラスタに入るようになった。

**検証結果**: production cuts のまま、全 9 specials イベントで
ground truth 200 px XY + 30 μm Z 以内に n_tracks_max ≥ 5 の merged vertex を検出：

| イベント | n | dist XY | dist Z |
|---|---|---|---|
| T011 | 8 | **2px** | 0.0μm |
| D005 | 12 | 6px | 0.0μm |
| NAGARA | 6 | 8px | 0.0μm |
| MINO | 10 | 22px | 0.0μm |
| D013 | 9 | 129px | 0.1μm |
| IBUKI | 9 | 138px | 0.0μm |
| T004 | 11 | 149px | 0.3μm |
| IRRAWADY | 7 | 160px | 0.0μm |
| KISO | 9 | 170px | 0.1μm |

### パイプライン status スクリプト

`scripts/status.py` を追加（引数なし）：
```
python scripts/status.py
```
トラック chunk 進捗・vertex マージ状態・KEKCC ジョブ状況を
1 コマンドで確認可能。

---

## 2026-05-12 — グラウンドトゥルースクリックセッション

`scripts/click_vertex.py` で 9 イベントの真の反応 vertex 位置を記録。

**重要な知見**: 全 specials イベントの真の vertex は
- XY: 画像中心付近（(1024,1024) から 72 px 以内）
- Z: ≈ 0 μm（スキャン z 範囲の中央）

`tests/specials_gt.json` に記録（XY ピクセル座標 + z_um + z_slice）。
許容範囲: 200 px XY、30 μm Z。

| イベント | 真の vertex | n_clicks |
|---|---|---|
| D005 | (1020, 1023) z=0.4μm | 1 |
| D013 | (998, 990) z=0.0μm | 1 |
| IBUKI | (982, 968) z=0.2μm | 2 |
| IRRAWADY | (1010, 1018) z=-0.3μm | 3 |
| KISO | (1096, 1028) z=-0.2μm | 3 |
| MINO | (1016, 1018) z=0.0μm | 2 |
| NAGARA | (1020, 1021) z=-0.1μm | 3 |
| T004 | (1023, 1038) z=0.4μm | 1 |
| T011 | (992, 984) z=-0.1μm | 2 |

`test_special_vertex_position` を追加：
merged vertex が ground truth の 200 px XY + 30 μm Z 以内にあるか確認。
当初はほぼ全イベントで失敗（pipeline がクロッシングを選ぶ問題）。

---

## 2026-05-11 — vertex 候補マップ; グラウンドトゥルース戦略; インデント修正

### vertex 候補マップ生成

IBUKI, D013, T004, T011 の全候補オーバーレイマップを生成
（`results/specials_crops/*_all_vertices_map.png`）。

**重要な発見（致命的）**: パイプラインが見つける最高 n の vertex 候補は
すべて **2 本のトラックの交差点** であり、真の反応 vertex ではなかった。
T011 のみ dist_center=50px で "かなり近い" 候補があった。

### グラウンドトゥルース収集戦略

`scripts/click_vertex.py` を作成：
- 全スライスの min projection を表示
- ユーザーが反応 vertex をクリック → ピクセル座標を記録
- ディレクトリ指定で自動 min projection

### コード品質: インデントルール修正

CLAUDE.md 規定の 2 スペースインデントに 13 ファイルを修正。

---

## 2026-05-10 — specials_x20 ティーチャーイベント; 統合テスト

### specials_x20 ティーチャーイベント

確認済みダブル超核（候補）イベント 13 件を
`/gpfs/group/had/sks/Users/shuhei/work/specials_x20/` に提供：

```
D005, D013, IBUKI, IRRAWADY, KISO, MINO, NAGARA,
T004, T004_3body, T004_center, T011, T011_100, T011_200
```

形式: PNG ファイル群 (`0000.png`, ...) + `image.json`。
ピクセルスケール: 0.289 μm/px（フルスキャンと同一）。
z 間隔: ~3 μm/スライス（フルスキャンの 2 倍）。

**パイプライン結果（全スライス、min_angle_spread=0）：**

| イベント | トラック数 | ベスト n | spread |
|---|---|---|---|
| IBUKI | 39,563 | 10 | 31.9° |
| NAGARA | 61,711 | 9 | 23.2° |
| MINO | 39,858 | 18 | 30.2° |
| KISO | 38,256 | 11 | 42.4° |
| IRRAWADY | 33,943 | 10 | 38.7° |
| D005 | 97,737 | 13 | 35.5° |
| D013 | 91,253 | 12 | **13.4°** |
| T004 | 141,982 | 10 | **18.0°** |
| T011 | 26,202 | 10 | **8.2°** |

D013, T004, T011 の spread < 20°: v4 フィルタでプライマリ vertex が除去される。
統合テストでは `min_angle_spread=0` を使用（フィルタ最適化は別問題）。

### 統合テストスイート（`tests/test_specials.py`）

`@pytest.mark.slow` テスト 2 種：
1. `test_special_reader_loads` — 全 13 イベント、スライス読み込み確認
2. `test_special_vertex_detected` — パイプライン全実行、`n_tracks_max >= 5` を確認

`pytest -m slow` で実行。デフォルト pytest では skip。

---

## 2026-05-10 — 目視確認; 角度広がりフィルタ; 実行トレーサビリティ; v4 ラン

### 目視確認とティーチャーデータ

`scripts/crop_vertices.py` を構築：**RAW** | **フォグ除去** | **二値** の 3 パネル。

v3 から 30 枚のクロップを目視確認（`n_tracks 5–12, n_slices ≥ 20, seed=7`）：

| カテゴリ | 件数 |
|---|---|
| 乳剤アーチファクト（クラック・ブロブ） | 7 |
| 良好なティーチャー（実粒子トラック） | 23 |
| **反応 vertex 候補** | **5** |

**重要知見**: `n_slices` が高くても本物の vertex とは限らない。
乳剤クラックは全スライスに渡って持続 → `n_slices` は純度の保証にならない。

**イベント種別：**
- ✓ **真の反応 vertex** — 多方向に細いトラックが放射状に出る。n_tracks ≈ 8+ で安定。
- △ **トラック交差**（偽陽性・軽微）— `max_ep_frac` で主に抑制済み。
- ✗ **乳剤アーチファクト偽**（偽陽性・主要）— 大きなクラックの端を Hough が検出 → 平行線の密集 → 小さな角度広がりの偽高多重度 vertex。

### 角度広がりフィルタ

`find_vertices()` に `min_angle_spread` を実装。
倍角トリック（[0°,180°) の方向を exp(2iθ) にマップ）で円統計的広がりを計算。

- アーチファクト vertex（n=34）: `angle_spread = 14.8°` → 閾値 ≥ 20° で除去。
- 真の反応 vertex: 広がりが大きい（多方向にトラック）。
- KEKCC ラン: `--min-angle-spread 20.0` を設定。

### v4 vertex ラン（min_angle_spread=20°）

KEKCC アレイジョブ 74625453 — 135 ジョブ完了。
出力: `results/vertices_merged_v4.parquet`（207,259 vertex）。

---

## 2026-05-09 — Vertex finding v2; パラメータ緩和

- パラメータ緩和（efficiency 優先方針）：
  - `min_len_px`: 100 → **50 px**（α トラック ~86 px を捕捉）
  - `min_intens`: 12 → **10.0**
  - `max_ep`: 100 → **150 px**
  - `beam_angle_cut`: 0 → **15°**
- v2 結果: **6,976,451 生 → 642,558 マージ** → 爆発。

**v2 爆発の根本原因：**
`max_ep=150` に相対カットなし。50 px の通過トラックの最近エンドポイント ≈ 25 px < 150 px → パスしてしまう。

**修正 — 相対エンドポイントカット（`max_ep_frac`）：**
- 絶対カット: `ep < max_ep`（150 px）
- 相対カット: `ep < max_ep_frac × トラック長`（0.5）

真の vertex トラック: エンドポイント ≈ 0 px → 常にパス。
通過トラック: エンドポイント ≈ 長さ/2 → 相対カットで除去。

- v3 結果（`max_ep_frac=0.5` 追加）: **1,754,298 生 → 221,278 マージ** ✓

---

## 2026-05-08 — 初回トラック解析・ピクセルスケール修正

- 2025 ビュー全体に `e07analyze` を実行 → `results/merged.parquet`（~24M トラック）
- スキャナ JSON のピクセルスケール誤りを発見：JSON では 3.0 μm/px だがスキャン形状から **0.29 μm/px** が正しい。`config/default.yaml` の `px_scale_um` を修正。
- 影響：既存 parquet の `grain_density` は 10 倍過小評価。再解析まで `n_grains` を直接使用。

**Hough パラメータ調整（目視確認）：**

| パラメータ | 旧値 | 新値 | 理由 |
|---|---|---|---|
| `hough_thr` | 20 | 35 | ノイズトラック削減 |
| `hough_ml` | 25 px | 50 px | 25 px = 7.3 μm；短すぎてノイズが多い |
| `hough_mg` | 4 | 5 | 僅かな改善 |
| `grain_radius` | 10 px | 15 px | 0.29 μm/px でのグレイン関連付け改善 |

**v1 vertex ラン（初回）：**
- パラメータ: `min_len=100, min_intens=12, max_ep=100`、ビームカットなし
- 結果: **95,160 生 → 8,468 マージ**
- 観察: 保守的すぎ；α トラック（~86 px < 100 px）を miss。

---

## (開発ログ注記)

---
